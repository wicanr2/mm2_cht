# 後續工作

「現在做到哪」在 [`CONTEXT.md`](../CONTEXT.md) §1.5，那張表是唯一狀態來源。
這一份只排**還沒做的事**。

## 怎麼讀這份清單

每一項有四欄，缺一欄就會被下一輪誤判：

| 欄 | 為什麼要有 |
|---|---|
| **卡在哪** | 沒有這一欄，看起來像「還沒排到」，實際上是「做不了」|
| **需要什麼** | 分辨「這台機器上就能做」與「要真機／要人耳／要另一個帳號」|
| **下一步** | 寫成可執行的動作，不是「再研究一下」|
| **不做的後果** | 有些項目永遠不做也沒關係，要講明白，否則每一輪都被重新撿起來 |

**劃掉的項目是結案，不要重開。** 使用者裁決過的寫上日期。
斷言被推翻時改的是正文，推翻紀錄集中在 `CONTEXT.md` §5。

## 2026-08-17 那一輪（使用者指定 G1 → A8 → O4 → O1 → O2 → O6）

六項都動過了，但**不是六項都結案** —— 分清楚才不會下一輪誤讀：

| # | 結果 | 殘留 |
|---|---|---|
| G1 | **結案** 玩家選攻擊目標接完 | —— |
| A8 | 量化完成、順帶解出三種牆面材質 | **仍開著**：要 openMSX 才分得出「分支走不到」還是「原版讀到殘影」|
| O4 | **結案** 門狀態複驗，順帶量到「開門只翻一側」 | —— |
| O1 | 扣費時點與提示序列結案，並修正「`C` 是施法」 | **拆出 O7**：戰鬥中的目標提示與傳送類數字，要高等級存檔 |
| O2 | **結案** 水行術旗標會活過存檔 | —— |
| O6 | **結案** 五首全部量到，其中兩首先前標反 | —— |

三個被推翻的斷言記在 `CONTEXT.md` §5。
外部條件那三項（R2／R3／R4）不在這個順序裡，等使用者的時機。

**下一批（使用者指定 2026-08-17）：O7 → A8**，也就是上面那兩個殘留項。

| # | 結果 | 殘留 |
|---|---|---|
| O7 | **結案** 戰鬥法術問 `On which (A-J)?`（全場，不是前排）且 `Esc` 不扣；傳送類的數字提示排在扣費之後，`Esc` 錢照扣 | 量測沒有殘留；**實作**那一半開成 G2 |
| A8 | **結案**：素材沒有缺（VRAM 是兩組素材輪流佔用同一片座標），而且找到真正的病灶 —— remake 的牆表抄自**戶外**渲染路徑，室內是另外六支函式、正牆還走不透空入口。換掉之後畫面是走廊 | 戶外視圖要接時才需要解材質選擇規則 |

**再下一批（使用者指定 2026-08-17）：G2 → G3**，兩項都結案：

| # | 結果 | 殘留 |
|---|---|---|
| G2 | **結案** 戰鬥中施單體法術先問打哪一隻，範圍是場上全部；Esc 不扣。「打幾隻」那張表靠讀 `cast.go` 語法樹防漂 | —— |
| G3 | **結案** 野外接上原版那條路徑（三組擋路物 ＋ 地形帶 ＋ `outf` 地板，每格查 `ds:52B2`）| 兩側的「與前一格連續」分支還沒照做 → **開成 G4** |

同一輪還修掉使用者指出的那張 MSX 截圖（牆表抄自戶外渲染路徑）
與 DOS／MSX 的場景素材切換（[`polish-spec`](polish-spec.md) P21）。

**再下一批（使用者指定 2026-08-17）：G5**，結案：

| # | 結果 | 殘留 |
|---|---|---|
| G5 | **結案** 室內第一人稱補到逐像素相同，12 個座標全部 0.0%。五個互相獨立的錯（縱列牆的條件、落點是表不是公式、正牆不透空、門要拿素牆當遮罩、火炬跟著牆種類走）＋ 一個零值當成牆的 bug | **剩 3 個像素** → 開成 **G7**。順帶把驗證迴圈從一分鐘降到幾秒（`cmd/mm2diff -oracle`）|

**再下一批（使用者指定 2026-08-18）：繼續 polish → G6**：

| # | 結果 | 殘留 |
|---|---|---|
| G6 | **四個平台接完**（Amiga、Mega Drive、高解析素材包的場景變體與野外；DOS 本來就有）。三個平台的素材都是**現成的**，只是 loader 沒接 —— Amiga 的檔案與 DOS 同名、MD 的 `scene.json` 早就帶著 `types` 對照 | **仍開著**：MSX 的戶外那組鑲嵌還沒解，貼圖參數抓得到、分派還沒讀 |

**下一批（使用者指定 2026-08-18）：MSX 的戶外（G6 殘留）→ G8**（視圖沒填滿，使用者當場指出）。

---

## 1. 要外部條件才做得了（這台機器上做不完）

| # | 項目 | 卡在哪 | 需要什麼 | 下一步 | 不做的後果 |
|---|---|---|---|---|---|
| R2 | 公開 repo 尚未建立 | 私有 repo 的歷史裡有由原版產生的 JSON（見 R1），所以公開版必須是**另建的乾淨一份**，不是把這一份轉公開 | GitHub 帳號操作，要使用者決定時機 | 照 [`release.md`](release.md) fresh init 一份；排除 `docs/research/soft-world/` 與 `data/hints.json`（《軟體世界》的研究內容）；在該 repo 跑 `tools/check_release.sh --public` | 專案維持 private。**不擋開發，只擋發行** |
| R3 | Windows／macOS 真機驗收 | 三平台封裝的 Docker smoke 都過了，但沒有真機開過。macOS 還要簽章與公證 | 一台 Windows、一台 Mac；macOS 另需 Apple 開發者憑證 | 真機各跑一次正常玩家路徑（進遊戲 → 走一格 → 打一場 → 存讀檔）；macOS 補簽章與公證 | 交付包只在 Linux 驗過。**Windows／macOS 使用者可能開不起來而我們不會知道** |
| R4 | 推廣片人耳驗收 | 現有 72 秒 1080p 版用的是玩家自備 `MM2.EXE` 轉出來的 DOS PC Speaker 音軌，沒有人從頭到尾聽過。**音樂那一段的阻塞已經解除**：Mega Drive 的 16 首都擷取出來了，remake 不給參數也會自己找音樂包，預設就是 Mega Drive | 人耳 | 用 Mega Drive 配樂重拍一次，人耳過一遍 | 推廣片可能有爆音、切點突兀之類機器聽不出來的問題 |

## 2. 可以在這台機器上驗（Docker 內跑得動）

這些是「remake 內部自洽，但沒有原版對照」的項目。**測試綠不算數**，
要對照原版才升級成已證實。工具已經備好：DOSBox 走
`tools/dosbox_run.sh`（timeline：wait／key／type／shot），Mega Drive 走
BlastEm 的 GDB stub。

| # | 項目 | 卡在哪 | 下一步 | 不做的後果 |
|---|---|---|---|---|
| ~~O4~~ | 門狀態 DOSBox 複驗 | **複驗完成**（2026-08-17）：不看畫面，直接傾印 DOSBox 記憶體讀屬性層。撞開米德格特 `(10,3)` 的北門 → 格 58 的 bit 6 由 1 翻成 0、地形層一個位元都沒動、同圖內走動與打完一場遭遇戰都維持 0；下樓到洞窟（地圖 17）再上來 → 屬性層與 `MAP.DAT` 的地圖 0 平面**逐位元相同**，沿路清掉的事件位元一併消失。順帶量到「開門只翻站的那一格」，已補 `TestOpenDoorLeavesTheOtherSideShut`。排路工具 `cmd/doorprobe`，證據見 [`door-state-oracle`](research/door-state-oracle.md)「實機複驗」|
| ~~O1~~ | 施法輸入的逐提示對照 | **主要幾條驗完**（2026-08-17）：施法入口不是移動畫面的 `C`（那是設定視窗）而是角色卡上的 `C`；提示序列是 `Cast Spell Level:` → `Number:` → 「`On whom (1-6)?`（答完立刻施放）」或「`'Return' to cast`」；**SP 在施放那一刻才扣**（提示中 14、Esc 後 14、施放後 13），原本掛「不得宣稱」的扣費時點結案；魔法偵測術不問哪一件物品、直接列出六格充能，remake 一致。**剩戰鬥中的怪物目標提示與需要數字的兩條**（傳送術／城市傳送術）——預設隊伍兩名施法者法力等級都是 1，學不到那些，要先養等級或另備存檔 —— **拆成下面的 O7**，不留在劃掉的列裡（劃掉的列沒人會再讀）。見 [`spell-interaction-oracle`](research/spell-interaction-oracle.md)「實機重播」|
| ~~O2~~ | 水行術旗標的存檔持久性 | **已證實（2026-08-17）：會保存**。原版的存檔是旅店登記，寫的是 `ROSTER.DAT`；`ds:03D5`–`ds:03E1` 那批旅行效果放在檔案**尾端 103 bytes**，水行術的 `ds:03D9` 是第 7 格。量法不必等高等級法術：用照明術當標記（存檔前後檔案只差兩個位元組：施法者 SP 與尾端那一格），**關掉 DOSBox 重開**再讀同一份名冊，`Light (1)` 還在；再把尾端改寫成遞增標記值讀回來，逐格反查出整張對照表。remake 存整個 `World.Globals`，`TestTravelEffectsSurviveSave` 守住。見 [`water-traversal-oracle`](research/water-traversal-oracle.md)「存檔持久性」|
| ~~O7~~ | 施法的戰鬥目標提示與傳送類數字 | **兩邊都量到了**（2026-08-17）。做法是**改名冊不改程式**（`cmd/mm2roster` 把施法者設成法力等級 9、SP 200、寶石 99、全學，跑的仍是原版自己的碼）。戰鬥中施單體法術問的是 `On which (A-J)?`，範圍是**場上全部**不是前排（同一場戰鬥的 `F-Fight` 問 `Fight which (A - E)?`）；`Esc` 不扣。傳送類相反：`Squares: (1-9)?` 與 `Town (1-5)?` 排在 `'Return' to cast` **之後**，那時候 `Esc` **SP 與寶石照扣**、隊伍不動 —— remake 刻意不照做，記為 [`polish-spec`](polish-spec.md) D4。邊界值不必另跑，收鍵的 root `sub_13268(lo,hi)` 定義就是「範圍內或 `0x1B`，其餘再讀」。剩下的是**實作**不是量測，見 G2。證據見 [`spell-interaction-oracle`](research/spell-interaction-oracle.md)「戰鬥中的目標提示與數字提示」|
| ~~O6~~ | Mega Drive 戰鬥結算那五首 | **五筆全部升成已證實，其中兩筆是反的**（2026-08-17）：BlastEm 在地城打同一場遭遇，**正反兩個方向各跑一次** —— 打贏（0 人死、6 隻死）命中 case 8 六次、case 6 零次；打輸（4 次隊員死、3 隻怪死）命中 case 6 四次、case 8 三次。所以 **case 6 是隊員死亡、case 8 是擊倒敵人**，先前標反了。錯的不是字串錨點而是「同一組詞兩份訊息都有」：隊員那份是 `␣goes down!`（`0x1CB0D`，前面有空格）、怪物那份是 `goes down!`（`0x1D587`），各自緊鄰自己的呼叫端。`tools/md_music_manifest.py` 已對調並重烘本機音樂包。見 [`md-music-scenes`](research/md-music-scenes.md)「戰鬥結算那五首」|

## 3. remake 還缺的機制

**這一格與上一格不同**：上面是「做對了但沒對照過」，這裡是「原版有、remake 沒有」。
G1（玩家選攻擊目標）在 2026-08-17 接完，同一輪的 O7 量出了下一項。

**逐像素驗證的迴圈已經不必每次都跑 DOSBox**：`cmd/mm2diff -oracle` 直接拿
上一次存下來的 `-orig.png` 當基準，一輪從一分鐘降到幾秒。第一次到某一格
還是要跑實機（要對記憶體裡的座標），之後改繪圖層都用 `-oracle`。

| # | 項目 | 卡在哪 | 下一步 | 不做的後果 |
|---|---|---|---|---|
| ~~G1~~ | 玩家選攻擊目標 | **接完**（2026-08-17）：攻擊與射擊都先問「打哪一隻」，近戰列前排、射擊列全場、都夾在 10，只剩一隻就不問，Esc 取消且不消耗回合 —— 全部照 `sub_18DAA` 開頭那一段（[`formats/08`](formats/08-combat.md)「打得到誰」、[`polish-spec`](polish-spec.md) P20）。`Encounter.Target` 指到的那一隻打不到或已經倒下時退回第一個站著的。驗收：`internal/ui/target_test.go` 七條 ＋ 截圖 `07b-target.png` |
| ~~G2~~ | 戰鬥中施法選怪物目標 | **接完（2026-08-17）**：單體攻擊法術先問「打哪一隻」，範圍是**場上全部**夾在 10（原版 `On which (A-J)?`，與近戰的 `Fight which (A - E)?` 不同），只剩一隻就不問，Esc 取消不扣 SP 與寶石。`Encounter.SpellTarget` 指到的那一隻倒下就退回原本的掃描順序。「這條法術打幾隻」另存一張 `spellMonsterCount`，靠**讀 `cast.go` 的語法樹**與 `spellEffects` 的 count 參數比對，改一邊忘了另一邊會紅。見 [`polish-spec`](polish-spec.md) P22 |
| ~~G3~~ | 野外的第一人稱素材 | **接完（2026-08-17）**：野外不是「換一組牆」而是另一條繪圖路徑 —— 三組擋路物（`OUTDOOR1-3`）＋ 地平線的地形帶（四個地形檔）＋ `OUTF` 地板，每一格畫什麼由**地形值查 `ds:52B2`**（0 不畫／1–3 擋路物／4 地形帶）；正面只畫最近的一個，兩側畫到那個深度為止。落點表全部從 DGROUP 讀出來，右側地形帶四筆的右緣都正好落在視圖右緣 216。`internal/view/outdoor.go`，驗收 `outdoor_test.go` 兩條 ＋ 截圖 `01l` 與實機 `42-outdoor.png` 對照。**剩一處沒照做**：兩側的「與前一格連續」分支（把相鄰兩格的樹接成一片），見 [`04-graphics`](formats/04-graphics.md)「野外是另一條路徑」|
| ~~G4~~ | 野外兩側的「與前一格連續」分支 | **接完（2026-08-17）**：最深那一格若同時有正面擋路物，兩側改用前一個深度的影格與 x、y 改用正面那一張；再前一格也有東西就整筆不畫。深度 1 的左側貼齊左緣，右側在 `right[0]` 空的時候用 176／152。**驗收是逐像素**：`cmd/mm2diff -map 11` 走出北門到指定格、讀記憶體對座標、再與 remake 比 —— `(7,3)N`／`(7,3)E`／`(7,6)N` 三處都是 **0.0%**（補之前 4.2%，差異是中距離少一片樹）|
| ~~G5~~ | 室內：兩側縱列的正牆 | **接完（2026-08-17）**：`cmd/mm2diff` 在米德格特與北門外的野外圖共 **12 個座標**都量到 **0.0%**（動工前 (8,0) 面東是 9.9%）。五件事各自是一個獨立的錯：① 縱列牆的條件是「這一側沒有側牆，而隔壁那一格有正面的牆」，不是跟著正牆無條件畫；② 落點是**表**不是「置中」公式，八個 y 有兩個各差 1 px；③ 正牆整塊不透空，門那一組的窗格是色號 8 的網紋；④ 側牆與縱列牆的門要拿**素牆那一張**當透空遮罩（兩張都是 8 才透空）；⑤ 縱列牆的火炬跟著那一面牆的種類走，用組 C 的影格，左右兩張表不能鏡射。順帶：`slot` 的零值是 `WallBarrier` 不是 `WallNone`，多畫了一整個深度（只漏出 3 個像素）。見 [`04-graphics`](formats/04-graphics.md)。**剩 3 個像素**：(7,4) 面西右側門的 (9,65)／(9,67)／(9,69)，成因與試過的兩條路寫在同一節 |
| G7 | 門那一組貼圖的透空規則還差 3 個像素 | (7,4) 面西右側那扇門（影格 24）的 (9,65)／(9,67)／(9,69) 三個色號 8 在原版是透明的（露出地板），**同一張影格**在柵欄那一區（列 20–36、行 14–23）的色號 8 卻是畫出來的 —— 任何「單一色號」規則都同時解釋不了這兩邊。目前用「素牆與門兩張都是 8 才透空」，那三格會多畫成灰 | 去反組譯裡找 `sub_1867C` 到底怎麼搬這塊：合理的猜測是門另外當一塊矩形貼（門會開），但還沒在碼裡找到那一步。試過而不成立的兩條寫在 [`04-graphics`](formats/04-graphics.md)：影格自己的「每直行頭尾透空段」（同樣差 3 個）、只拿素牆那張當遮罩（差 10 個）| 12 個座標裡有 1 個停在 3 個像素（0.012%），肉眼看不出來。**不擋任何事，但「逐像素相同」這句話目前有一個例外** |
| ~~G8~~ | MSX 的第一人稱沒有填滿視圖框 | **接完（2026-08-18）**：MSX 的視圖是 154×64，先前原尺寸置中放進 208×120 的框，寬只佔 74%、高只佔 53%。改成**先合成到 154×64 的離屏畫布、再整幅縮放一次填滿框**（`render.Screen.BlitHiFit`）—— 不是每一塊各自縮放，那樣相鄰兩塊會裂縫。長寬比差 8%（MSX 的像素是半寬），看不出來。**踩到一個坑**：`BlitHiFit` 一開始直接寫 Hi 層，被 `Flush` 從原版層整片蓋掉，症狀是視圖區全黑 —— 那看起來像素材沒載到，其實是沒有排進 `Flush` 之後的佇列。截圖 `01f`／`01g`／`01k` |
| G6 | 非 DOS 平台的場景切換與野外視圖 | **五個平台裡四個接完了**（2026-08-18）。素材本來就在，只是沒接：Amiga 有整套同名檔（`cave`／`castle`／`outdoor1-3`／`outf` ＋ 四個地形檔，張數與 DOS 逐項相同）；Mega Drive 的 `scene.json` 早就有三套區域與 `types` 對照（0／1 亂石砌、2／5 方石砌、3／4／6 戶外山景），loader 卻寫死只讀第 0 套；高解析素材包由 `cmd/mm2modern` 烘，改成四種場景一起烘。**剩 MSX 的戶外**：室內那四張表（`0x2020`–`0x2023`）早就接了，戶外走另一組鑲嵌（`0x2042`／`0x2043`／`0x2044`）與另一條繪製路徑（`sub_2B0A` → `sub_2A57` → `sub_1103`／`sub_1A40`／`sub_1C2B`），還沒解 | 貼圖參數**已經抓得到**：`tools/msxblits.py workplace/ida/msx_f004.asm` 掃出 102 處、100 處尺寸是立即值，目的座標減去 `(0,256)`（工作頁）就是視圖座標，而且是乾淨的結構 —— 左右成對（寬度加起來 154）＋ 置中的正面塊 ＋ 整條的地形帶，與 DOS 野外同構。缺的是**分派**：哪一筆對應哪個深度與哪個地形碼，要照室內那次的做法讀 `sub_2A57` 的跳表 | 按 `F6` 切到 MSX 走到野外會看到城鎮的牆。**其餘四個平台都對了** |

**編號 O3 去哪了**：舊清單的「戰鬥編隊與目標命令的細節」拆成兩半 ——
編隊那一半已經結案（前排隻數 `sub_19640` 的公式、死亡時 `ds:9FC5` 跟著縮的規則，
見 [`formats/08`](formats/08-combat.md)「前排」），目標命令那一半就是這裡的 G1。
編號不重用，寫在這裡是為了讓下一輪看得出它結了而不是掉了。

## 4. 反組譯筆記的缺口

835 個函式裡多數有筆記。**覆蓋率低不等於有缺口** —— root 的多數是
C runtime，不列。

| # | overlay | 有筆記 | 說明 |
|---|---|---|---|
| ~~N1~~ | `2SMITH` | **31/31** | **全解**：控制室見 [`re/05`](re/05-2smith-control-room.md)（已接進 remake，[`polish-spec`](polish-spec.md) P16），鐵匠見 [`re/07`](re/07-2smith-shop.md) |
| ~~N2~~ | `2CMDS` | **37/37** | **全解**，見 [`re/08`](re/08-2cmds-inventory.md) |
| ~~N3~~ | `1RETINN` | **12/12** | **全解**，見 [`re/06`](re/06-1retinn-roster.md)。順帶修掉一個會卡住玩家的洞：全滅之後所有按鍵都被吃掉，原版是回到最後投宿的旅店 |
| ~~N4~~ | `2COMBAT` | **116/116** | **全解**，見 [`re/09`](re/09-2combat-map.md)。順帶解出八個狀況位元的名字，以及整套怪物遠程／法術攻擊（三十二種，已接進 remake）|

寫筆記的收益本來只是「下一輪不必重開 IDA」，但**三次例外都在讀完之後才出現**：
控制室（N1）、全滅回旅店（N3）與怪物的遠程／法術攻擊（N4）都是讀完才知道
remake 少了整段機制。所以這幾項不能當成純文件工作跳過。

| # | 項目 | 卡在哪 | 需要什麼 | 下一步 | 不做的後果 |
|---|---|---|---|---|---|
| ~~N1b~~ | `ds:58B8` 印在載入之前 | **已證實**（2026-08-16）：不必走到終局 —— 事件的觸發地點寫在資料裡，用 `cmd/mm2patchevent` 把 `0e fd` 搬到起點旁邊就進得去。記憶體傾印顯示 `ds:58B8`–`ds:58BF` 全是 0，畫面第 19–22 列印出四行 `Version 1.01`。做法見 [`playtest/02`](playtest/02-event-relocation.md) |
| ~~N4b~~ | 怪物的遠程／法術攻擊 | **接完**：三十二種全部進 `internal/game/special.go`（[`polish-spec`](polish-spec.md) P18）。`ds:1436` 減 `0x11` 那一步也解掉了 —— 傷害那條路的 root `sub_13928` 會加回來，只有上狀況那條路沒加，remake 兩條都讀對的欄位（`polish-spec` D1）|

## 5. 素材與平台

| # | 項目 | 狀態 |
|---|---|---|
| ~~A1~~ | Amiga `.anm` 的動畫零件 | **解完**：72 個檔 530 個影格（72 基準圖 ＋ 458 零件）與 `0x31` 的動畫表都解出來並接進 remake。見 [`research/02`](research/02-other-platforms.md) |
| ~~A2~~ | Mega Drive 的場景素材 | **解完並接進 `F6`**，與執行時的組合緩衝區**逐像素相同**（24,960 個全中）。調色盤依區域類型挑、光照公式 `分量 × 亮度 ÷ 8` 也量出來了 |
| ~~A3~~ | 片頭疊圖的換格週期與觸發規則 | **解完**：`ds:18D8` 是 47 步的固定清單、`ds:1936`／`ds:1954` 是落點，一步 700 個 tick（PIT 除數 `0x0400` ⇒ 0.601 秒）。remake 照它播。見 [`formats/04`](formats/04-graphics.md) |
| ~~A4~~ | Mega Drive 還沒歸類的 LZSS 區塊 | **查完，`other` 歸零**：150 個裡 74 個是怪物圖的 nametable、74 個是 tile 池（68 個是帶區塊頭那族的重覆，位址差 14）、2 個是小張 tilemap。剩下六個沒歸類過的池裡，`0x09213C`／`0x0938DA`／`0x0943B8` 排在最後三張 nametable 前面 —— 那三張先前配到一百六十幾 KB 外的池，**畫出來是雜訊**。已修（`mdgfx.all_pools`）並重烘。另外三個早期的池與兩張小 tilemap 是 Mega Drive 自己的開機畫面（`0x002B66` ＋ `0x001FDC` 是 `NEW WORLD COMPUTING, INC.` 標誌），**remake 不採用** |
| ~~A7~~ | Mega Drive 的火炬 | **接完**：`sub_3836` 是十八段展平的程式碼，四個引數就是 nametable 的行列與寬高，鏡射常數 30，視圖座標 ＝ (行 − 2) × 8。53 個 tile 分成八張圖剛好分完。火焰動畫是 ROM `0x0BF4DC` 的 9 色表配三個取樣點（一相位 5 幀、整圈 45 幀）。remake 接了九組，含 DOS 沒有的正牆深度 1 |
| ~~A8~~ | MSX 沒定位的槽位 | **兩層都解決了（2026-08-17）**。一、「素材缺一塊」是錯的框架：貼圖參數是 **VRAM 絕對座標**，同一片座標由兩組素材輪流佔用 —— 室內載 `0x2020`–`0x2023`（462×128 蓋 x 0–461），戶外載一片鑲嵌（`0x2044` 蓋 x 308–503、`0x2042`／`0x2043` 蓋 x 308–489 × y 320–421，三個邊界分毫不差）。二、**remake 的牆表抄錯了路徑**：`sub_1103`／`sub_1A40`／`sub_1C2B` 是**戶外**的（`sub_2A57` 只從 `sub_2B0A` 呼叫），室內走 `sub_24FC` → `sub_3B83`（正牆 `sub_1E16` ＋ 門）＋ `sub_4752`（側牆 `sub_1F4D`／`sub_1FF9` ＋ 火炬）。正牆那條岩帶走**不透空入口 `0x6857`**，而工具只掃透空的 `0x685D`，所以它從來沒出現在抽出來的表裡。牆表換成室內那條、正牆改成三塊拼成的一條岩帶之後，畫面與 DOS 版同一個幾何（截圖 `01f`／`01g` 已重拍）。順帶：MSX 地圖檔就是 DOS 的（`0x0100`–`0x0109` ＝ 地形層 ＋ 屬性層 ＋ `ATTRIB` 記錄逐位元相同）；火炬九個位置、門三個深度全滿。**戶外的材質選擇規則**（`sub_2A57` 取牆種那個三位元欄位）留到接 MSX 戶外視圖時再解，不擋室內。見 [`research/02`](research/02-other-platforms.md) |

## 6. 已決定不做（不要再撿起來）

| # | 項目 | 決定 |
|---|---|---|
| R1 | git 歷史裡的五份原版產生的 JSON | **維持現狀**（`tools/check_release.sh` 已經把它當成「已決定接受」印成資訊而不是失敗，**工作 repo 的檢查是通過的**）。私有 repo 保留完整開發歷史；要清就得 `git filter-repo` ＋ force push，那一步沒有必要也沒有授權。公開時另建乾淨 repo（＝ R2）|
| A5 | Mega Drive 的光照 | **不做**（使用者決定 2026-08-16）：只採用 Mega Drive 的素材，不移植它的行為。公式與級數已量完存查（`research/02`「光照」）；remake 的暗處規則要接的話以 DOS 版為準 |
| A6 | Mega Drive 的 90 個英文文字區塊 | **不做**（使用者決定 2026-08-16）：那是 Mega Drive 自己的劇本，這個專案只採用它的素材。解出來的文字留在 `workplace/gfx/md-all/text/*.txt` 備查，不進對照流程 |
| O5 | 事件 `0x2a` 的獎賞領取 | **已變成設定**（2026-08-16）：`F2` 設定裡可切「自動入袋」與「按 S 領取（照原版）」。預設是自動 —— 事件流程沒有「按鍵前先把訊息掛著」的中斷點，自動比較順。見 [`polish-spec`](polish-spec.md) P11 |

## 7. 已經沒有未解項的區塊

不要再回頭盤點這些：

- **任務線**：兩位領主、八大職業考驗、競技賽、四位主教、Lord Peabody
  與年代之門、主線收尾 —— 全部解出並整理成 [`quests.md`](quests.md)
- **七個 overlay 全解**（`1MENU1`／`1RETINN`／`2CAVES`／`2BRAIN`／`2CMDS`／
  `2SMITH`／`2COMBAT`），其餘七個玩家路徑上的機制也追過，見 [`re/README.md`](re/README.md)
- **結局控制室**：守門的 Sheltem 戰、`WAFE` 中止碼、替代加密的密碼題、
  15 分鐘計時、通關結算與最終分數 —— 解完也接進 remake 了，
  見 [`re/05`](re/05-2smith-control-room.md) 與 [`polish-spec`](polish-spec.md) P16
- **角色記錄的旗標**：`+120`／`+124`／`+125`／`+129` 每一個位元、
  24 個全域夥伴旗標，見 [`re/03`](re/03-character-flags.md)
- **酒館**：五個選單項、酒與餐點的效果、四十則傳聞、`str.dat` 索引對應，
  見 [`re/04`](re/04-2brain-tavern.md) 與 [`tavern-rumors.md`](tavern-rumors.md)
- **檔案格式**：壓縮、圖形、字型、文字、地圖、事件、物品、怪物、法術
