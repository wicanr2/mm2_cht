# 門的開啟狀態 —— 存在哪裡、活多久

## 結論

門的開啟狀態存在**當前地圖的屬性層 `ds:5AD6`**（256 bytes，一格一個 byte），
不在地形層，也沒有第二份備份。

| 問題 | 答案 | 等級 |
|---|---|---|
| 成功後改了什麼 | `attr[格] ^= (terrain[格] & 方向遮罩) >> 1` —— 把該方向的牆位元翻掉 | 已證實 |
| 誰做這件事 | root `sub_13A64`，overlay 經 thunk `0x1765A` | 已證實 |
| 誰呼叫它 | 只有 `2MISC` 兩處：撞門成功 `0x1C20F`、開鎖成功 `0x1C326` | 已證實 |
| 地形層會不會變 | 不會。全十五個資料庫只有一處寫地形層，是事件腳本改地圖的 opcode | 已證實 |
| 前進、轉身、同圖內走動 | 保持開著（`sub_1BE24` 由地圖編號守門，不重載）| 已證實 |
| 離圖再進 | **關回去**。屬性層從 `MAP.DAT` 重讀 | 已證實 |
| 存檔重載 | 同上，讀檔後換圖一樣觸發重載 | 強推論 |
| `MAP.DAT` 會不會被改寫 | 載入器 `sub_160ED` 只用 `int 21h` 的 `3Dh`／`42h`／`3Fh`／`48h`／`49h`，沒有 `40h`；其他常式是否會寫同一個檔沒有掃過 | 載入器已證實，全檔未掃 |

推導鏈與位址在 [`docs/formats/06-map.md`](../formats/06-map.md)
「執行時的地圖緩衝區：誰載入、載入什麼」。

**同一個生存期還蓋到事件旗標。** 屬性層 bit 7 是「這一格有事件」，
事件 opcode `0x14` 清掉它表示用過了 —— 它跟門一樣，換圖就整層重讀。
remake 若把「事件只觸發一次」做成永久旗標，離圖再回來的行為會與原版不同。

反過來，自動地圖的走訪位元是另一份陣列（`ds:968C`，60 × 32 bytes），
以地圖編號索引，**不隨換圖重載**。兩者不要混在一起實作。

## 兩條開門路徑

`2MISC` 的兩個指令 handler，成功後都呼叫同一支 `sub_13A64`。

**撞門 `_2misc_e00`（`0x1C14E`）**

```
var_4 = (地形層[格] >> ds:59C7) & 3        ; 該方向的兩位元值
ds:039D == 1                     → 訊息 0xF2 + 重繪（室內外分流）
var_4 == 0                       → 同上（那個方向沒有東西）
(ds:59C8 & ds:59C6) & 55h == 0   → 同上（牆位元已經被翻掉 = 門開著）
var_4 != 2                       → 訊息 1（不是門）
力量 = 隊員[0] 的 +0x6B，隊伍 >1 人再加隊員[1] 的 +0x6B
擲 = rand(10,109) / 10
  擲 == 5                        → 直接成功
  力量 + 擲 >= ds:5998           → 成功        ; ATTRIB +18 撞門難度
  否則                           → 訊息 2（失敗）
成功後再擲 rand(1,100)：
  >= 51 → sub_13A64 開門，走訊息 0xF2 + 重繪
  <  51 → sub_1C41E 抽傷害、sub_1C390 施加到隊伍、ds:0430 = 3、重繪
```

**開鎖 `_2misc_e01`（`0x1C26E`）**

```
(地形層[格] >> ds:59C7) != 2               → 直接返回（不是門）
((地形層[格] & ds:59C6) >> 1) & ds:59C8 == 0 → 訊息 3（門已經開了）
sub_1C7AA 選人；回 0x1B → 走 loc_1728E 返回
擲 = rand(1,100)；技能 = 該員記錄 +0x1E
  擲 < 0x60 且 技能 >= 擲 → sub_13A64 開門 + 訊息 4（Success!）
  否則 再擲 rand(1,100)：
    ds:5999 >= 擲 → 訊息 2（失敗）    ; ATTRIB +19
    否則          → 陷阱：sub_1C41E、sub_1C390、ds:0430 = 3、重繪
```

**撞門成功不顯示 `Success!`**，直接開門重繪；`Success!`（訊息 4）只有開鎖有。

## 為什麼不再需要 DOSBox oracle

兩輪 DOSBox 窄任務都沒能在正常玩家路徑上觸發一次成功開門，
記錄留在下方。靜態證據已經把三個問題（改哪裡、誰改、活多久）全部答完，
而且是全資料庫掃描的封閉結論 —— 屬性層的**全部**寫入端只有六處，
`MAP.DAT` 的寫入端是零。實機驗證現在的角色從「取得答案」降為「事後複驗」。

掃描方法：`tools/ida_dsuse.py` 掃全部十五個資料庫，比對運算元的位移值。
**不能用 xref 圖** —— `[reg+disp]` 形式 IDA 不建 data xref，
查出來會是零，而零命中與「真的沒人碰」長得一模一樣。

## 輸入與工具雜湊

| 項目 | SHA-256／版本 |
|---|---|
| `workplace/orig/MM2/MM2.EXE` | `631facb658a39e0d438c451f8a43c9f6e2aeb774fc3843c1a9bac1e14bf8c4d4` |
| `workplace/orig/MM2/2MISC.OVL` | `c8291896a6db9b34564f44ea904f647c03ef2d5d18d09ee13ea52152a32e8b9f` |
| `workplace/orig/MM2/2PLAY.OVL` | `7078f30f87f9f25f8c296dc9207d70957c785b0ed83ad763e20e4012a82d2202` |
| IDA | `ida-pro-9.4-idapython:py312-v1`（IDAPython；基底 image 是靜默失敗）|
| 位址空間 | IDA 位址 = `0x10000` + 執行時偏移 |

## DOSBox 實機紀錄（負結果，保留存證）

原版從乾淨自備資料啟動，未使用 debug hook。`Return → S → G → Z` 穩定進到中門
第一人稱走廊；第二輪另抵達地圖 0 候選門 `(10,3,N)` 並由正常 `U` 指令操作，
只得到既有事件／噴泉文字。截圖在 ignored 的 `workplace/dosbox/shots/`。

| 截圖 | 雜湊 | 觀察 |
|---|---|---|
| `door-north.png` | `d657f226659f26bf1c294674616468b40a8715866bc074b8656608f514c17a01` | 正常路徑抵達中門走廊 |
| `door-reverse-bash.png` | `6250d8c048f47c580b78b0c185f1c6ca64f717eb21dc943094af74fe20545120` | 轉身後仍是第一人稱，未見成功訊息 |
| `door-reverse-n.png` | `e4eea6f06a28a44fd4711a6a311ed974f4b4cd8487715cd6333fc2946b5d78ff` | `N` 被後續流程當作隊伍選擇／輸入 |
| `door-bash-now.png` | `a600797a18e24f52a9afe3a2c4f5e1ce52760ed069afe4515b6f7ceb1b6f6ae4` | `B` 後仍停在原畫面 |
| `door-bash-n.png` | `a0677b06b697e306e88aefca66224c5b139630ba3db84a63efb614bf65007dff` | `N` 後進入解雇隊員提示 |

那兩輪順帶釘死一件事：**原版的撞門鍵是 `B`、開鎖是 `U`**；`D` 在原版是
`Dismiss whom (1-6)?`。remake 為了避開商店鍵而把撞門改成 `D` 並不對應原版。
