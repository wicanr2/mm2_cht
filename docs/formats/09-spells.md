# 法術系統

## 1. 一套編號貫穿三處

`SPELLS.DAT`、`2CAST1.OVL` 與 `2CAST2.OVL` 的跳表，用的是同一套 **0–95**
的法術編號：

```
0–47   巫師系（職業 2 弓箭手、職業 4 巫師）
48–95  牧師系（其餘職業）
```

分派寫在 root 的 `sub_15644`：

```asm
cmp  byte [bx+0Fh], 2      ; 弓箭手
je   不加
cmp  byte [bx+0Fh], 4      ; 巫師
je   不加
add  [bp-4], 30h           ; 其餘職業 +48
不加:
ax = [bp-4] * 2 + 7D60h    ; SPELLS.DAT 載入位置
```

**旁證**：`2CAST1` 的跳表第 0 項與第 49 項指向同一支 handler
（`sub_1CBEC`）。第 0 項是巫師系第 1 條、第 49 項是牧師系第 2 條 ——
兩者在手冊裡都是「喚醒術」。

`data/spells.json`（抄自手冊）的順序相反，所以 `cmd/mm2data` 產生
`spellcosts.json` 時就把兩半對調成手冊的順序。

## 2. 施法代價

見 [`02-data-files.md`](02-data-files.md) §9。摘要：

```
A 低 nibble   寶石消耗
B 位元 0–3    固定法力消耗
B 位元 4–6    每施法者等級的法力消耗
```

法力 = `固定 + 每等級 × 施法者等級`。

## 3. 會不會：角色記錄 +81

六個位元組 48 個位元，一位一條，低位在前。`Character.Knows(n)` 與
`Learn(n)` 走這個遮罩。

## 4. 效果：兩張跳表，還沒逐條解

`2CAST1.OVL` 的跳表在映像偏移 `0xD1CE`，**97 個 word**，指向六位元組的
trampoline；其中 43 個是實作，其餘指向同一個預設出口（`0x1D28E`）——
那些是由 `2CAST2` 處理的。

`2CAST2.OVL` 的跳表在映像偏移 `0xD080`，預設出口 `0x1D13C`，
**53 支實作**。兩張表的實作集合只重疊 5 條、聯集 97 條——
換任何一個候選起點，重疊都跳到 23–32 而聯集掉到 71–81。
這個統計是認定表頭的依據。

兩張表都用 **`SPELLS.DAT` 的順序**索引：`0–47` 巫師、`48–95` 牧師
（引擎內部的 `data/spells.json` 是反過來的，換算在 `cmd/mm2data`）。

已知的共用零件：

| 位址 | 作用 |
|---|---|
| `sub_1D046` | 顯示提示字串（`ds:315E`）並等 Enter。幾乎每支 handler 開頭都呼叫 |
| `sub_1CF5C` | 施法前的畫面準備 |

### 已解出的七條：牧師系的治療與解狀況

| 引擎編號 | 法術 | handler | 效果 |
|---|---|---|---|
| 3 | 急救術 | `sub_1CC5C` → `sub_1CE46(8)` | 狀況 `&= 2Fh`，生命 +8 |
| 7 | 治傷術 | `sub_1CCA8` → `sub_1CE46(0Fh)` | 狀況 `&= 2Fh`，生命 +15 |
| 16 | 解毒術 | `sub_1CCD6` | 狀況 `&= 77h` |
| 22 | 治病術 | `sub_1CD16` | 狀況 `&= 7Bh` |
| 30 | 恢復術 | `sub_1CD56` | 狀況 `= 0` |
| 33 | 解除石化 | `sub_1CD96` | 狀況**整個等於** `82h` 才作用，清成 0 |
| 39 | 復活術 | `sub_1CDCA` | 狀況**整個等於** `81h` 才作用 |

前五條都先檢查 `狀況 >= 80h`，是的話沒有效果。

**這七條反過來把狀況位元組（記錄 `+38`）的語意定出來**：每一支清掉哪幾位，
那幾位就是它治的東西。

| 位元 | 語意 | 誰清掉 |
|---|---|---|
| 2 | 疾病 | 治病術 |
| 3 | 中毒 | 解毒術 |
| 4 | 急救術治的那一項 | 急救術、治傷術 |
| 6 | 無意識 | 急救術、治傷術（`sub_1AFBC` 在生命歸零時設）|
| 7 | 嚴重（死亡／石化的高位）| — |

而 `82h` = 石化、`81h` = 死亡 —— 那兩條用 `cmp` 比整個位元組，不是測位元。

**代價的交叉驗證**：這七條的 `SPELLS.DAT` 值與手冊逐條相符 ——
1 SP、2 SP、3 SP、4 SP、5 SP+5 寶石、6 SP+6 寶石、7 SP+7 寶石。
七條全中，等於把上一節的欄位切法又驗了一次。

### 再十二條：全域計數型

handler 都只有十幾到三十幾個位元組，動作是「把某個 DGROUP 位元組加一或
設成一」：

| 引擎編號 | 法術 | handler | 效果 |
|---|---|---|---|
| 11 | 拒絕傷害 | `sub_1CCB4` | `ds:03D7` = 等級 + 20 |
| 15 | 製造食物 | `sub_1C87C` | 記錄 `+37` 食物 += 8，上限 40 |
| 19 | 水行術 | `sub_1C8C8` | `ds:03D9` = 1 |
| 21 | 風界傳送術 | `sub_1C8E0` | `ds:03DD` = 1 |
| 31 | 地界傳送術 | `sub_1C984` | `ds:03DF` = 1 |
| 35 | 水界傳送術 | `sub_1CA00` | `ds:03DC` = 1 |
| 41 | 火界傳送術 | `sub_1CA10` | `ds:03DE` = 1 |
| 52 | 照明術 | `sub_1C1B4` | `ds:03D5` += 1（上限 `0FEh`）|
| 59 | 飄浮術 | `sub_1C320` | `ds:03D8` += 1 |
| 61 | 魔法防護術 | `sub_1CC3A` | `ds:03D6` = 等級 + 10 |
| 71 | 守衛術 | `sub_1C550` | `ds:03DA` += 1 |
| 77 | 庇護術 | `sub_1C570` | `ds:0414` += 1 |

**這些位元組與事件腳本的全域變數是同一批**：四個界傳送術寫的
`ds:03DC`–`ds:03DF` 正是腳本選擇器 `0x27`–`0x2A`（見
[`07-event-script.md`](07-event-script.md) §12），所以腳本問得出
「開了哪幾道元素之門」。`internal/game` 兩邊共用同一個 `World.Globals`。

「等級 + N」讀的是記錄 `+0x71`（＝ +113）。名冊四十筆裡它與等級（`+32`）
逐筆相同 —— 那是等級的另一份。

### 2CAST1 的計數器型

照明系共用 `ds:03D5` 一個計數器，兩條法術用不同方式推它：
照明術 `+1`（上限 254）、持續照明術**一次 `+20`**（上限 235，
超過就設 255）。鷹眼術與巫師眼是**每級 `+5`、上限 250**，
兩支的組語逐位元組相同，只差計數器位址（`ds:03E0`／`ds:03E1`）。

強效治療是 `等級 × 1d10` 點治療 —— 與攻擊法術的 `sub_1A82C` 同形，
只是這一支把迴圈寫在 handler 裡。

| 引擎編號 | 名稱 | 計數器 | 增量 | 上限 |
|---|---|---|---|---|
| 4 | 照明術 | `ds:03D5` | +1 | 254 |
| 18 | 持續照明術 | `ds:03D5` | +20（一次）| 235 |
| 52 | 神聖之光 | `ds:03D5` | +1 | 254 |
| 55 | 鷹眼術 | `ds:03E0` | 每級 +5 | 250 |
| 67 | 巫師眼 | `ds:03E1` | 每級 +5 | 250 |

### 兩條要玩家輸入數字的傳送

原版用 `sub_16EC2(下限, 上限)` 讀一個按鍵，`0x1B` 表示取消。

**傳送術**（`sub_1C590`）讀 `1`–`9`，往面向的方向走那麼多格。
**每一步各自 `and 0Fh`**，不是一次加完再遮罩 —— 走九格會真的
繞一圈，落點與「加九再取模」不同。同樣不查牆。

**城市傳送術**（`sub_1CA20`）讀 `1`–`5`，`ds:0392 = 鍵 - 31h`
（地圖 0–4 是五座城），然後把 X 與 Y **都設成 `0FFh`**。
root 在 `ds:0393 == 0FFh` 時改用 `ATTRIB` `+14` 的預設進入座標 ——
**落點是資料決定的，法術本身沒有座標表**。

### 穿透術

`sub_1C722` 往面向的方向走一格，**完全不查牆** —— 這是全遊戲唯一
一條略過牆判定的移動。座標各自 `and 0Fh`，走出邊界就繞到對邊。

### 跳躍術

`sub_1C23E` 只在室內有效（`cmp ds:039D, 1 / je 失敗`，而它接下來查的
是室內那套牆位元組 `ds:59D6`／`ds:59C6`）。兩道牆各查一次：
目前格朝面向那面，以及中間格朝同一面。兩道都通得過才落在第二格：

	ds:0393 = (中間格 X + dx) and 0Fh
	ds:0394 = (中間格 Y + dy) and 0Fh

中間格本身也是 `and 0Fh` 算出來的，所以跳躍會跟著繞回對邊 ——
與一般移動同一套。

### 復活與年齡

`sub_13B68(記錄, n)` 是共用的加齡程序：`+33` 加 n，**上限 200**。

復活（`sub_1CAA4`）只對 `+38 >= 80h`（石化與死亡那一類）有效，
代價分三筆：

	sub_13B68(施法者, 1)      ; 施法者老一歲
	sub_13B68(目標, 5)        ; 目標老五歲
	+39 為 0 → 失敗
	+39 減一，同時寫進 +115   ; 幸運的兩份一起改
	+38 = 0                   ; 狀況清成正常

**幸運歸零就再也救不回來** —— 那是復活次數的上限。
`+39` 與 `+115` 一起寫這件事也確認了兩者是同一個值的兩份。

回春術（`sub_1C994`）也走加齡程序，但方向由擲骰決定：

	年數 = rand(1, 10)        ; 先擲年數
	擲值 = rand(1, 100)       ; 再擲成敗
	擲值 < 50 且年齡 >= 18 → 年齡減年數
	否則                    → 年齡加年數（走 sub_13B68）

**兩次擲骰一定都會發生**，成敗只決定年數的正負號。未滿 18 歲
一律變老 —— 那道年齡下限擋的是「一直回春回到出生」。

「選一名隊員」那批法術在原版走 `sub_1CF8C` 選單（回傳 `0x1B`
表示取消）。引擎用 `Session.Target` 代替，負值表示施法者自己。

### 兩條動到背包物品的

兩條都用**施法者自己**的背包（`ds:5DD6`），不是選隊員；
選物品走 `sub_1CB48`（回傳 `0x1B` 表示取消）。

**能量補充術**（`sub_1C648`）：`+64 + 槽位`（充能）加 `rand(1,6)`。
欄位本來就是 0 的直接失敗 —— 「這件東西沒有充能」與「充能用完」
共用同一個 0，所以用完就補不回來。

**加強法力**（`sub_1C774`）：`+70 + 槽位`（屬性）的低六位加一，
高兩位原樣保留。代價是 `50 × 目前值` 點法力，
**原版只檢查夠不夠、沒有真的扣**。低六位到 `0x3F` 時仍然會再加一，
進位撞進 bit 6 —— 引擎照抄，不修。

**複製術**（`sub_1C68C`）：把選中的那件抄到第一個空槽，
三個平行陣列（`+58` 編號、`+64` 充能、`+70` 屬性）一起抄。
**編號 `0xD0` 以上抄不了** —— 那道門檻擋掉的是任務物品那一段。

**去咒術**（`sub_1CB10`）：只在 `+64 + 槽位` 等於 `0xFF` 時動手，
把它改成 1。所以**充能欄的 `0xFF` 就是「被詛咒」的標記** ——
那一欄同時是充能次數與詛咒旗標，`0xFF` 是保留值。

這也解釋了能量補充術為什麼不能對 0 動手：`0` 是「沒有充能」、
`0xFF` 是「被詛咒」，中間才是真的次數。

### 兩條動到角色記錄的

**喚醒術**（`sub_1CBEC`，兩系都有）整隊掃過去，狀況位元組低於 `0x80`
的做 `and [記錄+38], 6Fh`。`0x6F` 清掉 `0x80` 與 `0x10`，而
`0x80` 那一路根本進不來 —— 所以它只清 `0x10`。一條叫「喚醒」的
法術唯一做的事就是清這個位元，**`0x10` 是沈睡**。

**回復陣營**（`sub_1C8F0`）把 `+13` 抄到 `+106`。所以
**`+13` 是原始陣營、`+106` 是目前陣營** —— `+106` 原本夾在金錢
（`+102`–`+105`）與屬性當前值（`+107`）之間，是個沒認出來的空格。

### 2CAST1 只管非戰鬥法術

把 43 支 handler 對回法術名之後看得很清楚：`2CAST1` 收的全是
「非戰鬥用」與「任何時刻」那兩類，**攻擊法術一條都不在裡面**。
戰鬥法術整批在 `2CAST2`。

### 攻擊法術的傷害公式（2CAST2）

攻擊 handler 的形狀一致：先呼叫 `sub_1D2AE` 選目標（回傳 `0x1B`
表示沒選到就整支跳過），或 `sub_1D23A` 確認場上有怪；擲傷害寫進
`ds:9FC6`；再呼叫 `sub_1714A(目標數, 目標編號, 屬性)` 結算。

`sub_1714A` 的第一個參數與手冊的「目標」欄逐條相符（1／2／3／4／5／6／10），
第三個是屬性：

| 值 | 屬性 | 依據 |
|---|---|---|
| 0 | 無 | 能量爆破術、飛劍術、粉碎術 |
| 1 | 火 | 火箭術、火球術、焚化術、烈火陣（手冊「除非怪物能抗火」）|
| 2 | 電 | 閃電箭、電擊術、超級電擊、高壓電擊術 |
| 3 | 冷 | 冷凍射線、冷凍術、冷凍光線、洪水陣 |
| 4 | 酸 | 酸液、噴酸術、后土陣（手冊「除非怪物能抗酸」）|
| 5 | 睡 | 催眠術 |
| 6 | 法術狀態 | 魔網、沈默術、定身術、麻痺術、死亡之指、幻影術 |

**設 `ds:9FC6` 的是傷害，設 `ds:9FC2` 加 `ds:9FC3 = 1` 的是狀態。**
兩者都走 `sub_1714A`，靠這兩組位址分辨。奇異之光術屬後者：
擲 `rand(1,9)` 選狀態種類，擲到 7 再擲 `rand(1,4)` 放進 `ds:9FC9`，
完全不碰 `ds:9FC6` —— 手冊的「不可預期之效果」就是這個。

擲骰用的是 root 的 `sub_11C88`，回傳 **`[lo, hi]` 兩端都含**的均勻值
（`cx = hi-lo+1`、`idiv`、餘數加 `lo`；`hi` 為 0 當 1、`lo > hi` 對調）。

| 引擎編號 | 名稱 | handler | 傷害 | 目標 |
|---|---|---|---|---|
| 10 | 傷痛術 | `sub_1CA6E` | `rand(1,12) + 3` | 1 |
| 20 | 噴酸術 | `sub_1CB8A` | `rand(1,49) + 11` | 3 |
| 27 | 致命蟲群術 | `sub_1CC34` | `rand(1,33) + 7` | 10 |
| 37 | 火焰枷鎖 | `sub_1CD7C` | `rand(255,400)` | 1 |
| 38 | 月光術 | `sub_1CDBA` | `rand(1,91) + 9` | 10 |
| 51 | 火箭術 | `sub_1C16A` | `rand(1,5) + 3` | 1 |
| 56 | 閃電箭 | `sub_1C1EA` | `rand(1,9) + 7` | 1 |
| 87 | 奇異之光術 | `sub_1C75C` | `rand(1,9)` | 10 |
| 90 | 隕石雨 | `sub_1C81A` | `rand(1,21) + 24` | 全部 |
| 94 | 星爆術 | `sub_1C8CA` | `rand(1,161) + 39` | 全部 |

多目標的 handler **逐隻重擲**，不是擲一次套用到全部
（`sub_1C7162(i)` 取第 i 隻的指標，迴圈內再擲一次）。

### 隨等級累加的那一批

另外 12 條走 `sub_1A82C(sides, bonus)`（在 `2COMBAT` 裡）：

	ds:9FC6 = 0
	n = ds:5DD6 指到的施法者記錄的 +113（等級）
	重複 n 次：ds:9FC6 += (sides 為 0 ? 0 : rand(1, sides)) + bonus

所以 `sides = 0` 的是「每級固定 bonus 點」，其餘是「**每級擲一次**
1d sides 再加 bonus」—— 逐級重擲，不是擲一次乘等級。

| 引擎編號 | 名稱 | handler | 參數 | 每級傷害 | 目標 |
|---|---|---|---|---|---|
| 50 | 能量爆破術 | `sub_1C130` | `(5,1)` | 1d5 + 1 | 1 |
| 62 | 酸液 | `sub_1C3D6` | `(5,3)` | 1d5 + 3 | 1 |
| 65 | 電擊術 | `sub_1C42E` | `(5,1)` | 1d5 + 1 | 4 |
| 68 | 冷凍射線 | `sub_1C4B4` | `(0,6)` | 6 | 1 |
| 70 | 火球術 | `sub_1C524` | `(5,1)` | 1d5 + 1 | 6 |
| 76 | 砂暴術 | `sub_1C620` | `(7,1)` | 1d7 + 1 | 10 |
| 81 | 冷凍術 | `sub_1C6B0` | `(0,10)` | 10 | 3 |
| 83 | 超級電擊 | `sub_1C6F8` | `(0,20)` | 20 | 1 |
| 84 | 飛劍術 | `sub_1C732` | `(11,1)` | 1d11 + 1 | 10 |
| 88 | 焚化術 | `sub_1C7B2` | `(21,19)` | 1d21 + 19 | 1 |
| 89 | 高壓電擊術 | `sub_1C7EE` | `(9,7)` | 1d9 + 7 | 10 |
| 93 | 地獄之火 | `sub_1C89E` | `(16,4)` | 1d16 + 4 | 10 |

手冊寫「每級 N 點」的三條**與程式碼完全相符**：冷凍射線每級 6、
超級電擊每級 20、焚化術每級 20—40。

### 傷害寫死的四條

| 引擎編號 | 名稱 | handler | 傷害 | 目標 |
|---|---|---|---|---|
| 14 | 冷凍光線 | `sub_1CB12` | 25 | 5 |
| 74 | 分裂術 | `sub_1C5A8` | 100 | 1 |
| 92 | 魔法黑洞 | `sub_1C86C` | 1000 | 1 |
| 42 | 扭曲重力術 | `sub_1CE7C` | 選定目標目前生命的一半 | 2 |

分裂術的 100 點與冷凍光線的 25 點與手冊**逐字相符**。扭曲重力術是
`ds:9FC6 = ds:9FAA[目標] >> 1` —— 在迴圈外算一次，兩隻吃同一個值，
不是各扣自己的一半（`ds:9FAA` 是怪物目前生命的 word 陣列）。

### 狀態類：`ds:9FC2` 的九個代碼

代碼減一之後同時索引兩張表：位元遮罩表 `ds:1022`
（`02 04 08 10 20 40 80 00`）與名稱表 `ds:0FEA`。位元 OR 進
`ds:9F86[槽位]` —— 那是怪物的狀態位元組。代碼 8、9 不設位元，
直接走死亡處理（`sub_18AF4`）。

| 代碼 | 位元 | 原文 | 譯名 |
|---|---|---|---|
| 1 | `0x02` | silenced | 沈默 |
| 2 | `0x04` | weakened | 衰弱 |
| 3 | `0x08` | frightened | 驚嚇 |
| 4 | `0x10` | slept | 沈睡 |
| 5 | `0x20` | held | 定身 |
| 6 | `0x40` | mindless | 心智渙散 |
| 7 | `0x80` | encased | 封困 |
| 8 | — | killed | 死亡 |
| 9 | — | disintegrated | 粉碎 |

**`0x18582` 一次 `test 0xB0`** —— 沈睡、定身、封困三個位元擋住行動，
其餘四個不擋。另外幾處各自單獨測：`0x1863A` 測沈默（不能施法）、
`0x18447` 測衰弱、`0x183F1` 測驚嚇、`0x184A1` 測心智渙散、
`0x18536` 測封困。

設 `ds:9FC2` 加 `ds:9FC3 = 1` 就是施加狀態，不設 `ds:9FC6`：

| 代碼 | 法術 | 目標 |
|---|---|---|
| 1 | 沈默術 | 4 ＋ 每級 1 |
| 2 | 衰弱術 | 10 |
| 3 | 幻影術 | 10 |
| 4 | 催眠術 | 4 ＋ 每級 1 |
| 5 | 魔網、定身術、麻痺術 | 4＋／5／10 |
| 6 | 衰弱心智術 | 5 |
| 7 | 狂風陣、洪水陣、后土陣、烈火陣 | 1 |
| 8 | 死亡之指 | 3 |
| 9 | 粉碎術 | 3 |

第三層對狀態的處置與傷害不同：**代碼小於 9 完全擋下**，
代碼 9（粉碎術）改判 50 點傷害。代碼 8（死亡之指）也在「小於 9」
那一邊，被擋下時目標完全沒事。

代碼 7 那四條再用 `ds:9FC9` 分元素（狂風 1、洪水 2、后土 3、烈火 4）；
奇異之光術是擲 `rand(1,9)` 隨機挑代碼，挑到 7 才擲 `ds:9FC9`。

### 抗性：三層

`sub_1714A` 的開頭把目標數夾在場上怪物數（`ds:0508`）之內，然後：

1. **抗魔法**：`ds:9E31` 非 0 時擲 `rand(施法者等級, 90)`，
   抗性大於擲值就擋下整個法術。
2. **屬性抗性**：前一層沒擋下且屬性不為 0 時查 `sub_18674(屬性 - 1)`
   —— 讀 `ds:9E36[屬性-1]`，**非 0 就完全免疫**，沒有機率可言。
3. **減半層**：擲 `rand(1, 191)`，不超過 `ds:9680[目標]` 時 ——
   傷害類**減半**（`shr ds:9FC6, 1`）、狀態類代碼小於 9 的**完全擋下**，
   代碼 9（粉碎術）改判 50 點傷害（`ds:9FC6 = 0x32`、`ds:9FC3 = 0`）。

   `ds:9680` 是場上每個位置的**怪物編號**（`sub_13B80` 拿它當
   `MONSTERS.DAT` 的索引去解記錄，`0x18228` 的佈場程序寫入）。
   怪物表照難度排序，所以編號本身就是強度 —— 這一層等於
   「越後面的怪越常吃到減半」，編號 191 以上必定減半。

最後若傷害是 0 也視為無效。

`ds:9E31` 來自怪物記錄 `+17` 的高 3 位元查 `ds:4DC0`：

	[0, 10, 20, 35, 50, 75, 90, 100]

解包在 `2COMBAT.img` 的 `0x13E1E`，同一個位元組拆成五份：
bit 3–4 進 `ds:9E30`、bit 5–7 **`& 7`** 之後查表，bit 0/1/2 則是
屬性抗性的其中三格（見下）。擲值上限是 90，所以理論上抗性 100
必定擋下 —— 但**256 隻怪物的索引只用到 0–6，第八格沒人用到**
（分佈 `{0:140, 1:4, 2:13, 3:44, 4:25, 5:18, 6:12}`）。

### 屬性抗性的七個旗標

`ds:9E36`–`9E3C` 七個位元組，索引 = 屬性參數減一。原版把它們從
怪物記錄的三個位元組的高位拆出來（`2COMBAT.img` `0x13D7A`–`0x13E19`）：

| 屬性 | 位址 | 來源 | 有旗標的隻數 |
|---|---|---|---|
| 1 火 | `ds:9E36` | `b23` bit 6 | 105 |
| 2 電 | `ds:9E37` | `b23` bit 7 | 68 |
| 3 冷 | `ds:9E38` | `b22` bit 6 | 170 |
| 4 酸 | `ds:9E39` | `b22` bit 7 | 100 |
| 5 睡 | `ds:9E3A` | `b17` bit 1 | 61 |
| 6 法術狀態 | `ds:9E3B` | `b17` bit 0 | 61 |
| 7 未定 | `ds:9E3C` | `b17` bit 2 | 54 |

`b22`、`b23` 的低五位是 AC 與傷害骰面數，高兩位一直是空的 ——
抗性旗標就塞在那裡。名字帶 `Fire` 的四隻全部有火旗標、零例外，
`Frost Dragon` 有冷旗標。

三層抗性與狀態的施加都已實作。**各狀態的持續時間與解除條件**
還沒解（在 `2COMBAT` 裡，`0x195BA` 把整個位元組清 0 是其中一處）。

手冊寫的傷害範圍是骰子式的（`2—8`＝2d4、`10—100`＝10d10……），
**上界與程式碼逐條相符，下界只有月光術對得上**。程式碼是裁決者，
表列的是程式碼的值；手冊那一欄的差距記在
`CONTEXT.md` §5。目標數目前取自手冊，等級是**強推論**。

### 隊伍增益與戰鬥旗標

`sub_1D13E` 與 `sub_1D170` 只是印字串（`ds:31CA`／`ds:31D3`）再暫停，
不是效果 —— 效果就在 handler 自己那三、四行。兩種形狀：

**全域計數器**（`cmp ds:XXXX, 0FFh / jae 跳過 / inc ds:XXXX`），
與 `2CAST1` 那十二條同一個形狀：

| 引擎編號 | 名稱 | 計數器 |
|---|---|---|
| 2 | 祝福術 | `ds:03E3` |
| 64 | 隱身術 | `ds:03E4` |
| 72 | 防護罩 | `ds:03E5` |
| 91 | 強力護罩 | `ds:03E6` |

**戰鬥中一次性**（`cmp X, 0 / jne 跳過 / inc X`），旗標落在
`ds:9FC0`–`ds:9FCD` 這段戰鬥用區域：

| 引擎編號 | 名稱 | 旗標 | 附帶條件 |
|---|---|---|---|
| 73 | 時間扭曲 | `ds:9FC8` | `ds:59A0` bit 3 成立就不行 |
| 80 | 陷敵術 | `ds:9FC4` | `ds:59A0` bit 0 成立就不行 |
| 44 | 神之干涉 | `ds:9FCD` | — |
| 6 | 驅魔術 | `ds:9FCB` | 只對不死生物（`ds:9E33`）|
| 45 | 神聖之咒 | `ds:9FCA` | 直接設 1 |
| 28 | 狂暴術 | `ds:9FC1` ＋ `ds:9FC0` | 選一名隊員 |

### `ds:59A0`：禁止哪些法術

`ds:59A0` 是 **`ATTRIB` 記錄的 `+26`**，隨地圖一起載進來 ——
在十四個 overlay 裡只有讀、沒有寫，連 root 也沒有寫入指令。
認出來的線索是 `ds:5994`：root 在 `ds:0393 == 0FFh` 時拿它當預設
進入座標拆 nibble，那正是 `+14`，於是記錄基底 `ds:5986` 也就定了。
被測到的五個位元各自對到具體法術：

| 位元 | 擋住的法術 | 測試處 |
|---|---|---|
| `0x01` | 陷敵術 | `2CAST2` `0x1C69C` |
| `0x08` | 時間扭曲 | `2CAST2` `0x1C594` |
| `0x10` | 傳送術 | `2CAST1` `0x1C5A6` |
| `0x20` | 穿透術 | `2CAST1` `0x1C72F` |
| `0x40` | 魯易浮標、飛行術、傳送到地面 | `2CAST1` `0x1C355`／`0x1C40A`／`0x1C93C` |
| `0x80` | root `0x14023` | — |

它**不是** `+13`：那一格是 0–100 的百分比，對百分比做
`test ..., 40h` 沒有意義。

`ds:9E33`（不死）也還沒解，所以附帶條件那一欄尚未實作。

### 傳送到地面用的三個位址

`sub_1C92A` 把地面返回點寫回隊伍位置：

	ds:0393 = ds:599C and 0Fh     ; X
	ds:0394 = ds:599C shr 4       ; Y
	ds:0392 = ds:599E             ; 地圖編號

所以 **`ds:0392` 是目前地圖編號**、`ds:599C` 是進地下城時記下的
地面座標（低 nibble X、高 nibble Y）、`ds:599E` 是地面地圖編號。
`ds:599C` 為 0 或 `ds:59A0` bit 6 成立時整條法術失敗。識別術（引 57）與神聖賜與（引 25）
是純顯示，前者印怪物資料、後者只印訊息。`internal/game/cast.go` 對它們照扣代價、
不假裝有效果（`CastResult.Effect` 是空字串）。

## 5. 誰會施法

牧師、巫師、遊俠、弓箭手。名冊四十筆裡只有牧師與巫師的法力欄位非零，
與手冊「遊俠與弓箭手要較高的經驗等級才有法力」一致。
