# 事件腳本

事件不是「踩到就顯示某條字串」，而是**執行一段腳本**。直譯器與 50 個
handler 都在 `2PLAY.OVL`（重建映像 `2PLAY.img`，位址換算見
[`01`](01-overlay-and-memory-layout.md) §2.5）。

## 1. 觸發流程

```
踩到格子
  → 查事件表，取得 Index 與 Kind          （docs/formats/02 §4）
  → 從腳本區開頭跳過 Index 段（0xFF 分隔）
  → 逐 opcode 執行該段
```

跳段的程式碼在 `sub_1A606` 開頭：讀 `arg_0`，反覆呼叫 `sub_18DC8`
掃到 `0xFF`，重複 `arg_0` 次。所以**事件記錄的 `Index` 是腳本段號**，
不是字串序號。

## 2. 直譯器

`sub_1A606` 的主迴圈：

```asm
call sub_18DC8          ; 讀 opcode
sub  ah, ah
sub  ax, 1              ; switch 50 cases
cmp  ax, 31h
jbe  short loc_1A673
add  ax, ax
xchg ax, bx
jmp  cs:jpt_1A676[bx]   ; 跳表
```

腳本指標是 `word_1042A`，腳本緩衝基底 `0x6052`：

```asm
sub_18DC8:              ; 讀下一個位元組並前進
        mov     bx, word_1042A
        inc     word_1042A
        mov     al, [bx+6052h]
```

## 3. 顯示字串的三個 opcode

```
01 NN   sub_1905E  → loc_16E79  靠左
02 NN   sub_19074  → loc_16EFD  開一個 0x26×0x16 的視窗
04 NN   sub_19110  → loc_16F52  算出長度後置中
```

三者共用同一組取字串的程序：

- `sub_18FD0` 讀一個位元組 N，把字串指標 `word_154C0` 重設到區塊開頭
  （`word_1042C`）再往後跳 N 條。
- `sub_19016` 從那裡讀到 `0xFF` 為止，存進 `0x54D0`。

`04` 的置中是算出來的：`repne scasb` 量長度、`sub cx, 1Ch`（欄寬 28）、
`neg`、`shr 1`。

## 4. opcode 長度表

腳本能一路走完不需要解出全部語意 —— 只要知道每個 opcode 吃幾個參數。

長度表本身在原版是一張查表，`sub_18F64`（跳過 N 個 opcode）用它前進：

```asm
loc_18F72:
        mov     bl, [si+6052h]   ; 讀該位置的 opcode
        sub     bh, bh
        shl     bx, 1
        add     si, [bx+15E6h]   ; ← 加上該 opcode 的長度
```

`ds:15E6h` 是 DGROUP 內偏移，而 DGROUP 落在檔案之外（執行時才配置），
所以表的內容從程式碼數出來：**長度 = 1 + handler（含它呼叫的子程序）裡
`sub_18DC8` 的呼叫次數**。`sub_18FD0` 內含一次，所以顯示字串類都是 2。

用全部 1,363 段腳本驗證這組長度：**83.7% 剛好走到段尾**。
以 `04` 開頭的段全部長 2、以 `0e` 開頭的段全部長 2，與表一致。
剩下 16% 是還有誤的少數 opcode。

## 5. handler 表

| opcode | 長度 | handler | 語意 |
|---|---|---|---|
| `0x00` | 1 | `sub_1B0B2` | 未解 |
| `0x01` | 2 | `sub_1905E` | **顯示字串**（靠左，`loc_16E79`） |
| `0x02` | 2 | `sub_19074` | **顯示字串**（開 0x26×0x16 視窗，`loc_16EFD`） |
| `0x03` | 2 | `sub_190F2` | 未解 |
| `0x04` | 2 | `sub_19110` | **顯示字串**（置中，`loc_16F52`） |
| `0x05` | 2 | `sub_19160` | 未解 |
| `0x06` | 2 | `sub_191EC` | 未解 |
| `0x07` | 1 | `sub_193B8` | 未解 |
| `0x08` | 1 | `sub_1940E` | 未解 |
| `0x09` | 1 | `_2play_e05` | 未解 |
| `0x0a` | 1 | `_2play_e06` | 未解 |
| `0x0b` | 3 | `sub_1947E` | 讀兩個位元組，`sub_18EE6` 換算後呼叫 `sub_17036` |
| `0x0c` | 3 | `sub_194D4` | 未解 |
| `0x0d` | 2 | `sub_19560` | 未解 |
| `0x0e` | 2 | `sub_19716` | 讀一個位元組當子命令（1/2/3/4/5/0x80…），設定 `word_1043A` |
| `0x0f` | 1 | `sub_198C8` | 未解 |
| `0x10` | 2 | `sub_198D2` | 未解 |
| `0x11` | 2 | `sub_198F2` | 未解 |
| `0x12` | 4 | `sub_19912` | 未解 |
| `0x13` | 4 | `sub_19984` | 未解 |
| `0x14` | 1 | `sub_19990` | 未解 |
| `0x15` | 5 | `sub_19A02` | 未解 |
| `0x16` | 3 | `sub_19ABC` | 未解 |
| `0x17` | 3 | `sub_19B20` | 未解 |
| `0x18` | 5 | `sub_19B38` | 未解 |
| `0x19` | 5 | `sub_19B44` | 未解 |
| `0x1a` | 3 | `sub_19C1A` | 未解 |
| `0x1b` | 2 | `sub_19C40` | 未解 |
| `0x1c` | 2 | `sub_19C5E` | 未解 |
| `0x1d` | 2 | `sub_19C72` | 未解 |
| `0x1e` | 2 | `sub_19C8A` | 未解 |
| `0x1f` | 7 | `sub_19E40` | 未解 |
| `0x20` | 7 | `sub_19F38` | 未解 |
| `0x21` | 1 | `loc_19F44` | 未解 |
| `0x22` | 3 | `sub_19F90` | 未解 |
| `0x23` | 1 | `loc_19FC6` | 未解 |
| `0x24` | 3 | `sub_1A01E` | 未解 |
| `0x25` | 3 | `sub_1A04C` | 未解 |
| `0x26` | 1 | `sub_1A082` | 未解 |
| `0x28` | 3 | `sub_1A126` | 未解 |
| `0x29` | 1 | `sub_1A19A` | 未解 |
| `0x2a` | 9 | `sub_1A1A0` | 未解 |
| `0x2b` | 2 | `sub_1A1E2` | 讀一個位元組；`byte_10509` 非零時呼叫 `sub_18F64` 跳過該數量的 opcode（條件分支） |
| `0x2c` | 2 | `sub_1A202` | 未解 |
| `0x2d` | 3 | `sub_1A21E` | 未解 |
| `0x2e` | 3 | `sub_1A386` | 未解 |
| `0x2f` | 1 | `sub_1A404` | 未解 |
| `0x30` | 2 | `sub_1A45A` | 未解 |
| `0x31` | 4 | `sub_1A4BC` | 未解 |
| `0x32` | 2 | `sub_1A570` | 未解 |

## 6. 現況

`internal/game` 逐 opcode 執行腳本，認得 `01`/`02`/`04` 三個顯示字串的，
其餘照長度跳過。**EVENTSI 的 1,865 個事件格裡，1,215 個（65.1%）
能顯示訊息**；剩下的是腳本裡沒有顯示字串、或長度表在該段有誤。

未解的 opcode 一律不猜語意：長度已知就跳過，長度未知就停在那裡。

## 7. 下一步

1. 修長度表剩下的 16%：從走不完的段反推是哪個 opcode 的長度有誤。
2. `0x2b`（232 段以它開頭）是條件分支 —— 解出 `byte_10509` 的語意
   就能正確處理分支，而不是把兩條路徑的字串都收進來。
3. `Kind` 欄位（高 nibble）推測是觸發條件（走到／搜尋／開門），待驗。
