# 原版遊戲資料 / 執行檔：一律不入版控（玩家自備合法副本）
*.zip
*.rar
# 其他平台的原版媒體。`.adf`（Amiga 磁片）、`.dsk`（MSX）、Mega Drive 的
# ROM 都是原版資料 —— 硬性原則 5：不散布原版執行檔、資料檔、美術或音樂。
# 目錄整個擋掉而不只擋副檔名，因為 Mega Drive 的 ROM 是 `.md`，
# 只擋副檔名會連文件一起擋掉。
#
# **開頭的斜線不能省。** 沒有斜線的 `amiga/` 會比對**任何一層**的同名目錄，
# 連 `internal/assets/amiga/`（Go 套件）一起擋掉 —— 而本機編得過、測試也過，
# 要到別人 clone 下來才會發現套件不見了。
/amiga/
/msx/
/genesis/
*.adf
*.dsk
*.dsk
workplace/
*.pdf

# 反組譯中間產物
*.i64
*.id0
*.id1
*.id2
*.nam
*.til
*.asm
*.gpr
*.rep

# Go
bin/
dist/
dist-all/
.local-full/
local-full/
*.exe
*.test

# 雜項
.DS_Store
*.log
Thumbs.db

# Python 快取（tools/*.py）
__pycache__/
*.pyc

# 翻譯工作檔含原版英文全文，不入版控；版控裡只有 translations/zh-Hant.json（譯文 + 原文雜湊）
translations/strings.json

# 由原版產生的資料（cmd/mm2data），是原版內容不入版控
data/opcodes.json
data/combat.json
data/encounter.json
data/specials.json
data/labels.json
data/experience.json
data/terrain.json
data/fields.json
data/traps.json
data/pictures.json
data/spellcosts.json
data/creation.json

# 攻略／說明書網站的產出。地圖由玩家自備的原版算出來，比照其他衍生產物不入版控。
# **要有開頭那條斜線。** 沒有的話 `site/` 會match 任何一層叫 site 的目錄，
# 連帶把原始碼的 docs/site/、tools/site/、docker/site/ 一起忽略掉 ——
# 而被忽略的目錄不會出現在 git status 裡，症狀是「檔案寫了卻沒進版控」。
/site/
