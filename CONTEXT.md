# CONTEXT — 專案脈絡與文件索引

這份是全專案的單一入口。對話被壓縮、或換一個新 session 接手時先讀這份，
再依索引跳到需要的文件。工作規範在 [`CLAUDE.md`](CLAUDE.md)。

最後更新：2026-08-16

---

## 1. 一句話現況

原版的壓縮、圖形（含怪物圖與動畫）、字型、道具表、事件字串已經解開並在 Go 上重實作；
引擎已經能跑：載入原版 `MAP.DAT` 與事件檔，在 16×16 的地圖上移動、
踩到事件格顯示繁中訊息（`cmd/mm2walk` 每步輸出 PNG，headless 可驗收）。
**牆的判定已解出，第一人稱視角已經畫得出來**：走廊、側牆、透視地板、
遠處的正牆、會動的火炬都到位，中文訊息疊在上面。事件腳本改成逐 opcode 執行，
六成五的事件格能顯示訊息。角色記錄已解，隊伍面板接上畫面，
存檔位元組層級可往返。**框線照原版量出來的座標，框裡放什麼按中文重排** ——
機制照反組譯，呈現以看得懂為準。

**中文化 2,702 條全部完成**（事件字串、`STR.DAT` 全文、怪物名、物品名，
以及 `MM2.EXE` 尾部那 787 條 UI 與播報文字）。`STR.DAT` 原本以空行分組，
控制室那六張表擠在同一條 key 裡；匯出器已依原版程式碼的讀取邊界補切
（`strHardSplit`），條數因此從 2,695 變成 2,702，譯文一個字都沒改。

**引擎與資料分開**：查表、標籤、法術全部走 `data/*.json`，Go 只留規則。
原版衍生的資料由 `cmd/mm2data` 從玩家自備的 `MM2.EXE` 產生，不入版控。

## 1.5 唯一目前狀態表（2026-08-17）

這張表是 remake 收尾的唯一工作入口；後面的歷史筆記與已完成清單只用來追證據，
不能單獨把已結案項目重新打開。
**還沒做的事排在 [`docs/todo.md`](docs/todo.md)**，那一份每一項都寫了
「卡在哪／需要什麼／下一步／不做的後果」四欄，並且把
**「要外部條件才做得了」（真機、人耳、GitHub 帳號）**、
**「這台機器上就能驗」**、**「remake 還缺的機制」**、
**「已決定不做」**分開放 —— 混在一起會讓下一輪把「做不了」誤讀成「還沒排到」。
這張表講狀態，那一份講下一步。綠色測試只證明 remake 內部自洽，沒有原版 oracle
的項目不可宣稱 parity。

| 類別 | 目前狀態 | 下一個可驗收條件 |
|---|---|---|
| remake 已完成 | 從名冊進 Middlegate、城鎮／野外移動、即時事件 Y/N、設施入口、戰鬥、非魔法物品效果、繁中與 JSON 存檔都有正常 UI 路徑與自動測試；96 條法術規則已在引擎內實作。**全滅不再卡死（2026-08-16）**：原本 `ModeDead` 把所有按鍵吃掉，現在照原版回到最後投宿的旅店（[`polish-spec`](docs/polish-spec.md) P17）。**結局控制室已接（2026-08-16）**：`0e fd` → 守門的 Sheltem 與四隻元素生物 → `WAFE` 中止碼 ＋ `+129` bit 5 → 替代加密的密碼題（15 分鐘倒數）→ 每人五千萬經驗與最終分數。**密碼題是刻意的差異**：中英並陳、答案附在輸入欄旁，比對邏輯不動（理由見 [`polish-spec`](docs/polish-spec.md) P16）。勝敗場數（`ds:0410`／`ds:0412`）補進存檔。**2026-08-16 的一批介面調整**：功能鍵在任何畫面都固定（`F1` 說明、`F2` 設定、`F4` 存檔、`F10` 離開並先自動存檔、`Esc` 一律取消），戰鬥多了 `F8` 快速戰鬥（走的是同一條回合結算，不是另寫一套），事件 `0x2a` 的獎賞領取方式變成設定裡的開關（預設自動、可切回原版的按 `S`）。音樂不給參數也會自己找音樂包，**預設 Mega Drive**。| 每輪保持 `go test ./...` 與最小正常玩家路徑抽測。 |
| remake 施法輸入 | 隊員、施法者背包物品、數字與飛行欄列提示已接正常 `C` 選單；確認前不呼叫 `Cast`，Esc 不扣 SP／寶石，提示不進存檔。只依目前 handler 的 typed consumer 接線，未證實怪物目標不猜補。見 [`spell-interaction-oracle.md`](docs/research/spell-interaction-oracle.md)。| **主要幾條已補（2026-08-17）**：施法入口在角色卡不在移動畫面（移動的 `C` 是設定視窗）、提示序列 `Cast Spell Level:` → `Number:` → `On whom (1-6)?`／`'Return' to cast`、**SP 在施放那一刻才扣**（提示中不扣、Esc 不扣），remake 一致。剩戰鬥中的怪物目標提示與傳送／城市傳送那兩條數字，要高等級存檔。|
| 門狀態（已結案）| **機制全部解出並接進 remake（2026-08-14）**：開門寫的是屬性層 `ds:5AD6`，`attr[格] ^= (terrain[格] & 方向遮罩) >> 1`；唯一執行者是 root `sub_13A64`，唯二呼叫點是 `2MISC` 撞門成功 `0x1C20F` 與開鎖成功 `0x1C326`。屬性層只在**地圖編號改變**時由 `sub_1BE24` 從 `MAP.DAT` 重讀，`MAP.DAT` 沒有寫入端 → **同圖內門保持開著，離圖再回就關回去**；事件 bit 7 生存期相同，自動地圖走訪位元（`ds:968C`）才是持久的那一份。見 [`door-state-oracle.md`](docs/research/door-state-oracle.md)、[`06-map.md`](docs/formats/06-map.md)。**remake 已接**：`World.OpenDoor` 翻屬性層位元、`CurrentMap` 在換圖時還原兩層、撞門補上原版「力氣過了再擲一次，`< 51` 觸發陷阱」那一段；`internal/game/door_test.go` 四條驗收（開門只動屬性層、離圖回來門關上、事件旗標同生存期、開門只翻站的那一格）全綠。| **複驗完成（2026-08-17）**：傾印 DOSBox 記憶體讀屬性層 —— 撞開 `(10,3)` 北門後 bit 6 由 1 翻成 0、地形層不動、同圖內維持；下洞窟再上來，整層與 `MAP.DAT` 的地圖 0 平面逐位元相同。順帶量到「開門只翻站的那一格」（`TestOpenDoorLeavesTheOtherSideShut`）。|
| remake 水域 gate | 靜態控制流已證實只有 `terrainClass == 4 && ds:16DA == 0x0A && ds:03D9 == 0` 才顯示 `Can't swim!`；`EnterOutdoor` 已依 `ATTRIB +4` 低 nibble 接入，水行術設 `03D9`，休息／已定位換圖路徑清 `03D5–03E1`，remake JSON round-trip 保留旗標。Docker `go test ./internal/game/...` 通過。**`ds:16DA` 已升級為已證實（2026-08-14）**：寫入端只有 `2PLAY` 兩處，值就是 `ATTRIB +4` 低 nibble，`0x0A` ＝ `ocean.16` 貼圖組（地圖 7、8、11–16；地圖 41–44 改查 `ds:16B3` 表）。`03D5–03E1` 全段寫入端也掃完，**沒有船旗標**，整段只有 `03D5` 每步遞減。見 [`water-traversal-oracle.md`](docs/research/water-traversal-oracle.md)。| 原版正常玩家的休息、換圖、DOS save/load replay 仍是 oracle unknown；有可重播存檔後才補 exact parity。|
| remake 一般寶箱 | 一般戰鬥勝利已接到 `Encounter.VictoryChestFromItems`→`ui.Session.Chest`，勝利後自動開四選單；怪物 `+0x10` 掉落欄位、`sub_188FC` 金幣／寶石累加、`sub_19BF8` 0–3 件物品生成均有 typed 鏈。快速戰鬥、正常 UI、逃跑移除前累加與離開清除均有測試；競技賽與事件 `0x2a` 分支保留。**「探索中搜尋寶箱的來源」已結案（2026-08-14）：原版沒有那條路徑**，`ds:0434`／`ds:6950–695F` 的八個寫入端全部掃出，內容只來自事件腳本與戰鬥戰利品。見 [`chest-trigger-oracle.md`](docs/research/chest-trigger-oracle.md)。**觸發點也結案了（2026-08-15）**：四選單由探索模式按 `S`（搜尋）打開 —— `2PLAY` 指令分派 `0x181E8` → thunk `0x17336` → root `0x13814`，那支五組獎賞陣列全空就印 `Nothing Here!`。`_2misc_e02` 的 thunk 全域只有這一個呼叫端，所以原版**所有**獎賞都只能經搜尋領取。`ds:0434 == 0xFE`（神殿）由主迴圈 `0x18032` 自動 `inc` 成 `0xFF` 並自動搜尋，先前列為「語意未定」的那條就是它。remake 已改成戰後擺著、按 `S` 才開，並補上「這裡什麼都沒有」。見 [`command-keys-oracle.md`](docs/research/command-keys-oracle.md)。| 事件 `0x2a` 那條 remake 仍自動發放（原版要按 `S`），差異理由記在 [`polish-spec`](docs/polish-spec.md) P11。|
| remake 怪物動畫 | 使用者選擇先接可逆的保守方案：正常戰鬥 UI 逐步遵守 `MONSTERS.16` 的原始 hold，播放每張圖第一個完整合法序列；無合法序列退回基準圖，換場重設。這證明動畫資料與 UI 垂直鏈已接通，但序列對應待機／攻擊／受擊仍是強推論，不宣稱 DOS exact。**動畫表的結構已修正（2026-08-15）**：沒有「表頭」，`FF` 是每一段的結束標記，59 個槽共 240 段（原本記 181 是把 59 個第一段當表頭扣掉）；槽 9 檔案裡那張表是壞的，原版在 root `0x12711` 用 `ds:4B92` 的 36 bytes 修補，remake 已照做。**第一段不是動畫是播放腳本**（段編號, 停留），bit 7 ＝「`rand(1, 低7位)` 隨機挑一段」（root `0x15735` 呼叫的正是亂數產生器）；136 個腳本項的段編號一律落在 1..段數-1，零例外。影格編號越界也不是壞資料 —— 原版比對影格數後畫影格 0。remake 已照腳本挑段並隨機化。| 日後取得 DOS 連續戰鬥 oracle 時，只替換序列用途選擇；不得推翻已證實的影格、序列與 hold 解碼。|
| remake 本機音樂 | Mega Drive／MSX／Amiga 三版均已由序列器與正常呼叫證實有音樂；Mega Drive 的 16 首場景曲目最完整。引擎已有 PCM WAV 音樂包 manifest、完整性／路徑安全驗證、正常 UI 的城鎮／地城／戶外／戰鬥／設施映射與循環播放器；缺少部分包角色時明確靜音。原版音檔不進版控，`local-full --music-pack-dir` 才可帶入。見 [`music.md`](docs/music.md)。| **擷取工具鏈已完成並驗證（2026-08-13）**：`mm2-blastem:0.6.3-pre-732f5689d438` ＋ libvgm `61fc6725` 的 `vgm2wav` ＋ 檔頭正規化，`tools/blastem_run.sh` 一次執行從 ROM 走到 remake 解碼器實際吃得下的 WAV。**18 首曲目已由本專案自己解出**（不再依賴 VGMRips 的外部清單）：每首前 4 bytes 是結束指標，18 個 `move.l (sp)+,-$37B0(a5)` 前面 100% 是 `move.l #<曲目位址>,(sp)`，零例外。觸發點也定了 —— 往 RAM `$FFCB62` 寫一個 32-bit 曲目位址，下一次 vblank 就換曲，不必玩到那個場景。選曲的 21 個 case 也解完（`0x0B620` 的跳表 switch，case 19 依另一變數在四首間分支），相異曲目正好 18 首，與位元組掃描互相印證。**逐首擷取已打通**：ROM 一個位元組都不改（這片改任何一個位元組就開不了機，機制是 `sub_BFC28`：ROM `0`–`0xBFC28` 的長字加總必須等於 `0x3ACE1FBA`），改用 GDB remote stub（`blastem -D`，走 stdio）在 `0x06CB02` 下中斷點、每幀寫 RAM `$FFCB62`、壓 40 幀並驗證 `$FFCB5E` 切過去才錄。`tools/md_music_dump.sh` 一次跑完 18 首。**場景對照也解出來了**：39 個呼叫端（11 直接 ＋ 28 走 a5 thunk，只掃直接呼叫會漏七成）逐一對到 case 與曲目，再用地圖區間（`sub_FB86`）與字串錨點定語意。**16 個角色全部已定，對到 16 首不同曲子**；九個標已證實 —— 四個區域主題靠地圖區間與 DOS 版語意互相印證，五個設施（旅店 case 11／鐵匠 13／酒館 14／神殿與法師公會同為 15／訓練所 16）是**在模擬器裡逐一走進去量的**：選曲函式下中斷點，記下 `d0` 與回傳位址，配店內截圖。走法由 `tools/md_town_route.py` 從 DOS 版 `MAP.DAT` 的牆位元 BFS 算出（兩版地圖佈局相同，六個入口座標完全吻合），不必盲試。音樂包由 `tools/md_music_manifest.py` 產生，過 remake 自己的 `music.LoadManifestFile` 驗證。**場景對照一律用反組譯推、不用人耳**，整條推導在 `tools/md_music_scenes.py` 可重跑。見 [`md-music-scenes.md`](docs/research/md-music-scenes.md)。現有推廣片仍標為 DOS PC Speaker。見 [`md-music-driver.md`](docs/research/md-music-driver.md)。|
| Mega Drive RE | **收尾（2026-08-14）**。缺口清單清空：第一人稱繪製鏈（`sub_FC38` → `sub_3DE2` → DMA）、視野貼圖格式、六個設施與戰鬥的音樂對照（走進去量的，配截圖）、開機完整性檢查 `sub_BFC28`、隊伍座標 `0xFFF3A4`／`0xFFF3A6`、角色記錄 `0xFFCE9F`＋134 全部解出。動態工具鏈完成：`md-trace`／`md-walk`（依 SP 判 modal、`--expect` 逐步核對座標）／`md-fight`（自動戰鬥＋無敵）／`md-ram-diff`。見 [`md-re-status.md`](docs/research/md-re-status.md)。| **2026-08-15：那五個角色現在播得到了。** remake 加了一次性音效（stinger）路徑：戰鬥中消滅敵人／隊員陣亡、打贏、全滅、按 `S` 撿到戰利品各對一個角色，一幀只播一個（依重要性挑）。`castle` 也接上了 —— `2PLAY _2play_e10` 的跳表 case 2 與 5 推 `castle*.16`（檔名從 DGROUP 初值段解指標讀出來的），對上場景碼區間就是地圖 45–59。所以**戰鬥結算那幾首的強推論現在會被玩家聽到**，值得補一次動態驗證。`intro` 也接上了（2026-08-15）：remake 開機先進片頭，按任意鍵進遊戲。**2026-08-16：第一人稱場景素材也接進 `F6` 了。**視野貼圖表（`0xFFCD40`，60 個長字）、二十個落點、地板與天空、調色盤（`0x6FC2A` ＝ CRAM 第 2 條）全部解出來，用 BlastEm 傾印的視野格子陣列組出來的畫面與模擬器自己的組合緩衝區**逐像素相同**（24,960 個全中）。工具是 `tools/mdscene.py`，載入是 `loadMDTown`。**光照也量出來了**：亮度 0–8 九級，`分量 × 亮度 ÷ 8`，實際只用四個穩態級（8 正常、6 夜晚、2 無光源、0 換場起點），過渡由 `sub_B880(起,迄)` 每幀走一格；日夜另有一組目標 `-$35D4(a5)`（晝 8、夜 6）。**素材也全部抓出來編目了**（`tools/mdassets.py`）：tile 池 72、視野點陣圖 91、整頁介面 tilemap 3（建角／探索／戰鬥，這一輪新解）、裸 tile 池 2、調色盤 80 組。**火炬也接了（2026-08-16）**：`sub_3836` 是十八段展平的程式碼，四個引數就是 nametable 的行列與寬高，鏡射常數 30，視圖座標 ＝ (行 − 2) × 8 —— 整組位置從靜態反組譯就讀得出來，不必再取樣。53 個 tile 分成八張圖剛好分完（10+8+4+1+18+8+3+1），這是版面的獨立驗證。remake 接了九組，含 **DOS 版沒有素材的正牆深度 1**。火焰動畫也解完：`sub_7F78` 拿 ROM `0x0BF4DC` 的 9 色表，第 5／6／7 格各取 `n`／`n+3`／`n+1`，一相位 5 幀、整圈 45 幀、不吃光照；火炬自己那一條調色盤在 ROM `0x6D03C`，**顏色靜態就讀得到**，實機量測確認的是機制與週期。**素材編目也收尾了（2026-08-16）**：先前列為「還沒歸類、多半是假陽性」的 150 個 LZSS 區塊全部歸類完（74 張怪物 nametable、74 個 tile 池含 68 個重覆、2 張小 tilemap），`other` 歸零。過程中抓到三張畫壞的怪物圖：`0x09213C`／`0x0938DA`／`0x0943B8` 這三個**沒有區塊頭**的 tile 池排在最後三張 nametable 前面，而 `pair_pool` 只看帶頭的池，那三張因此配到一百六十幾 KB 外的池、**整張畫成雜訊**——而雜訊與「這張本來就長這樣」分不開。已修並重烘。 |
| remake 片頭 | **已完成（2026-08-15）**：`MASTER.16` 十五張裡 320×196 的那張是底圖，其餘 14 張是疊在上面的動畫。透空色 **7**、每張的落點都用 60 張 DOSBox 截圖逐像素定出來（第 0 張在 (160, 161)：只有 key=7 能零誤差，落點錯一格不合像素從 0 跳到 391）。13 張全部在 27 秒的取樣裡出現過。獨角獸的頭尾常態交替，樹叢裡幾張臉偶爾探出來。見 [`04-graphics.md`](docs/formats/04-graphics.md) §標題畫面的疊圖動畫。| **換格與觸發規則也解完了（2026-08-16）**：`1MENU1` 的 `sub_1C1FA` 讀 `ds:18D8` 的 **47 步固定清單**（不是亂數），落點在 `ds:1936`／`ds:1954`，與截圖反推的位置逐項相同；一步是 `sub_14EFE(0x46)` ＝ 700 個計時器 tick，而 `TIMER.DRV` 把 PIT 除數設成 `0x0400`（1,165.2 Hz），所以 0.601 秒一步、47 步 28.2 秒，跑完原版自己進主選單。remake 照這張清單播。第 5／6／9 張各有 3–9 個像素與截圖不合，原因未查。|
| DOS 反組譯覆蓋 | **稽核過（2026-08-15）**：14 個 overlay ＋ root 共 835 個函式。目錄總表與各 overlay 的狀態在 [`docs/re/README.md`](docs/re/README.md)。`1MENU1`、`2CAVES`、`2BRAIN` 三個 overlay **全解**。角色記錄 `+125`／`+129` 的每一個位元、`ds:03F6` 起 24 個全域位元組（＝可雇用夥伴的解鎖旗標）、三隻獸旗標的寫入時機見 [`docs/re/03`](docs/re/03-character-flags.md)；酒館的五個選單項、酒與餐點的效果表、餐點在 `+118` 留下的旗標、依日期輪替的傳聞，以及**七張執行時載入表的 `str.dat` 索引**（訊息 `n` ＝ `str.(114+n)`，由三個獨立呼叫點驗證）見 [`docs/re/04`](docs/re/04-2brain-tavern.md)。**`1RETINN` 也全解了（2026-08-16）**：旅店登記、名冊與隊伍編組（一般角色 6 人、連雇傭兵 8 人的兩層上限）、五座城的離開落點表（`ds:21E8`／`21EE`／`21F4`，第 0 項 (7,3) 面 `'N'` 與 DOSBox 量到的起點吻合）、以及**全滅回到最後投宿的旅店**（`_1retinn_e04`），見 [`docs/re/06`](docs/re/06-1retinn-roster.md)。任務線的玩法在 [`docs/quests.md`](docs/quests.md)，四十則酒館傳聞的中英對照在 [`docs/tavern-rumors.md`](docs/tavern-rumors.md)。**結局控制室已全解（2026-08-16）**：`2PLAY` 的 `0x0e` 分派 `0xFD` → `_2smith_e01`（2SMITH `+CEC8`）先用 `ds:9680` 的五格清單（`FF E1 C2 C1 E0` ＝ Sheltem ＋ 水／風／火／土四元素）打一場 `ds:0415 = 0x83` 的特殊戰鬥，過了才進 `sub_1D2A4`。控制室三道關卡：十格中止碼比對 `ds:45FC` 的 `"WAFE      "`、隊伍要有人帶 `+129` bit 5、然後是**替代加密的密碼題** —— `sub_1D19C` 每次重抽一張 0..25 的隨機字母表，四行密文的明文是 `str.302`–`305`（泰拉憲法序言），玩家要把 `ds:45BA` 指到的 `"Preamble"` 用同一張表編碼、在 15 分鐘內打出那 8 個字元。通關給每人 50,000,000 經驗（`+129` bit 3 擋重複），最終分數 ＝ 全隊經驗值總和。四十七條字串是 `sub_17732(3)` 從 `str.280`–`str.326` 一次讀進來的。見 [`docs/re/05`](docs/re/05-2smith-control-room.md)。**`2SMITH` 鐵匠（[`07`](docs/re/07-2smith-shop.md)）、`2CMDS`（[`08`](docs/re/08-2cmds-inventory.md)）、`2COMBAT`（[`09`](docs/re/09-2combat-map.md)）三個 overlay 也在 2026-08-16 全解。**`2COMBAT` 那一份解出**兩套獨立的怪物攻擊系統**：近身命中附帶的三十種效果（`ds:9E27`）與遠程／法術的三十二種（`ds:9E24`，吐息威力＝這隻怪目前的 HP、群體攻擊一次只走 4–7 格、順序由 `ds:1462` 四組決定且走到空位就白費、`explodes` 與 `frenzies` 打完自爆）。連帶把**八個狀況位元的名字**定出來（`ds:1456`／`ds:145C` 把位元值與 `ds:106E` 的十六則訊息直接配對，`0x01` 詛咒／`0x02` 沈默／`0x20` 麻痺／`0xFF` 抹消先前都沒有來源）。| **`2COMBAT` 已無未解的函式，整套遠程／法術攻擊也接進 remake 了（2026-08-16，`internal/game/special.go`，[`polish-spec`](docs/polish-spec.md) P18）。** 連帶讀完 root 的兩支：`sub_138A8` 是運氣加等級的豁免，`sub_13928` 是傷害的最終調整（抗魔法免傷、幸運減半、元素除以四，然後扣血並改狀況位元組）。`ds:1436` 減 `0x11` 那一步也解掉了 —— `sub_13928` 取偏移時加回 `0x11`，所以傷害那條路讀的是對的抗性欄位；上狀況那條路（`sub_1B2DE`）沒加回來，那是原版的漏洞，remake 不照抄（[`polish-spec`](docs/polish-spec.md) D1）。root 的無筆記函式多數是 C runtime，不列缺口。|
| 原版 oracle 未知 | **`ds:58B8` 已證實（2026-08-16）**：不必走到終局 —— 事件的觸發地點寫在資料裡，`cmd/mm2patchevent` 把 `0e fd` 搬到起點旁邊就進得去，跑的仍是原版自己的程式碼。記憶體傾印顯示 `ds:58B8`–`ds:58BF` 全是 0（這一步關掉的正是靜態掃描關不掉的「指標間接寫入」），畫面第 19–22 列印出四行 `Version 1.01`。做法與可重跑指令見 [`docs/playtest/02`](docs/playtest/02-event-relocation.md)。剩四項可在這台機器上驗（[`docs/todo.md`](docs/todo.md) §2）：門狀態複驗、施法的逐提示對照、水行術旗標的存檔持久性、Mega Drive 戰鬥結算那五首。另外設施 overlay 的逐畫面行為與原版存檔格式仍未證實。**戰鬥的目標選擇已經不是 oracle 問題而是 remake 缺口**（`sub_18DAA` 的 `A`–`J` 選單已解、`Reachable` 已實作，但沒有選單，見 todo G1）；第一人稱的火炬差異在 2026-08-16 結案。| 四項各自的下一步寫在 todo；其餘保留為證據限制。|
| 可選 polish | 文字比例、非 DOS 素材視覺、地圖畫廊和音訊人耳驗收。| 不得取代四個玩家阻塞項。|
| 文件與截圖 | **同步過（2026-08-17）**：`README.md` 的成果段、狀態表、`F6` 循環與 Mega Drive 火炬那幾段改寫成現況；截圖由 `cmd/mm2shots` 重跑，新增 `19-settings.png`；`18-control-room.png` 的產生方式（`TestControlRoomScreenshots` ＋ 固定種子 `0x1234`）寫進 [`docs/screenshots/README.md`](docs/screenshots/README.md)。`workplace/dosbox/shots/` 的十九份記憶體傾印（2.1 GB）刪掉了 —— **同一條 timeline 是決定性的，再跑一次就有**，PNG 保留。| 每輪重跑 `cmd/mm2shots`，畫面有變才換圖。|
| remake 戰鬥：選目標 | **接完（2026-08-17，G1）**：攻擊與射擊都先問「打哪一隻」，可選範圍照 `sub_18DAA` 開頭 —— 近戰 `ds:9FC5`（前排）、射擊 `ds:0508`（全場），都夾在 10；只剩一隻就不問（原版 `var_C <= 1`），Esc 取消且**不消耗回合**（原版 `var_E--`）。`Encounter.Target` 指到的那一隻打不到或已經倒下就退回第一個站著的。選單用數字不用 `A`–`J`、倒下的不列，兩處刻意不同都寫在 [`polish-spec`](docs/polish-spec.md) P20。| `internal/ui/target_test.go` 七條 ＋ 截圖 `07b-target.png`。**這一項結掉之後，`docs/todo.md` §3「remake 還缺的機制」是空的**。|
| MSX 素材（修好） | **2026-08-17 修**：MSX 整套素材先前被完整性檢查打掉（貼圖表有一格越界 ＋ 火炬只有九個位置裡的五個，卻拿 DOS 的「全滿」契約去要求），症狀是它安靜地不在 `F6` 循環裡。改成 `msx.WallSlots`／`msx.TorchSlots` 由貼圖表自己算，`selectTheme` 失敗時把 loader 的理由接進錯誤訊息，`TestDeclaredSlotsHavePixels` 守住「宣告有圖的槽位必須切得出非空的圖」。兩張 MSX 截圖因此重拍 —— 版控裡那兩張其實是高解析素材包的畫面。| 正牆深度 2 與更深處的門、火炬仍未定位（[`docs/todo.md`](docs/todo.md) A8），不畫。|
| 交付：三平台與推廣片 | 使用者確認 Windows x64、macOS universal、Linux x64；公開包只含 remake 並由玩家自備合法原版，含原版資料與選配音樂包的完整版只留在 ignored 的 `.local-full/`，不得提交、推送或上傳。新增音訊後端後，Linux local-full、Windows x64 公開包與 macOS x86_64＋arm64 universal 公開包均已完成 Docker 封裝 smoke，公開包沒有音檔。現有 72 秒 1080p 推廣片仍使用玩家自備 `MM2.EXE` 的 DOS PC Speaker 轉譯。| Windows／macOS 真機、簽章、公證與推廣片人耳驗收仍是外部 gate。**音樂那一段的阻塞已解除**：Mega Drive 的 16 首擷取完成且已是 remake 的預設音源，推廣片可以用它重拍，只差人耳過一遍（`docs/todo.md` R4）。|

## 2. 已完成

| 領域 | 狀態 | 文件 |
|---|---|---|
| overlay 機制與記憶體佈局 | 14 個 overlay 全數反組譯，599 個函式 | [`docs/formats/01`](docs/formats/01-overlay-and-memory-layout.md) |
| LZW 壓縮 | 演算法讀自 `sub_12242`，全部資料檔可解 | [`docs/formats/03`](docs/formats/03-lzw-compression.md) |
| `.16` 圖形 | 兩型檔頭都可解，26 個檔全部 render，含 `MONSTERS.16` 的 433 個影格。標題畫面與原版截圖逐像素 99.92% 相同 | [`docs/formats/04`](docs/formats/04-graphics.md) |
| `MM2.CH` 字型 | 8×8 × 128 字元，ASCII 對位驗證 | [`docs/formats/02`](docs/formats/02-data-files.md) §2 |
| `ITEMS.DAT` | stride 20 × 256 筆 | [`docs/formats/02`](docs/formats/02-data-files.md) §1 |
| `STR.DAT` | LZW + 每 byte +0x1C，400 行純文字，與原版畫面逐字相符 | [`docs/formats/05`](docs/formats/05-text-system.md) |
| 事件段佈局 | 事件表 + skip + 腳本區 + 字串區，讀自 `sub_1A85C`；60 段適用，另 11 段是沒有事件表的腳本庫 | [`docs/formats/02`](docs/formats/02-data-files.md) §4 |
| `MAP.DAT` 兩層結構 | 地形層 + 屬性層各 16×16；屬性層 bit 7 = 有事件（五段 100% 零例外）。MAP 段 k 對應 EVENTSI 段 k | [`docs/formats/06`](docs/formats/06-map.md) |
| 事件字串 | 71 段全數抽出 1,308 條 | [`docs/formats/02`](docs/formats/02-data-files.md) §4 |
| 中文顯示 | 24×24 點陣、中英混排、`@` 換行、缺字檢查 | — |
| EGA 調色盤 | 原版標準 16 色，截圖 100% 落在表內 | [`docs/formats/04`](docs/formats/04-graphics.md) §1 |
| 原版 oracle | DOSBox headless + timeline 自動化，可一鍵跑到第一人稱視角 | [`docs/playtest/01`](docs/playtest/01-oracle-timeline.md) |
| `DEFAULT.DAT` 角色 | 六個預設角色與原版名冊逐一對上 | [`docs/playtest/01`](docs/playtest/01-oracle-timeline.md) §4 |
| Go 解碼器 | `lzw` / `gfx` / `font` / `cjk` / `events` / `text`，測試對照原版 | — |
| **遊戲邏輯層** | `internal/game`：地圖兩層、移動、轉向、事件觸發，確定性測試 | — |
| **可跑的雛形** | `cmd/mm2walk` 走固定路線輸出每步 PNG：載入原版地圖 → 移動 → 執行事件腳本 → 顯示中文 | — |
| 原版起始座標 | Middlegate (7,3) 面北；以 DOSBox 從角色選擇按 `Z` 後的記憶體 dump 直接量得，不是 `ATTRIB.DAT +14` 的傳送預設座標 | [`docs/playtest/01`](docs/playtest/01-oracle-timeline.md) §7 |
| 中文化管線 | `mm2strings export/check`，版控只存譯文與原文雜湊 | — |

## 2.5 中文化的素材分兩批

| 來源 | 條數 | 內容 |
|---|---|---|
| `EVENTSI/O.DAT` | 1,308 | 設施名、短提示、按鍵詢問 |
| `STR.DAT` | 400 行 | 劇情、對話、選單、結局、謎題 |

兩批都是明文，逐條翻即可。詳見
[`docs/formats/05-text-system.md`](docs/formats/05-text-system.md)。

## 3. 已解出的機制：證據索引

⚠ **這張表不是待辦清單，是證據索引。** 它按時間追加，記錄每一項機制是怎麼解出來的、
證據在哪，用來回查某個結論的出處。**目前狀態一律看 §1.5**，
不得把這裡的單一列當成「還在進行中」而重開研究。

| 機制 | 怎麼解出來的、證據在哪 |
|---|---|
| **新開遊戲起點與第一條設施路徑** | `ui.Load` 以原版「選好 Middlegate 隊伍後離開名冊」的狀態啟動：地圖 0、(7,3)、面北；DOSBox dump 的直接證據見 `docs/playtest/01-oracle-timeline.md` §2、§7。`ATTRIB.DAT +14` 的 (7,5) 是傳送預設入口，不能混用。新鮮原版重測：北走第 2／3 步是旅店／神殿招牌（`04 NN`，不攔輸入），第 4 步才進神殿；答 N 後下一步正常離開。UI 因此把無 `0x07` 的招牌獨立為非阻塞提示，並在無事件的新格清掉舊設施狀態。已有存檔時 `Restore` 會覆蓋初值；`TestFreshLoadUsesOracleMiddlegateStart`、`TestFacilitySignsDoNotWaitForConfirm` 與 `TestFreshStartReachesTempleThroughUI` 守著這條鏈。 |
| **牆的判定（Mega Drive 版獨立印證）** | MD 版 `sub_F5CE` 依朝向（ASCII `N`/`S`/`E`/`W`）選遮罩與位移取牆位元，四組值 `(0xC0,6)`／`(0x30,4)`／`(0x0C,2)`／`(0x03,0)` 與 DOS 版完全相同。兩個不同團隊、不同 CPU 的移植用同一套地圖資料佈局，所以下面那一列的結論更硬。見 [`md-re-status.md`](docs/research/md-re-status.md) |
| **牆的判定** | 屬性層每格四個位元：bit 6/4/2/0 = 南/東/北/西，1 = 有牆。遮罩與位移量出自 `sub_1423E` + `sub_15E68` 的 `and 55h`；位元與方位的對應由「牆有兩面必須一致」定出（60 張地圖自洽率 93.8%，次高 86.7%，隨機 50%）。三重旁證：外圈 64 面全封閉、牆線圖是一座城、第一人稱畫得出來。見 [`docs/formats/06`](docs/formats/06-map.md) §4 |
| **第一人稱視角** | `internal/view/firstperson.go`。四個深度、正牆與左右側牆、透視地板。素材配置自證：側牆寬度累加 24→56→80→96 剛好等於同深度正牆的左緣。`cmd/mm2walk` 走一趟每步輸出 PNG |
| **事件腳本直譯器** | 位在 `2PLAY.OVL`。50 個 handler 的跳表（`jpt_1A676`）、腳本指標（`word_1042A`）、取字串程序（`sub_18FD0` + `sub_19016`）都已解出。`01`/`02`/`04` 是顯示字串（靠左／視窗／置中）。**EVENTSI 的 1,865 個事件格有 1,215 個（65.1%）能顯示訊息** |
| **事件即時輸入（已完成的 remake 垂直鏈）** | `0x07`、`0x09`／`0x0a`、`0x26`、`0x2f` 會保存原始段號／腳本號／下一個 opcode 位移並真正暫停；UI 只在問題出現後收 Y/N，另有選人與十字元文字輸入。`0x0e` 遇到特殊代碼會照 `sub_1956E` 轉入腳本庫；中門西側 `(0,5)` 的 `Scripts[21] = 0e 11` 已實跑到段 61 的 `Scripts[0]`，才顯示「付 10 金到 Sandsobar」並由 `0x09` 暫停。Y 扣金後傳送至地圖 4 `(8,1)`；N 時方向鍵仍被吃掉，下一次輸入才撞牆。後半段的付款、傳送、一般設施、獎賞與遭遇延到輸入後才結算。`State` v2 保存未完成事件並相容讀取 v1；`TestAskWaitsForResponse`、`TestPendingEventStateRoundTrip`、`TestEventYesNoWaitsForPlayerInput`、`TestMiddlegateGateQuestionWaitsForAnswer`、`TestMiddlegateGateYesTeleportsToSandsobar` 守住。原版 `0x09` 的「問題前方向鍵無效、N 後才解除」已於 2026-08-12 DOSBox 正常路徑重播；見 [`docs/playtest/01`](docs/playtest/01-oracle-timeline.md) §10–11。設施 overlay 的逐畫面 parity 是獨立項目，不可拿這條鏈宣稱已完成。 |
| **overlay 位址換算** | `IDA linear = 檔案偏移 + 0xF800`；root `0x10000`–`0x12800`、level-1 `0x17E10`+、level-2 `0x1C130`+。追函式前先判斷屬於誰，否則會在根映像裡白找。見 [`docs/formats/01`](docs/formats/01-overlay-and-memory-layout.md) §2.5 |
| **角色記錄** | 130 bytes/筆。名稱、職業（0–5）、六個屬性、年齡、HP／上限、屬性的當前值都已定位。職業與屬性順序互相定錨：六個預設角色的屬性峰值全部落在自己職業該高的那一項。見 [`docs/formats/02`](docs/formats/02-data-files.md) §5 |
| **隊伍面板** | `internal/view/party.go`。右上那一格六列，一人兩行：編號與完整名字走原版 8×8，職業（狀況不正常時改印狀況）走中文高解析層，生命依比例變色、法力另一色。**框線照原版量出來的座標，框裡放什麼按中文重排** —— 原版把隊伍擠在下方兩欄各三列，一欄 154 px 放不下完整名字與中文職業 |
| **手冊掃描** | 93 頁整理成 [`docs/manual/part-1..5.md`](docs/manual/)。官方譯名（地名、人名、職業、屬性、法術、設施）、七項屬性表、八種職業、五種族、三陣營、城鎮地圖與圖例、96 條法術數值。手冊自身不一致處已逐項標出 |
| **座標系** | 第 0 列在**南**邊。手冊城鎮地圖的 y 軸由下往上，圖上設施位置與事件表的格編號在 y 上十處抽驗有八處完全相同。連帶把牆的位元改成 bit6=北，與 `sub_1423E` 的 `'N'`→`0xC0` 一致 |
| 事件表欄位語意 | 佈局已從 `sub_1A85C` 解出（見 [`docs/formats/02`](docs/formats/02-data-files.md) §4）。**71 個非空段全部歸得了類**：60 段是事件表、11 段是沒有事件表的腳本庫（`TestNoIrregularSegments` 守著）。`Cell` 是格位置已確定；一般事件的 `Index` 是解析後 `Scripts[Index]` 的直接零起算下標，腳本庫的 selector 則由 1 起算而對應 `Scripts[0]`；**`Kind` 的高 nibble 已解出是方向遮罩**（北 `0x80`／東 `0x40`／南 `0x20`／西 `0x10`），面向不對整筆跳過，remake 已照做 |
| 中英字級比例 | 英文走原版 8×8 放大 3 倍、中文走 24×24 點陣，像素密度是 3:1，英文看起來明顯較粗。可用但不協調 —— 要對照原版畫面決定是否改成 2 倍 + 16×16。**冬之魔就是在這一項上走了回頭路，不要等到全部翻完才處理** |
| **翻譯** | **2,702/2,702（100%）**。來源五組：事件字串（indoor/outdoor）、`STR.DAT` 長文字、怪物名、物品名，以及 `MM2.EXE` 尾部的 787 條 UI 與播報文字。刻意不翻的條目（交織密碼的密文、座標表、製作群人名、控制室謎題的答案）政策寫在 [`translations/glossary.md`](translations/glossary.md)。中文字型烘到 2,239 字（來源含 `data/reference.json`）|
| **訊息在地化** | 2,683 個事件格裡有 1,689 個會顯示訊息，這些訊息 100% 能換成中文。逐段查表 —— 一段腳本可能連續顯示好幾條字串，整段拿去查會有四分之一查不到 |
| `STR.DAT` 訊息索引 | **解了**：根本沒有逐條索引 —— 是「區塊 + 順序游標」。`ds:52F4` 五個區塊起點（0／1596／3932／4742／6212，每個都落在段落開頭、相鄰距離都 < 0x960），載入時把 0x960 bytes 解碼進 `ds:A06E` 並把游標 `ds:52F2` 歸零，「取下一條」回傳游標處再掃到 NUL。**每一條是一行顯示不是一則訊息**（四百段最長 38 字），一則訊息是連續數行。remake 的 `text.Parse` 切法與原版一致；翻譯檔把連續數行併成一個 key（`str.N`），那是**刻意的**——譯者需要看到整段上下文，而顯示端本來就要重新斷行 |
| 密碼謎題 | 結局前的謎題是把美國憲法序言做成密文，翻成中文後原本的解法不成立，要單獨設計 |
| `MAP.DAT` 其餘位元 | **地形層的 `bit 6/4/2/0` 分辨實牆與門**：把室內外分開量之後對向一致率 **99.7%**（先前的 91.7% 是把野外二十四張混進來的）。室內 36 張裡門佔牆面 1.8%（283 面）。**室外那一套也解了**：地形碼（`Attr & 0x1F`）查 `ds:52B2` 分成 5 類 —— 山區要隊伍裡兩名登山家、森林要兩名探險家（技能編號 0x0B／0x0D 與手冊一字不差），水域另有旗標未解。分佈 74.0% 可通行／15.5% 水域，取 `Terrain` 層會得到 52.8% 水域，一片汪洋。**兩個平面的分工也定了**（`sub_15E68`）：擋不擋路來自屬性層的低位元（`attr & 遮罩 & 0x55`），是哪一種來自地形層的兩個位元（`terrain >> 位移 & 3`，3 當成 1），查 `ds:4E4C` 的七句訊息表。分開取之後 21,092 面牆裡實牆 78%、屏障 14%、門 7.7%。**種類 3（訊息表折成實牆的那一個）的候選語意是「牆上有火炬」**，假設待驗，見 `internal/game/wall.go` 的 `HasTorch`|
| 第一人稱視角的細節 | `cmd/mm2diff` 對 (7,5) 面北量到**與原版逐像素完全一致（0.0%）**。牆、地板、天空、門、補牆、火炬都到位。換一個位置 (8,0) 面東還有 9.9%，集中在走廊深處：正牆深度 3 的落點與深度 0–2 的置中規則對不上（原版在視圖 80，置中是 96），而且遠端那一條橫向的牆帶還沒解 |
| **遊戲中切換不同平台的素材** | `F6` 換來源（DOS → Amiga → MSX → 高解析素材包）、`F5` 換畫法（原版像素 ↔ Scale3x），兩者**正交**，四種組合都成立。能這樣換是因為素材本身對得起來：`town.32` 與 `TOWN.16` 都是 32 張、同樣的深度與側牆順序，所以幾何一行都不用改，真正的差別只有透空色（DOS 8、Amiga 0）與火炬張數（DOS 每格四張含燈桿底圖，Amiga 三張）。掛點在 `render.Screen` 的**高解析貼圖佇列**（`BlitHi`／`BlitHiKey`），由 `Flush` 在放大原版層之後補畫 —— 非 DOS 的素材帶自己的調色盤，塞不進原版層那張 EGA 畫布，只能走這條路。素材端全部走 `TownSet.blit`／`blitKey` 一個分發點。**只有「DOS ＋ 原版像素」還走原版層**，那條路徑要與原版逐像素相同（`cmd/mm2diff` 守著）。素材包（`cmd/mm2modern`）的圖已經放大過，幾何要用**原版座標空間**的寬高算，`TestPackSetAlignsWithOriginal` 用「Scale3x 保留中心點」這條性質逐格比對守著。截圖 `docs/screenshots/01b`～`01e` |
| **事件腳本的 opcode** | **`EVENTSI.DAT`／`EVENTSO.DAT` 用到的 49 種 opcode 全部有語意，直譯器也全部有分支**（`TestEveryOpcodeInDataIsHandled` 守著，拿 switch 的 `default` 當量測）。六個顯示字串（`01`–`06`）、兩對條件跳躍（`2b` 與 `10`／`11`）、兩個固定遭遇（`12`／`13`）、事件消耗（`14`）、讀寫角色欄位（`15`／`18`，經 `sub_1AA00` 的 128 項選擇器跳表換成記錄偏移）、隊伍持有物品（`16`）、給予物品（`19`）、拿走背包物品（`28`）、等按鍵（`07`）、換地圖（`0c`，地圖 = 參數 & 0x3F、座標低 nibble 是 X 高 nibble 是 Y）、擲骰（`1c`）、門檻檢查（`1b`）、欄位加減（`1f`／`20`，付款就是 `20`）、問 Y／N（`09`／`0a`）、播曲子（`0d`）、進城鎮設施（`0e`，子命令 1 旅店／2 訓練／3 酒館／4 神殿／5 法師公會／6 鐵匠／7 大腦淨化）、數第二技能（`32`）、全域變數（`17`／`1a`）、世紀範圍（`22`，`ds:03CA`）、時間前進（`2c`，`ds:03C8`）、停頓（`1d`／`1e`）、顯示圖片（`0b`，經 `sub_18EE6` 依場景查表換成 `monsters.16` 圖號）、重畫（`0f`／`29`）、打字謎題（`2f`／`30`）、湊錢（`24`／`25`）、隊伍成員條件（`2d`）、教法術（`2e`）、傷害（`31`）、待領獎賞（`2a`）、日期條件（`23`）、改寫地圖（`21`）、選人（`26`）。**唯一「認得但不做」的是 `0x08`** —— 它改的是主迴圈的讀取行為，remake 一次讀完整段所以沒有對應動作；`0x00` 是結束標記、`0x27` 在跳表裡沒有 call。見 [`docs/formats/07`](docs/formats/07-event-script.md) |
| **怪物表** | `MONSTERS.DAT` 已解：26 × 256。名稱每個位元組加了 `0x80`（所以 grep 怪物名找不到東西）。**數值那 12 bytes 是位元欄位**：低位是基數、高位是 10 的冪次的倍率索引（解包規則抄自 `sub_3B80`）。生命、經驗、攻擊次數、傷害骰已解出並驗過 —— Sewer Rat 8 HP、Mega Dragon 64000 HP／3200 萬經驗。見 [`docs/formats/02`](docs/formats/02-data-files.md) §6 |
| `MONSTERS.16` 動畫表的表頭 | 第一個 `FF` 之前那 2–14 個位元組未解，常以 `8X 00` 收尾但槽 9 是 `2F 0A 10 3B 06 04 83 00`。像素、影格、動畫段、圖號→槽號的回退規則都已解（見 docs/formats/04 §2），原樣保留這段 |
| **法術表** | 96 條全備（牧師 48 + 巫師 48），名稱、形式、對象、說明抄自手冊（`data/spells.json`）；**施法代價已改用原版的** `SPELLS.DAT`（`data/spellcosts.json`，見上一列）。**施法框架已實作**（`internal/game/cast.go`：會不會、法力等級、法力與寶石的扣除）。**牧師系的治療與解狀況七條效果已實作**（急救術 +8、治傷術 +15、解毒／治病／恢復／解除石化／復活），而且那七條反過來把狀況位元組的語意定出來：bit 2 疾病、bit 3 中毒、bit 4 急救術治的那項、`82h` 石化、`81h` 死亡。另**十二條全域計數型**也解了（照明／飄浮／守衛／庇護／水行／四個界傳送／魔法防護／拒絕傷害／製造食物），它們寫的位元組與事件腳本的全域變數是同一批 —— 四個界傳送術寫的 `ds:03DC`–`ds:03DF` 正是腳本選擇器 `0x27`–`0x2A`。**19/96 已實作**。`2CAST1` 收的全是非戰鬥法術，攻擊法術整批在 `2CAST2`。仍缺的是其餘的**效果** —— `2CAST1` 的跳表在映像偏移 `0xD1CE`，97 個 word 指向六位元組 trampoline，43 支由它自己實作、其餘給 `2CAST2`。見 [`docs/formats/09`](docs/formats/09-spells.md) |
| **隨機數產生器** | `sub_11C88` 逐行重寫成 `internal/game/rng.go`：`+0x9248` → `ror 3` → `xor 0x9248` → `+0x11`，取 15 bit 後對 `hi-lo+1` 取模。播種用 DOS 時鐘。同種子同數列，所以隨機結果可以與原版一致、可重播 |
| **怪物槽與離場** | 場上一隻怪 = 六個平行陣列的同一格（編號 `ds:9680`／狀態 `ds:9F86`／未解 `ds:9F92`／剩餘行動 `ds:9F9E`／HP `ds:9FAA`(word)／行動輪數 `ds:5480`），交換 `0x181A4` 與移除 `0x18A5E` 兩處各列出同一組六個。**死亡與逃走走同一條路**（`sub_18AF4` → `sub_18A22`）：從陣列裡刪掉、後面往前搬、總數減一、前排數重夾 —— **原版沒有屍體**，畫面上不存在「死掉的怪物」這個狀態。逃走判定也解了：士氣層查 `ds:1036`（`3, 9, 24, 255`），門檻小於隊伍最高等級的一半時每輪五成機率逃；士氣層 3 永不逃。怪物記錄 b19 同時裝群體大小上限與士氣層。狀態位元的作用點補到四個：衰弱傷害減半、驚嚇命中減半、心智渙散完全不行動、封困每輪自傷 `ds:102A[層]`。**溜跑指令 `R` 也解了**：`ATTRIB +13` 是這張地圖的成功率（城鎮一律 100），擲 `rand(1,100)` 小於它就成功，跑掉的是下指令的那一個人；角色的移除用「最後一格補洞」，與怪物的「整批往前搬」是兩種寫法。**行動順序也換成原版的**：怪物的鍵是記錄 b24（`(x&0x1F)+1`、bit5 ×10；Zombie 2、Mega Dragon 250），角色的鍵是記錄 `+110`（屬性區第四格的當前值）；每次在「這一輪還沒動過」的裡面各取最大再比，平手時角色先動，第 11 隻以後的怪物不進排程。第四格是**速度**（見 `internal/game/party.go` 的 `Stat`），所以原版拿速度排行動順序、也拿速度算防護等級加成，與手冊一致。**九個狀態位元的作用點全部解完**（沈默 → 施法失敗，在十四個 overlay 裡只有一個測試點；衰弱總傷害減半、驚嚇命中減半、沈睡定身封困擋行動、心智渙散用不了特殊攻擊、封困每輪自傷）。**怪物的特殊攻擊也解了**：b17 低 5 位是型態、高 3 位查出使用機率，`+20` 高 nibble 是可用次數；施法型態（0x0F–0x1E，0x1D 除外）會被沈默或**當前格屬性** `ds:59C8` bit1 擋成 `*** Spell Failed ***`。**事件檔的腳本庫段也解了**：編號 60 以上沒有事件表，`uint16 skip` + `0xFF` 分隔的腳本，由程式碼把 `ds:0392` 暫時換成該段號載入（`2PLAY` `0x196F6`）；城鎮與野外那些「付 N 金幣送你去某地 (y/n)」就住在這裡。再停止拿「Kind 低 nibble 恆為 0」當判準（整份資料只有一筆反例，EVENTSO 段 37 的格 98 `Kind=0xF1`）之後，**71 個非空段全部解得開，一段未解都沒有**。見 [`docs/formats/08`](docs/formats/08-combat.md) |
| **冒險指令與存檔接上前端** | 撞門（D）、開鎖（U，自動挑盜行最高的人）、存檔（S）、戰鬥中溜跑（F）。存檔分兩份：名冊走**原版格式**（`ROSTER.DAT`，位元組完全一致往返，原版讀得回去），遊玩狀態（位置、種子、劇情旗標）走 remake 自己的 JSON —— 原版把那些放哪還沒解。開機自動接續。溜跑的成功率是這張地圖的 `ATTRIB +13`，跑掉的是一個人不是整隊 |
| **說明書的手札也收進遊戲** | 紙本前兩章是敘事，遊戲裡一個字都沒有：**序言**（柯拉克怎麼失蹤）與**科隆的歷史**（四大自然種族、能源水晶球、卡隆國王與火龍、亡命沼澤的由來）。共 47 段收進 `data/reference.json` 的 `lore`，按 `K` 查得到。連帶修掉一個安靜的缺字：那些字不在譯文檔裡，`build_cjk_font.py` 沒烘進去，「昆登」在畫面上變成「　登」而**不會報錯** —— 字型建構改成也讀 `data/reference.json`，2,082 → 2,115 字 |
| **紙本說明書的內容開始收進遊戲** | 1988 年靠磁片出貨，有一批參考資料只印在紙本上。**已收進遊戲**：96 條法術的消耗／形式／對象／效果 —— 施法選單把游標那條的說明顯示在訊息區（資料在 `data/spells.json`，抄自珍017 中文說明書）。**查證過的負面結論**：MM2 的 DOS 版**沒有**「請翻到說明書第 N 段」那種抄寫式防拷，787 條 EXE 字串裡沒有 word／page／paragraph 形式的提問（`TestNoManualLookupPrompts` 守著）。第二技能一覽（15 條）、技能在哪學（12 家）、城鎮指令（6）、冒險畫面指令（27）、職業成長、序言與科隆的歷史（47 段）也都收進去了，按 `K` 查得到。世界地圖也接了（2026-08-15）：掃描件不能散布，但 `A1`–`E4` 的結構可以從玩家自己的 `ATTRIB.DAT` 算回來 —— 二十張野外圖的鄰接欄位連成 5×4 環面，錨點是「米德格特在 C2」（外圍地圖 11 的 `(7,3)` 事件 `0c 00 f5` 換到地圖 0），按 `W` 看得到，格上的地名沿用手冊那 51 條。見 [`world-grid-oracle.md`](docs/research/world-grid-oracle.md)。見 [`docs/in-game-manual.md`](docs/in-game-manual.md) |
| **可以玩了：Ebiten 前端** | `cmd/mm2` + `internal/ui`。開視窗、吃鍵盤、畫第一人稱視角與隊伍面板。互動邏輯（模式切換、訊息佇列、選單、戰鬥逐回合）全在 `internal/ui`，**與視窗系統無關** ——同一份程式碼 headless 也跑得起來，所以「按這個鍵之後畫面變成什麼」有測試守著，不是只有「編得過」。按鍵：↑ 前進、↓ 後退、← → 轉向、Enter／空白 推進訊息與打一回合、Y／N 回答事件、R 旅店休息受訓、戰鬥中 T 射擊／P 防護／V 檢視／X 對調、物品選單裡 E 使用、Esc 離開、F5 切換牆面畫法、F6 切換素材來源（DOS／Amiga／MSX／素材包）。執行檔在 docker 的 xvfb 裡實際跑起來截過圖（`workplace/gfx/gui/gui.png`）|
| **射擊（`S`）解完並接上前端** | 近戰與射擊是同一支 `sub_18DAA`，差別全在 `ds:54A4` 一個位元組。兩件事跟著換：**可選目標**（近戰只有前排 `ds:9FC5`，射擊是場上全部 `ds:0508`，都夾在十個字母）與**欄位**（骰面 `+76`↔`+78`、加成 `+77`↔`+79`）。**弓箭手的等級不是換掉骰面** —— `0x18F06` 另外擲一次 `rand(1, min(等級,100))` 塞進 `ds:54A1`，而 `ds:54A1` 是每一擊都要加的固定值，整個動作只擲一次。所以射擊的價值是打得到後排，不是命中率。連帶補上一個從沒被觸發的漏洞：兩個遭遇建構子都沒擲前排（`RollFront`），前排恆為 0；照原版的規則，那代表近戰打不到任何人。攻擊動詞也解了：**怪物擲 `rand(1,8)` 查 `ds:1058` 的八選一**（攻擊／打鬥／衝鋒／交戰／刺向／揮砍／攻向／纏鬥），隊伍固定用 ` attacks `／` shoots `。前端按 T 射擊 |
| **使用物品（`U`）全數接上前端** | 物品記錄 `+0x0F` 是**使用效果**：0 不能使用、`>= 0x80` 是附帶法術（編號 `(值 & 0x7F) - 1`，用原版的巫師 0–47／牧師 48–95 順序）、其餘是**已實作**的八路 byte 效果。79 件法術型物品全部對得上名字；28 件非魔法型物品依效果高三位改寫有效生命上限的高 byte、當前力量／速度／準確度、戰鬥等級或法力等級。效果量先做 `byte(Attr + low-nibble)` 回捲，再飽和加到目標；MaxHP Potion 因而忠實把 14/14 變成 14/526，不採攻略表寫的 +2。**充能欄的位置已證實**（背包 `+64`、已裝備 `+46`）：使用一次減一，減到 0 就把物品編號填成 `0xFF`，而 `0xFF` 正是 `Useless Item`。兩支處理常式**不對稱**：背包的最後一次仍然發動，已裝備的最後一次有扣沒有用。前端在物品選單按 E 使用，會分別播報施法或實際數值變化；存檔、一般休息／神殿／訓練的上限生命週期都已接上。 |
| **防護法術終於有效果 + 兩份等級的理由** | 原版的 `P` 指令（`sub_1A882`）把五條防護法術的計數器一次列了出來，連帶把先前掛著「來源未追」的兩個全域加成接上：`ds:03E3` 祝福術加在**命中值**、`ds:03E7` 聖光加值加在**總傷害**、`ds:03E5` 防護罩讓受到的**近戰**傷害減半、`ds:03E6` 強力護罩一律減半（兩道獨立，都在時剩四分之一）、`ds:03E4` 隱身術**原版根本沒有實作**。這四條先前施得出來但**完全沒有效果**。另外解出**為什麼角色記錄有兩份等級**：戰鬥判定讀的是 `+113` 不是 `+32`，而勇氣術（`sub_1CA40`，最後一條未實作的法術，96/96 完成）整支只做 `add byte ptr [bx+71h], 6` —— 手冊寫的「暫時提昇 6 級、維持到戰鬥結束」就是靠兩份等級做出來的。戰鬥指令補上防護（P）與檢視（V），開鎖改成先挑人 |
| **九個戰鬥指令全部接完** | 最後一個是對調（`E`）。它呼叫的 `sub_1705A` 是 thunk，照格式拆開得 overlay 編號 7 = `2MISC2`、執行時偏移 `0xC370`，減掉載入段 `0x0C13`×16 得 overlay 內偏移 `0x240` = `_2misc2_e02`。它問兩個索引（任一個按 Esc 就整件事取消），**兩個平行陣列一起搬**：`ds:0416` 的記錄指標與 `ds:548C` 的「這一輪動過沒有」—— 行動旗標跟著搬，換位置的人不會憑空多一次或少一次行動。換的是戰鬥隊形不是名冊順序 |
| **地圖畫面（紙本替代品）** | 原版沒有任何地圖畫面 —— 1988 年靠磁片出貨，玩家拿方格紙自己畫，只有五座城鎮的地圖印在手冊裡。這一頁是為了使用者要求的「把完整的遊戲訊息都放在遊戲內」而加的，規則跟著紙本走：**五座城鎮整張看得到**（手冊本來就印了），**其他地圖只顯示走過的格**（紙本沒給，當年也是自己畫的）。判準用地圖編號 0–4 不是場景碼 —— 六十張裡室內含地城全是 0，拿它當判準會把整個地下城攤開。探索記錄壓成每張 32 bytes 進 remake 自己的存檔（不寫 `ROSTER.DAT`）；記錄點放在 `World.Trigger`，走路／跨圖／傳送／腳本搬人全都會經過那裡。前端按 M |
| **火炬會動了 + 版面座標校正** | `TOWNT.16` 的 36 張是 **3 組 × 3 個深度 × 4 張**：每個深度「一張含燈桿的底圖 + 三張只有火焰的動畫」，動畫與底圖共用左上角只蓋上半部，燈桿不重畫（原版省重繪的老作法）。位置是拿圖去原版截圖上滑動比對定出來的（96% 以上），規律是**火炬貼在該深度側牆的內側邊緣**。同一套比對順便校正了整個版面：深度 0 的側牆在截圖的 (8,22) 與 (192,22) **逐像素 100% 相符**，三式聯立得 `FPX,FPY = 8,8`（先前是 0,0，整個視圖偏了 8 px）。紅框線也量出來了，四塊區域的座標寫進 [`docs/playtest/01`](docs/playtest/01-oracle-timeline.md)；隊伍名單照原版移到畫面下方兩欄各三列。深度 2 的火炬在現有截圖裡一張都沒出現（最佳比對只有 50%），位置未驗證所以不畫 |
| **寶箱解完並接上前端** | 入口是 `2MISC.OVL` 的 `_2misc_e02`。四個選項（打開／找陷阱／偵測魔法／離開）在 `ds:2A36`；**箱子名是算出來的**：`ds:28A2` 的 5 列 × 8 欄，欄就是品質，而品質直接由「裡面有幾樣東西 + 最高物品等級」算出，所以看名字就知道值不值得冒險。**開箱與找陷阱是同一段判定**：`rand(1,100)`，> 96 一定觸發（4% 盜行再高也擋不掉）、≤ 記錄 `+30`（盜行）安全；而**找陷阱做完一定接著開箱**（`sub_1C824(0FFh)` 是「跳過判定直接開」）。偵測魔法不擲骰沒有風險。陷阱播報是 3 層 × 4 種 × 2 行（電火毒刺）。還沒解的是**誰擺出這一頁** —— `ds:0434` 非零走的是腳本 opcode `0x2a` 的 `Treasure!`（已接上 `ClaimReward`，先前那份獎賞是擺好了沒人領），為零才走箱子選單 |
| **翻譯管線的盲點** | 抽字串的規則是「NUL 到 NUL 之間全部可列印才算一條」，指標表夾在中間時整段被丟掉，**連帶把接在指標表後面的字串一起丟掉** —— 而那條字串在遊戲裡照樣顯示。箱子名的第一格 ` Wooden Crate `（`ds:22BA`）就是這樣消失的。試過兩種自動補救都不行：「取最後一個不可列印之後的字」會把指標的高位元組黏在開頭，位移差一個位元組譯文就永遠不生效；「推進到某個指標目標」會停在字串中間（`on Spells`、`ell Book`）。**位移錯一個位元組的條目比沒有更糟：看起來翻好了，實際上不會生效。** 改成只收「位置已被反組譯證明」的額外偏移（`knownExtra`），總數 2,677 → 2,678 |
| **版面照原版重做** | 拿 `shots/fpv.png` 的紅色框線量出四塊區域（橫線 y 3/131/147/187、直線 x 2/217/314、橫列分隔 x 98/170/250），照著畫：第一人稱視圖、右上狀態框（防護：照明／魔法／力場）、`'O' 選項 / Day / Year / Face` 那條橫列、下方大框。**下方大框是兩用的** —— `fpv.png` 放隊伍名單、`c2.png` 放三行對話，所以訊息不必另外找地方擺。年份在 `ds:03C8`（整個尾部資料區裡值等於 900 的 word 只有這一個，又緊鄰日期計數器）。全域變數的初值改成從 `MM2.EXE` 尾部抄 —— 那一段就是執行時的 DGROUP 初值，不抄的話年份顯示 0 |
| **翻譯管線的第二個盲點** | `qualifies` 把 `\n` 當成不可列印而整條丟掉，於是**主選單那四條**（`C - Create New Characters`、`V - View All Characters`、`T - Transfer Characters`、`G - GO TO TOWN`）、輸入姓名的提示、磁碟提示、版權行、`OPTIONS`、`Characters`、`Hirelings` 共 13 條從來沒進過翻譯檔。`\n` 是遊戲真的會印的字元，放行即可。連同建角色那一頁四條有位置證明的（`ds:07A4` 由 `ds:0876` 的七筆指標表證明、`0884` 接在那張表之後、`08AC` 接在一個 word 之後、`095F` 前面是旗標），總數 2,678 → **2,695，仍是 100%** |
| **建立新角色解完並接上前端** | 主迴圈在 `1MENU2.OVL` 的 `sub_18F48`：`A`–`G` 對調兩項屬性、`1`–`8` 選職業、Enter 重擲、Esc 離開。**屬性是三次 `rand(10,79)/10` 的和**（3–21）。**職業門檻**由 `sub_18952` 的八個 case 逐條讀出：武士力量 ≥ 15、聖騎士力量／人格／耐力都 ≥ 13、弓箭手智慧＋準確度 ≥ 13、牧師人格 ≥ 13、巫師智慧 ≥ 13、賊運氣 ≥ 13、忍者速度＋準確度 ≥ 13、野蠻人耐力 ≥ 15 —— 而且是**擲完當場算好一張表，不能選的不亮**，不是選了才擋。接著問種族／陣營／性別／姓名（`sub_18B1A` 依序），名冊滿了印 `*** Roster is Full ***`。前端按 N 建角，數字鍵選職業，姓名可打字（含退格與 10 字上限）。第一級生命／法力已照 `sub_18624` 查表：生命 = `ds:06E6[職業] + ds:06F2[耐力]`；牧師以人格、巫師以智慧查 `ds:071E`，其餘職業為 0（`data/creation.json`）。 |
| **版面按中文重排 + 兩個中文化破口** | 框線仍照原版量出來的座標，**框裡放什麼改成按可讀性分配**（使用者的設計裁決：機制照反組譯，呈現不受原版限制）。原版把隊伍擠在下方大框的兩欄各三列，一欄 154 px，扣掉編號與生命值之後名字只剩九格 ——「Sir Felgar」被截成「Sir Felg」，中文職業完全放不下。改成右上那一格六列（一人兩行：編號與完整名字／職業與生命法力），下方大框整塊給對話，戰鬥中也看得到隊伍狀態。連帶修掉三處把中文算成兩格寬的估算（中文 atlas 24 px、原版字型放大 3 倍也是 24 px，一樣寬），折行改成照中英混排的慣例斷（英文只在空白處斷、句讀不落行首、開括號不留行尾）。**兩個中文化破口**：物品名與怪物名的譯文鍵是編號而 ui 這條路徑拿原文去查，512 個名字整批沒接上 —— 查不到就顯示原文，所以沒有任何錯誤，畫面只看得出「還沒翻」。兩者都補了測試 |
| **稽核陳舊的「未解」，順手撿到三個 opcode** | 「早就解了卻還掛在未解清單上」比沒有清單更糟 —— 它讓下一輪重做已經做完的事。這一輪把 `CLAUDE.md`／`CONTEXT.md`／`docs/**` 的「未解／還沒解」逐條核過，清掉八處已經有答案的：DGROUP 的讀取路徑（它就是初值段，沒有路徑可追）、`MAP.DAT` 512 bytes 的兩層分工、`ds:03E3`／`ds:03E7`、怪物精靈座標、`STR.DAT` 訊息索引、城鎮設施 opcode、`ROSTER.DAT` 筆數、中門西側那個「無解的矛盾」。`CLAUDE.md` 的「第一批未知」整節拿掉 —— kick-off 清單不會自己過期，而那份文件每個 session 都會載入。**稽核順手撿到真的缺口**：加了 `TestEveryOpcodeInDataIsHandled`（拿直譯器 switch 的 `default` 分支當量測，不另外維護一份名單）之後，49 種 opcode 裡有 3 種沒有分支 —— `0x0a`（另一條 Y／N）與 `0x19`（給予物品）文件寫著已解、程式沒有，`0x08` 是刻意不做。`0x19` 依 `sub_19B44` 補上（含「對象 ≥ 0x80 時編號從結果暫存器取」與「背包全滿掉在地上」兩條），症狀先前是「劇情說給了你一把劍，背包裡沒有」，沒有任何錯誤 |
| **英數字改走同一套字型** | 原版的 8×8 字型放大三倍是 3×3 的方塊像素，與 24×24 原生的中文擺在一起明顯粗一截。改成 **Noto Sans Mono 烘的 12×24 半形點陣**（與 Noto Sans CJK 同一個字族），筆畫粗細一致。**漢字全形、拉丁半形**是中日韓排版的慣例，不是把英文也撐成全形。兩個坑：比例字型的 `m`／`w` 在 12 px 格裡會被切掉（畫面上變成 `Herrit`、`Terʌʌin`，**不會報錯**）—— 要用等寬字型，進位量 0.6 × 字級，20 級剛好 12 px；逐字置中會讓 `o` 與 `g` 各自跑到自己的中央，要照基準線對齊。連帶把隊伍面板整塊移到高解析層，名字一個字只佔 4 個原版像素，92 px 的面板排得下完整名字。排版的估算全部改走 `render.TextStyle.Advance` |
| **大腦淨化解完並接上前端 ＋ 屬性順序改對** | `0e 07` 進的是 `2BRAIN` 的第二個入口（`_2brain_e01`，執行時 `0xC7E2`）：收費 100 金**只驗不扣**（整支沒有寫回 `+0x66`），清掉 `+80` 的兩項第二技能，並對每一項呼叫 `sub_1C5CA` 把技能給的屬性加值扣回去（武器專家準確度 +5、扒手盜行 +15、勇士十四個欄位各 +1…）。**那張扣值表順便推翻了記錄的屬性順序**：`+0x10` 那六格是力量／智慧／人格／速度／準確度／運氣，耐力在 `+39`／`+115`。四條獨立證據，見 [`docs/formats/02`](docs/formats/02-data-files.md) §5。這同時解掉「手冊說速度、程式讀耐力」那個掛很久的矛盾 —— 兩邊讀的都是 `+110`，而 `+110` 就是速度。建角色先前把畫面的七格照序號抄進記錄，耐力之後整批錯開一位，現在照 `sub_18624` 的對應寫 |
| **酒館競技賽與神殿買法術都解完了** | 酒館（`0e 03`）只有一件事：**競技賽**，而且要有入場券。四張券是物品 208–211（綠／黃／紅／黑），階層＝券號−208；對手是`((階層×3 + ds:4102[地圖]) << 4) + rand(1,16)`，**每名隊員各一隻、全部同一種**。獎金 200–50,000 依「階層 × 城鎮」查 `ds:4108`，**只給第一個人**；另外在記錄 `+126`／`+127` 點一個獎章位元（十二種組合各一個）—— 那兩個位元組先前在未解的尾段裡。神殿的 D／E／F 也是賣法術：`sub_1C4A2` 與公會的 `sub_1C3A0` **逐行對稱**，差別只有讀哪一組表（神殿 `ds:46B2`／`ds:46C6`，公會 `ds:46DA`／`ds:46EE`，四張在 DGROUP 裡連著）與職業閘門（神殿要牧師或聖騎士）。**法術編號在表裡加了 `0x30`** —— 那是先前一直對不上編號的原因，還原之後是牧師系內的 0 起算序號。價格是六格的執行時陣列 `ds:58E2 + i*4`，D／E／F 那三格由 `sub_1C4A2` 算出來，A／B／C 那三格的算法還沒解 |
| **A 清單收尾：天花板、世界地圖、打字謎題的答案** | **天花板不是素材是程式畫的**：量自原版截圖 y 8–21，黑藍各半、橫向兩格一組逐列交錯，規則 `((x−FPX)/2 + (y−FPY)) 偶數塗藍`；先鋪滿視圖區再讓地板與牆蓋上去，深度不同露出來的高度自然就不同。**世界地圖**是手冊摺頁，遊戲裡一個字都沒有 —— 51 條地名（含格線索引 A–E × 1–4）進說明書。掃描解析度不足的那批**不編譯名**，只列原文。**打字謎題**：答案就寫在腳本裡（`0x2f` 後面那條 `0x30`），編碼是 `明文 = 0x11A − 位元組`。七題解出來是 `46`／`23`／`64`／`32`／`MEENU`／`KEYS`／`DRUIDS`；結局的中止碼不走這條路，控制室在 `2SMITH` 裡拿玩家打的十個字比對 `ds:45FC` 的 `"WAFE      "`，另外還要隊伍裡有人的記錄 `+129` bit 5 是 1。**謎底靠英文文字遊戲，翻成中文就解不開** —— 所以答案附在提問後面，而且是從腳本解出來的不是另外建表 |
| **第一類四項一次解完（純閱讀，沒開 IDA）** | 都是「函式位址已知、只缺讀」：**神殿三項價格**（`sub_1C616`／`sub_1C5B8`／`sub_1C5A6`）＝ 基數 × 等級 × 城鎮倍率，基數 1000／100／10／0 依狀況、陣營不同才收、捐獻不乘等級；城鎮倍率 `ds:46A8` 是 1/5/2/3/2。**價錢 0 是「這一項不必做」的旗標**，不是免費。**寶箱陷阱**與地圖陷阱**共用同一條公式**（`sub_1C4A6` 只做畫面閃爍，結算在 `sub_1C390` → `sub_1C338`），也就是專案裡早就實作好的 `Traps.Damage`；而且**賊與忍者會多吃一次**（`loc_1C3C2` 之後緊接 `loc_1C3D1`，中間沒有 `jmp`）。**第一級的生命與法力是查表不是擲骰**：`ds:6E6[職業] + ds:6F2[耐力]`、法力 `ds:71E[人格／智慧]`，起手就會四條法術（`0x5C`／`0x3A`）—— **這一項的答案上一輪讀 `sub_18624` 時就在同一段裡**，當時只在找屬性偏移所以讀過去了。教訓寫成規則：讀一支函式時把它寫的**每一個欄位**都記下來 |
| **雜誌攻略進遊戲：按 M 或 F3 看地圖＋提示** | 1990 年代《軟體世界》那幾期的 MM2 攻略整理成 294 條提示（46 個地點 277 條 ＋ 17 條通用），**每一條都帶出處**（`檔名:行號`）—— 內容沒辦法用原版驗，能做的只有「說得出是哪一頁寫的」。推論等級只分兩級：`已證實` 是雜誌以外另有本專案內的獨立來源（手冊轉錄、`data/*.json`、反組譯），共 32 條。野外 20 區的地圖編號用**飛行術跳表**（`ds:30BC`）推出來，二十個值剛好填滿分群表的 5–16 與 33–40，無重複無缺口；地下城的編號本專案還沒定，`map` 一律 `null`，以「區域＋地表入口座標」定位。**7 組衝突兩邊都留著**（如邪惡巫師塔的門牌左右：雜誌 L46/R23、珍017 手冊 L23/R46，而善良巫師那組兩邊一致）。地圖畫面右欄顯示，↑↓ 翻頁 |
| **三個「解出來就放著」的欄位都接上了** | 原版把它們算出來卻沒有人讀，所以**原意從二進位裡救不回來**；remake 各給一個與資料形狀相容的用途，並在文件與註解裡標明「這是加的不是還原」。**怪物 `b22` bit7 → 異界生物**（100/256 隻，偏向後期強敵：防護 ≥ 20 佔 68% 對 27%、生命 ≥ 200 佔 59% 對 15%）：驅散類四條法術對牠們傷害 ×1.5，驅魔術也驅得動，遭遇時另外點名。**屬性層 bit 5 → 房間輪廓**（125 格、6 張圖，**每一格都有牆**，排成矩形外框或三面牆的房間）：自動地圖畫成亮青色，進出時報一句。**商店進貨的 0–25 洗牌 → 貨色輪替**：每天從同一類的三十件貨底裡抽六件，索引 `(城 × 6 + 排列[i]) mod 30`。洗牌照原版「重擲到沒抽過為止」寫，不換演算法 —— 分佈相同但消耗的亂數不同，而序列是共用的 |
| **火炬的位置全部量出來了，順帶推翻「每面牆都有火炬」** | 新工具 `cmd/mm2match` 把 `.16` 的每一張影格滑過 126 張原版截圖找落點，判準是**整批截圖裡只有唯一一個 100% 的落點**。`TOWNT.16` 的三組因此全解：左側牆、右側牆、正牆，各三個深度；深度 2 左在 `p5.png` 的 (64,59)，右側牆深度 2 在 126 張裡一次都沒出現、用左右鏡射補（深度 0 與 1 都成立）。先前寫「用途未解」的第三組是**正牆火炬**（燈桿直立），在 `s5.png` 目視確認。**火炬不是每面牆都有**：`p5.png` 左邊三個深度都是牆、只有深度 2 那面點著 —— 所以「每面側牆都畫」是錯的。候選條件是牆種類 3（逐方向存、訊息表折成實牆、佔城鎮牆面兩三成），標成假設，推翻方式寫在 `HasTorch` 的註解裡 |
| **兩張防護等級表其實是同一張** | `ds:74D`（建角寫進 `+36`）與 `ds:4D84`（執行時重算）的關係是 `ds:74D[速度] == max(0, 屬性修正(速度 + 2))`，速度 3–21 一項不差。先前記成「≥ 12 時差 1、但速度 20 又不差」—— 那個唯一的例外正是**比錯了**的證據（速度 20 對到門檻 22，階梯在那裡剛好沒進位）。順帶解開三張建角表的擠壓排法：屬性值域 3–21 讓每張表的 `[0]`–`[2]` 永遠讀不到，原版就把它們疊在前一張的尾巴上；`ds:74D` 的尾端疊在起始物品表的**分段 0**，而分段 0 走特例分支不查表。用 `cmd/mm2data` 抽表、`TestCreationACIsShiftedStatBonus` 把恆等式釘住 |
| **第二類三項一次掃完：屬性層 bit 5/3/1、`ds:03DB`、`ds:03E4`** | 三項都是「誰碰這個位元組」，一次跨十五個映像的窮盡掃描就有答案（掃描前先拿 `ds:03E3` 做正對照）。**屬性層的奇數位元是整格的旗標，而且讀的地方不是平面是當前格的快取 `ds:59C8`**（`sub_1B4E0` 每步抄一個位元組進去）—— 只掃平面找不到讀取點，那正是它們掛很久的原因。bit 3 = **這一格不能休息**（950 格、12 張圖，`Too dangerous!`）、bit 1 = **這一格不能施法**（206 格、7 張圖，`*** Spell Failed ***`）、**bit 5 沒有任何一處讀它**（125 格、6 張圖）。`ds:03DB` = **詛咒**：怪物特殊攻擊 case 2 施加、命中判定裡扣掉、狀態畫面印 `Cursed - N to Attacks!`，**神殿捐獻會清成 0** —— 那就是捐獻真正的作用。`ds:03E4` **隱身術原版沒有實作效果**：只有 `sub_1C412` 自己讀（為了封頂）與遞增，零個其他讀取。順帶釘死兩個平面的分工：`ds:59D6` 地形層、`ds:5AD6` 屬性層，`ds:5BD6`／`5CD6`／`5DD8`／`5ED8` 是相鄰地圖的 |
| **神殿改成選單，陣營與生命的副本也解了** | 原版的神殿是四項選單（`STR.DAT` 的神殿段）。**恢復狀態**（`sub_1C178`）以基礎生命上限 `+96` 為門檻：只有目前生命 `+94` 低於它時，才把目前生命與有效上限 `+116` 一起抄回基礎值，再清狀況位元組 `+38`；**死亡也在這一條裡**。**恢復陣營**（`sub_1C1B2`）只做 `[記錄+106] = [記錄+13]`：`+13` 原始、`+106` 當前。等級的 `+32`／`+113` 是本體／戰鬥副本；生命的 `+94`／`+96`／`+116` 則是目前／基礎上限／有效上限，不能把原始名冊的相同值誤讀為同一欄。 |
| **城鎮設施接上選單：旅店、鐵匠鋪、法師公會、訓練所、酒館** | **旅店是編組畫面**不只是休息 —— `1RETINN.OVL` 印的是 `Characters`／`Hirelings` 兩張清單加 `Ctrl A-X to Add/Remove`，建好的角色在這裡編進隊伍（休息受訓／編入／移出／存檔）。**鐵匠鋪**三項：購買／出售／鑑定，都對背包六格動作，定價共用 `ShopPrice`。**法師公會**（`2TEMPLE` 第二進入點）每座城賣固定四條法術：清單在 `ds:46DA`、價格碼在 `ds:46EE`，**價格是壓縮碼**（低 5 位基數，bit5/6/7 各乘 10／100／1000，可疊加）。法術編號是巫師系 0 起算 —— 只有巫師與弓箭手能買、四條都查得到、而且價格隨城鎮階梯單調上升（米德格特 10–1000 → 亞特蘭汀 50000–100000），三件事互相印證。訓練所與酒館也上了選單；酒館的競技賽要入場券、打聽消息那段未解 |
| **牆的兩個位元不同源** | `sub_15E68` 同時讀四個位址：方向遮罩 `ds:59C6`、位移 `ds:59C7`、屬性層 `ds:59C8`、地形層 `ds:59CA`。**擋不擋路** = 屬性層 `& 遮罩 & 0x55`（只留偶數位元）；**是哪一種** = 地形層 `>> 位移 & 3`（**兩個位元一起取**，3 當成 1），再查 `ds:4E4C` 的訊息表（0 `Barrier!`、1 `Solid!`、2 `Locked!`、3 `Not Locked!`、4 `Success!`、5 `Impassable!`、6 `Can't swim!`）。先前把兩個位元都從同一個位元組取，算出 92.8% 的牆是門 —— 那個荒謬的數字就是同源假設的反證。分開取之後室內三十六張是**實牆 98.2%、門 1.4%、屏障 0.5%**（15,303 面）。**屬性層高位元的問題也結案**：`and al, 55h` 把 bit 7/5/3/1 整個遮掉，它們不參與牆判定；bit 7 已知是「有事件」，bit 5/3/1 對向一致率只有 86.5%（牆位元 99.7%），確定不是牆的性質 |
| **戰鬥流程骨架** | `internal/game/combat.go`。行動順序（速度高者先）、身體狀況五態、生命歸零→無意識→死亡的轉移、九個戰鬥指令。指令按鍵與 `2COMBAT.OVL` 的 `jpt_19573` 對得上（`sub ax, 46h` 之後 17 個 case），其中 `F`/`S`/`R`/`U`/`P` 五個已找到 handler |
| **成長與設施** | `internal/game/facility.go`。升級（每級生命點數照手冊的職業表：武士／野蠻人 1–12、遊俠／弓箭手 1–10、牧師／賊／忍者 1–8、巫師 1–6）、法力等級對照（經驗等級 1/3/5/7/9/11/13/15/17）、旅店休息、神殿治療、訓練所升級、戰後給經驗。**升級所需經驗已是原版的**（`data/experience.json`，出自 `sub_CC8C`）：職業分兩組，等級 2–10 查表加倍，11 級以上分段等差；名冊四十筆全數通過門檻檢驗，多數精確落在門檻上。神殿價目仍未解 |
| 經驗值欄位 | **在 `+98`（uint32）**，存檔寫得回去。名冊四十筆跟著等級從 0 走到 1,280,000。寫回這些新欄位之後，位元組完全一致往返仍然成立 |
| **遊戲迴圈** | `internal/game/session.go` + `cmd/mm2play`。走地圖、觸發事件、走出邊界換圖、依機率遭遇、開打、全滅判定、存檔。怪物的挑選與**遭遇的觸發**都已換成原版的：每走一步擲 `rand(1, ATTRIB+9)`，擲出 1 才遇敵，而且事件格不擲（`sub_17EB9`）。五座城鎮的分母是 200 或 100，野外多半 100 |
| **存檔** | 分兩份。**名冊**走原版格式：`Character.Encode` / `EncodeRoster`，`DEFAULT.DAT` 與 `ROSTER.DAT` 都是**位元組完全一致**的來回轉換 —— 解析時整筆 130 bytes 留著，寫回只覆蓋已定位的欄位，其餘二十幾個未解的位元組原封不動。空槽與檔尾那 103 bytes 的殘餘也原樣保留。**遊玩狀態**（位置、朝向、亂數種子、`ds:03F6` 起的 24 個劇情旗標、世紀）走 remake 自己的 JSON（`game.State`）—— 原版把這些放哪還沒解。少了它，全遊戲的進度一離開行程就消失 |
| **戰鬥可以打了** | `internal/game/attack.go` + `Encounter.Fight`。六人隊伍對怪物群、依速度輪動、命中與傷害判定、倒下與全滅。`cmd/mm2fight` 印出完整戰報。一級雜兵一回合清空，第 250 號的超級巨龍讓隊伍全滅 —— 難度梯度正確 |
| opcode 長度表 | **已換成原版的**。先前用兩種方法推的表，50 個裡對了 46 個；錯的四個在腳本裡出現太少，約束釘不死 |
| 遭遇表 | **已換成原版的**：`rand(1,100)` 落在門檻表哪一段決定怪物類別，再由基礎編號加難度範圍。觸發時機也解了（見上一列）。仍暫定的是**難度**要怎麼從地圖算出來 —— `ATTRIB.DAT` `+13` 曾是候選，現已確定是溜跑成功率，不能拿來當難度 |
| **戰鬥判定已換成原版的** | `sub_8398` 逐行讀出：命中率 = 命中門檻[難度層] − 目標防護等級（保底 5%），每次揮擊各擲 `rand(10,1009)/10` 判定，命中的傷害是 `rand(1, 骰面數)`。門檻表 `ds:103A` 與怪物的生命／經驗／攻擊次數／傷害骰全部到位。隊伍打怪物走的是**另一條路徑**（`sub_8E81`），也已解出：5% 直接命中、3% 直接落空，其餘擲 `rand(1, 25 + 等級/命中除數)` 加命中加成，要 ≥ 目標防護。怪物的防護等級來自記錄第 22 個位元組。武器的骰面數改由**已裝備的物品**算出來（`sub_CE12` 的規則，物品表 +0x10 是骰面數）。怪物的圖號在記錄 +0x15（`b & 0x7F`，範圍 1–60）；圖號 − 1 是 `MONSTERS.16` 索引表的項次，指到空槽就往後借（`sub_6818`）。**怪物的「速度」找到了一半**：記錄 `+20` 的高 nibble 加一是**每輪最多行動幾次**（原版抄進 `ds:9F9E`，行動一次減一，用完不再輪到），已實作；同一支函式還有一道 `rand(1,100)` 對 `ds:9E25` 的擲骰，索引會走到表外，語意未定，先不實作。**護甲的累加位置也解了**：`+31` 是裝備累加出來的防護值，`+36` = `+31` + max(0, 耐力的屬性修正)，夾在 0–255（root `sub_14F3A`）。屬性修正表 `ds:4D84` 一併解出（10–13 是 0、14–15 是 +1、超過 250 是 +19）。見 [`docs/formats/08`](docs/formats/08-combat.md) |
| 角色記錄的 SP | `+88`/`+90`（uint16 目前／上限）。只有牧師與巫師非零，與手冊「遊俠與弓箭手要高經驗等級才有法力」一致 |
| 角色記錄的其餘欄位 | root 的人物資料畫面繪製函式逐欄呼叫 `sub_29DE(序號, 值)`，再以名冊、寫入端與玩家路徑交叉驗證。已定位防護 `+36`、寶石 `+92`、經驗 `+98`、黃金 `+102`、戰鬥等級 `+113`、法力等級 `+114`、耐力 `+39`／`+115`；六格屬性是力量／智慧／人格／速度／準確度／運氣。生命三格不是冗餘：`+94` 目前生命、`+96` 基礎上限、`+116` 有效上限。原始名冊剛好讓後兩者相等，MaxHP Potion、休息、神殿與訓練的讀寫端已定出其不同生命週期。物品仍是六組平行陣列 `+40…+75`，八種抗性在 `+22…+29`，**盜行在 `+30`**。 |
| `SPELLS.DAT` | **已解**：192 bytes = 96 × 2，載入 `ds:7D60`（`1MENU1` 的 `0x1C760`）。**前 48 筆是巫師與弓箭手、後 48 筆是牧師系** —— root 的 `sub_15644` 對其餘職業把序號加 48。欄位：`A` 低 nibble 是寶石、`B` 位元 0–3 是固定法力、位元 4–6 是每施法者等級的法力。與手冊逐條比對，92 條裡法力 84 條、寶石 87 條相符；對不上的是手冊自己標錯「每等級」的八條與寶石超過 15 的五條。見 [`docs/formats/02`](docs/formats/02-data-files.md) §9 |
| **地圖屬性表** | `ATTRIB.DAT` 已解：64 × 60，一張地圖一筆。**新解出四個欄位**：`+4` 低 nibble 是場景（貼圖組）碼、`+9` 是遭遇機率的分母、`+14` 是預設進入座標、`+20` 是陷阱傷害的左移量。**`+18` 是撞門的難度門檻、`+19` 是開鎖失敗那一擲的門檻**（由 `2MISC.img` `0xC1E4`／`0xC2F3` 定出）：五座城鎮 10–50、野外全 0、地城 20–100，且兩者互相自洽（撞門難度 0 的地圖鎖難度也是 0）。`+0` 是自己的編號（六十筆 0…59 一個不差，這也釘死了 stride）。`+5`…`+8` 是四方向的鄰接地圖，兩軸的對向性都是 60/60。五座城鎮四面自指、野外正好 20 張互連，與手冊的 A1–E4 吻合。見 [`docs/formats/02`](docs/formats/02-data-files.md) §8 |
| **執行時記憶體佈局** | `0x5986` 地圖屬性（ATTRIB 一筆，`+18`／`+19` 是撞門與開鎖的門檻）、`0x59C6` 方向遮罩、`0x59D6` 牆與事件層、`0x5AD6` **執行時算出來的一層**（`0x3A64` 每走一步從第一層算出來寫進去，不是檔案資料）、`0x6052` 腳本緩衝。三塊連續（`0x5986+64 = 0x59C6`）。載入走 `sub_160ED`（開檔→查索引表→讀段→複製到 DGROUP）。見 [`docs/formats/01`](docs/formats/01-overlay-and-memory-layout.md) §2.7 |
| **thunk 表** | 217 個，格式 12 bytes（`call far 077D:0344` + overlay 編號 + `jmp far` + 目標偏移）。**這是追跨 overlay 呼叫的鑰匙** —— overlay 不直接 call root，一律走 thunk，所以在 overlay 的反組譯裡 grep root 函式名永遠找不到。RNG 的 thunk 是 `sub_16F76`，正是戰鬥模組裡被呼叫最多的函式（58 次） |
| **城鎮設施接上迴圈** | 踩到設施格就進去：旅店休息、神殿治療、訓練基地升級。`0x0e` 有兩條已證實的路徑：子命令 1–7 直接進設施（旅店、訓練基地、酒館、神殿、法師公會、鐵匠、大腦淨化），指定代碼範圍則由 `sub_1956E` 轉派到腳本庫段 60–70。招牌字串**不能**拿來判：招牌格與入口格是分開的兩格（Middlegate 旅店招牌在 (7,5)、入口在 (7,3)），用招牌判會在招牌格就把人送進設施。中門西側的 `0e 11` 是轉派到段 61 的付費傳送；逐畫面的設施 overlay parity 仍是獨立項目。 |
| **原版的查表已取得** | DGROUP 的初值段就在 `MM2.EXE` 尾部（`EXE 檔內偏移 = DGROUP 偏移 + 0x8630`），靜態可讀。`cmd/mm2data` 從玩家自備的原版產生 `data/*.json`：opcode 長度表、遭遇門檻與怪物編號表、職業攻擊次數除數、三十種特殊攻擊、介面標籤。執行時的記憶體 dump 留作第二個獨立來源。見 [`docs/formats/01`](docs/formats/01-overlay-and-memory-layout.md) §2.6 |
| **公開版的來源邊界** | 私有工作 repo **不改寫歷史**；公開時另建乾淨 repo，排除五份 `MM2.EXE` 衍生 JSON、原版資產與 `docs/research/soft-world/` 的雜誌逐字研究稿，再以 `tools/check_release.sh --public` 驗證。使用者於 2026-08-12 決定私有研究與公開原創專欄並行：專欄可放改寫摘要、勘誤與頁碼出處，不能重印掃描或全文 OCR。見 [`docs/release.md`](docs/release.md) |


## 4. 文件索引

```
CLAUDE.md                          工作規範、硬性原則、oracle 順序、工具鏈
docs/inventory.md                  63 個原版檔案 + 封存檔的 SHA-256
docs/formats/01-overlay-and-memory-layout.md
docs/formats/02-data-files.md      各資料檔的記錄結構
docs/formats/03-lzw-compression.md 壓縮與 STR.DAT 的位移層
docs/formats/04-graphics.md        .16 圖形、場景素材與火炬的位置表
docs/formats/05-text-system.md     文字系統（STR.DAT 長文字）
docs/formats/06-map.md             地圖：兩層結構、牆與門、屬性層 bit 7
docs/formats/07-event-script.md    事件腳本：50 個 opcode 的直譯器
docs/formats/08-combat.md          戰鬥判定：命中率、傷害骰、門檻表、怪物槽
docs/formats/09-spells.md          法術系統：96 條法術、代價、效果實作狀態
docs/formats/10-character-creation.md  建角：屬性擲骰、職業門檻、第一級生命與法力
docs/re/00-function-index.md       反組譯函式索引（345 個符號，掃 docs/ 與程式碼產生）
docs/research/02-other-platforms.md  Amiga／Mega Drive／MSX 的素材盤點與難度
docs/playtest/01-oracle-timeline.md  原版 oracle 的按鍵流程與前置條件
docs/playtest/README.md            實機對照：路線、固定遭遇、Sandsobar 傳送
docs/release.md                    公開釋出：歷史裡五份衍生資料、私有研究與乾淨 repo 的決定
docs/packaging.md                  Windows／macOS／Linux 的公開包與本機完整版打包閘門
docs/research/spell-interaction-oracle.md  原版施法提示分群、位址與待重播矩陣
docs/research/door-state-oracle.md 門成功狀態的負結果與下一個正常路徑
docs/research/water-traversal-oracle.md 水域旗標的靜態控制流與待驗生命週期
docs/research/chest-trigger-oracle.md 一般寶箱與事件 `0x2a` 自動獎賞的分流證據
docs/research/soft-world/README.md 私有《軟體世界》連載研究：頁碼、轉錄狀態與辨讀缺口
docs/columns/soft-world/           可公開的原創專欄：改寫摘要、驗證紀錄與勘誤（不含原文）
docs/in-game-manual.md             把紙本說明書的內容收進遊戲：盤點與現況
tools/ida.sh                       IDA 9.4 headless（analyze / ovl / m68k / z80 / script / raw）
tools/ida_func.idc                 把一段位址的反組譯寫成文字（`.i64` 不入版控，看碼的方式要留得住）
tools/gen_func_index.py            掃 docs/ 與程式碼重建 docs/re/00-function-index.md
tools/amiga32.py                   解 Amiga `.32`：影像目錄、調色盤、nibble RLE、`.anm`
tools/adf.py                       從 `.adf` 抽檔（OFS，要剝掉每個資料區塊 24 bytes 的標頭）
tools/mdgfx.py                     解 Mega Drive 的區塊：9-bit 調色盤 + LZSS
tools/msxdsk.py                    MSX `.dsk`：兩張磁區表、常駐引擎、調色盤、RLE、逐 id 抽圖
tools/msxblits.py                  從 MSX 的反組譯抽第一人稱貼圖參數（SX/SY/NX/NY → DX/DY）
tools/msxview.py                   照那張表重畫 MSX 的第一人稱視圖，並輸出 layout.json
tools/sheet.py                     把一批 PNG 排成總覽圖（書架式排版，透空色從邊界灌底色）
tools/ida_m68k_code.idc            強制裸 68000 映像從指定位址當程式碼分析
tools/mdscan.py                    在 Mega Drive ROM 裡用列相似度找未壓縮的 4bpp tile
tools/build_ovl_image.py           重建執行時佈局供 IDA 反組譯 overlay
tools/dosbox_run.sh                原版 oracle（timeline: wait/key/type/shot）
cmd/mm2match                       素材位置反推：把 .16 的影格滑過原版截圖找落點（牆要 -key 8）
cmd/mm2diff                        畫面比對：驅動 DOSBox 走到指定格截圖，與 remake 逐像素比
tools/mm2lzw.py  mm216.py  probe_dat.py
internal/assets/{lzw,gfx,font,events}  Go 版解碼器
internal/render                    兩層畫布（原版像素層 + 高解析文字層）
cmd/mm2                            **可以玩的那一支**：Ebiten 視窗 + 鍵盤
internal/ui                        互動層：載入、按鍵、模式切換、畫面組成（與視窗無關）
cmd/mm2dump                        headless 輸出 PNG，供無 GPU 環境驗收
cmd/mm2strings                     匯出/檢查可翻譯字串
tools/build_cjk_font.py            從譯文烘 24×24 中文點陣 atlas
internal/music                     本機音樂包 manifest、角色與完整性／路徑安全驗證
docs/music.md                      Mega Drive／MSX／Amiga／DOS 音源證據、音樂包與擷取工具鏈
docs/research/md-re-status.md      **MD 逆向的單一狀態入口**：已解出什麼、每一塊「為什麼先前走不通」
docs/research/md-music-driver.md   MD 音樂驅動的命令介面、播放入口、防竄改與逐首觸發
docs/research/md-music-scenes.md   MD 曲目與場景的對照：呼叫端→case→曲目的完整推導
docker/blastem/                    BlastEm 0.6.3-pre ＋ libvgm 的固定版擷取環境
tools/blastem_run.sh               headless 跑 MD ROM：送鍵、錄 VGM、轉成標準 PCM WAV
internal/assets/cjk                atlas 載入與缺字檢查
assets/font/cjk24.bin              烘好的 atlas（隨譯文與原始碼重烘）
cmd/mm2modern                      把素材烘成高解析素材包（PNG + set.json，預設落在 workplace/）
internal/assets/amiga              Go 版 `.32` 解碼器，供遊戲直接載 Amiga 素材
internal/assets/msx                Go 版 MSX 磁片：兩張磁區表、調色盤、RLE 圖形、第一人稱貼圖表
docs/gallery/                      四個平台的素材總覽圖（README 引用）
translations/zh-Hant.json          譯文 + 原文雜湊（工作檔 strings.json 不入版控）
```

## 4.4 事件段與地圖號：靠檔頭的偏移表

兩個事件檔的開頭都是一張 **71 項的 32 位元偏移表**，用**地圖號**索引，
值是該地圖事件資料在檔內的位元組偏移，**0 表示這張圖在這個檔裡沒有事件**。
表佔 `71 × 4 = 284` bytes，最小的非零偏移正好是 284。

- `EVENTSI`：44 個非零，地圖 `0–4, 17–32, 45–63, 66, 67, 69, 70`
- `EVENTSO`：27 個非零，地圖 `5–16, 33–44, 64, 65, 68`

兩邊互不重疊、聯集 71 個。拿 `ATTRIB` `+18` 的室內／室外分類逐張比對，
**60 張有分類的地圖全部落在正確的那一邊**（`TestSegmentIsMapIndex`）。

找到它的方式：`2PLAY.img` `0x1603F` 用 `bl = ds:0392`（地圖號）、
`bx <<= 2`、`dx = [bx+6052h]`、`cx = [bx+6054h]`，然後 `int 21h` AH=42h
LSEEK 過去。**地圖號直接索引，中間沒有轉換。**

`internal/assets/events` 的 `Parse` 早就是照這張表做的，
`Segment.Index` **就是地圖號**。⚠ 但它會跳過空槽，所以
**切片位置不等於地圖號** —— 用 `range` 的下標去比會得到
「兩邊各半落錯」的假象。要用 `sg.Index`。

## 4.5 公開前的阻擋項

`tools/check_release.sh` 是釋出前的 deny-list 掃描，五道檢查：
版控裡有沒有原版副檔名、從原版烘出的素材包、私有《軟體世界》逐字研究稿、
由原版產生的 JSON，以及 git 歷史裡是否還留著那些 JSON。在公開 repo 帶
`--public` 執行時，任一項命中都會回傳非 0；私有工作 repo 的既知歷史與研究稿
只會明確報告，不會阻斷日常開發。

目前唯一未過的是第五道（git 歷史）：

	data/creation.json
	data/experience.json
	data/pictures.json
	data/terrain.json
	data/traps.json

五份都已經 `git rm --cached` 並進 `.gitignore`，但**歷史裡還在**。
份數以 `tools/check_release.sh` 的當次執行結果為準，不要照抄這裡的清單。
清理要改寫歷史（`git filter-repo --invert-paths`）再 force push，
腳本會把完整指令印出來 —— **那一步需要先取得同意，不自行執行**。

`data/classes.json` 與 `data/spells.json` 留在版控裡是**刻意的**：
兩份都是手抄自手冊的表（職業表、法術表），屬於翻譯文本那一類，
不是從原版二進位抽出來的。判準寫進腳本：由 `cmd/mm2data` 產出的
每一份都帶 `"source"` 欄位指向原版檔名，手抄的沒有。

## 5. 已被推翻的斷言

| 曾經寫過 | 實際 | 怎麼發現的 |
|---|---|---|
| 競技賽是酒館選單的一項（`0e 03` 進 `2BRAIN` 的第一個入口）| 競技賽是**自己的一格** `0e 08` → `_2brain_e00`（`+C130`），三座城的地圖上各有一個；酒館是同一個 overlay 的第三個入口 `_2brain_e02`（`0e 03` → `+D15A`），五個選單項呼叫的函式裡沒有競技賽 | thunk 表把 `0e 03`／`0e 08` 分別對到 `+D15A`／`+C130`；酒館那支裡的 `Gladiator` 字串是稱號表的一格（`ds:00FD` 前面是 `Gambler`），不是入口 |
| opcode `0x2d` 的第二運算元讀不到、沒有旗標就一律不成立 | **兩個運算元各帶一個值，任一個對上就算符合**；比較鏈之前還有一道前置比對（`0x1A365`）。bit 5 沒設時找的是「不符合的人」 | `2d 03 05` 那一格的台詞是 `I cannot help anyone but Clerics and their Robber assistants.` —— 3 牧師、5 盜賊，兩個都放行。**台詞對不上就是讀漏了**，回去逐位址讀才看到前置比對 |
| `1MENU1.OVL` 比大小欄位多的 16 bytes 是「程式碼加零填充」，描述表 +0x02 的語意「退回未知」 | 那 16 bytes 在**檔案最前面**，是重定位表（2 項 × 4 bytes 補齊到一個 paragraph）；+0x02 就是項數。程式碼從 `0x10` 起，大小欄位確實是讀取長度 | 稽核 DOS 反組譯覆蓋率時發現 1MENU1 只解出 2 個函式／28 條指令。兩個進入點在檔案裡都落在指令中間，**各加 16 才是 `55 8B EC`**。修掉載入位移後同一份輸入解出 27 個函式，該檔既有位址一律偏了 16。**反組譯結果異常地少時先驗進入點落不落在指令邊界** |
| 標題畫面上「只有一對疊圖會動」，差異就那 48 個像素 | `MASTER.16` 的 **14 張全部**是疊圖，13 張在 27 秒的取樣裡都出現過，分佈在 8 個熱點 | 拿來比對的那張截圖剛好只有一個熱點亮著。改抓 60 張連續截圖，未被解釋的差異一路指出其餘熱點 |
| Amiga 怪物的調色盤「不在 `.anm` 裡」，要另外找一個 `.32` 借；場景檔「只有前 16 格有色」 | **每個 `.anm` 自帶調色盤** —— 它的本體就是標準的 `.32` 容器（`body = 0x31 + d[0x30] − 1` 起：count、`count×6` 目錄、64 bytes 調色盤、才是像素）。一隻怪的顏色是自己檔案裡的 `0x00`–`0x11` ＋ 場景的 `0x12`–`0x1F`。場景檔也是後半有色不是前半 | 掃資料全部落空（`.anm` 內掃 32 色與 15 色視窗、執行檔內掃長串合法色值、六個 `.32` 逐一試畫）。有效的一步是**追誰寫那個送進硬體的緩衝**：`LoadRGB4` 的 thunk → `sub_33ED2` → `A4−0x4F0` → `sub_33322`。**掃資料找不到的東西，去程式碼裡找誰寫它** |
| `.anm` 頂端 14 列是「用途未定的非影像資料」，總覽圖要裁掉 | **是像素起點算錯**：像素從 `body + 4 + count×6 + 64` 開始，先前從 `body` 開始，早了六十幾個位元組，前十幾列就是目錄與調色盤被當成像素 | 起點對了之後那條帶自己消失。**「這段資料用途未定」在起點沒被獨立驗證之前，先懷疑起點** |
| Mega Drive 的圖形區塊只是還沒排對，換個寬度就會變成圖 | **題目本身錯了**：tile 是去重過的池，順序與畫面無關。真正的圖是 11×11 個 tile ＋ 一張 nametable（`raw % 242 == 0`，242 ＝ 11×11×2），ROM 裡 75 筆，對上 DOS `MONSTERS.16` 的 75 個槽。而且**格子的順序是 sprite 順序不是 row-major** | 先掃遍所有整除寬度、直橫排、1×1–4×4 sprite 分組，接縫連續度最好只到 1.25 —— 那個「全滅」才是訊號：**參數空間掃完還全滅，就不要再擴大參數空間，去找誰決定每一格畫哪個 tile**。sprite 版面是從實機 work RAM `0xFFD2A8` 的 sprite 屬性表影子讀出來的 |
| MSX 深度 2、3 的正牆貼圖在 `sub_1F4D`／`sub_1FF9`／`sub_1E16` 裡，要拆開得逐條追呼叫端 | **正牆四個深度全在 `sub_1103`**：`HL` 是深度、`DE` 是橫向索引，最左最右兩支畫側牆、其餘那一支畫正牆，目的 x 是 `35n+49`／`14n+63`，`n=0` 正好置中。`sub_1F4D`／`sub_1FF9` 是**側牆**的繪製函式 | 看呼叫端 `sub_4752`：牆值 `1` 只呼叫 `sub_1F4D`、`2` 再加 `sub_D10`（側牆的門）、`3` 再加 `sub_1956`（側火炬）—— 配件早就標定是側牆的。**推測某支函式畫什麼之前，先看誰呼叫它、跟誰一起呼叫** |
| `.16` 的 `B` 字尾未解 | **面板圖磚**。四個檔（town／cave／castle／out）同構，各 36 張 16×11，與 `F`／`T`／牆同一批載入，由 `_2play_e13`／`_2play_e14` 以 16 px 步長畫出。裡面有兩組，**兩組都已證實**：灰／藍斜紋那組是 `Protection` 面板底紋；藍底格＋牆線＋門標＋四向箭頭那組是探索中按 `M` 的俯視地圖（2026-08-15 實拍 `shots/31-map.png`）| 先渲染出來看，方向箭頭讓人直接斷成「自動地圖圖磚」—— 然後同一輪裡截圖把其中兩張指到了 Protection 面板。**渲染看得出圖磚長什麼樣，看不出它被畫在哪裡**；要斷用途得有畫面或繪製端的座標 |
| `_2play_e14`（俯視地圖）全檔只有定位術一個呼叫端 | **有兩個**：`0x172B2` 那個 thunk 確實只有 `2CAST1` 用，但 `2PLAY` 的指令分派 `0x18189` 直接近呼叫同一個進入點 —— 那是 `M` 鍵 | 掃指令分派時撞見的。**「只有一處」若只掃了 thunk，就只證明了 thunk 只有一處**；同一個映像裡的近呼叫不經 thunk |
| 法術跳表：`2CAST1` 97 筆 @ `0xD1CE`、`2CAST2` 97 筆 @ `0xD080`，兩表重疊 5 條、聯集 97 條 | **`2CAST1` 96 筆 @ `0xD1CE`、`2CAST2` 92 筆 @ `0xD084` 且索引是「法術編號 − 2」**。43 + 53 = 96，**重疊 0、缺漏 0** —— 兩張表把 96 條剛好分完 | 原本的表頭是拿「重疊最小、聯集最大」的統計挑的，而那個統計只固定得了兩張表的**相對**對齊，定不了絕對位置。補上兩個結構條件（每一筆是預設出口或 6 對齊的 trampoline、表尾正好接上預設出口）才夾得住；而結構條件單獨用又會漏掉那兩筆錯位。**三個條件缺一不可** |
| 事件表的 `Kind` 高 nibble 是「類型」（走到／搜尋／開門），待驗 | **是方向遮罩**：bit7 北、bit6 東、bit5 南、bit4 西。`2PLAY sub_1A8C4` 在 Cell 相符後 `test Kind, ds:16D2[朝向位移]`，面向不對整筆跳過。remake 原本完全不看它，2,730 筆裡 1,202 筆（44%）的觸發條件是錯的 | 追「誰讀 Kind」時掃 `ds:6052` 的參照，`sub_1A8C4` 連讀三個位元組正是三個欄位。資料側四個單方向值各約 250 筆、幾乎相等 —— **類型碼沒有理由在四個單一位元位置長出四個等高的桶** |
| Mega Drive 還沒歸類的那 150 個 LZSS 區塊「多半是掃描的假陽性，只有一個在 ROM 裡有人指到」 | **全部有結構**：74 張怪物 nametable（`raw % 242 == 0`）、74 個 tile 池（`raw % 32 == 2` 且張數欄與長度相符，其中 68 個是帶頭那族的重覆）、2 張小 tilemap。`other` 歸零 | 拆長度分布就看出來了 —— 十幾種長度各出現四到十三次，假陽性不會這樣分佈。**「有沒有人指到」這個過濾器在這一題上零證據力**：150 個裡只有一個命中，而其中至少 80 個確定是程式在用的東西。同一個過濾器在調色盤那一題上決定性，在這裡有洞 —— 過濾器沒中不等於東西不存在 |
| `ds:1436` 減 `0x11` 之後當偏移用是原版算錯了，remake 不能照抄 | **一減一加抵銷**：`sub_1B70C` 減 `0x11`，root `sub_13928` 取偏移時算的是 `記錄[0x11 + 值]`，傷害那條路讀的就是對的抗性欄位。減這一步是為了讓真正的偏移不會撞進 0／1／2／3／0xFF 五個特殊號碼。只有上狀況那條路（`sub_1B2DE`）沒加回來，那才是漏洞 | 為了實作去讀 root 的 `sub_13928`。**「這個算式看起來不合理」在讀完使用端之前只是假設** —— 減法的意義在加法那一端 |
| 怪物特殊攻擊只有三十種 | **三十二種**。碼 30（`paralyze`）與 31（`swarms`）真的有怪在用 —— Shaman、Strangler、Seductress、Priest 與 Insect Plague、Killer Bees、Swarming Wasps | 實作前先統計 256 隻怪的 `SpecialIndex`，最大值是 31。`specialCount = 30` 讓那七隻的招式查無此碼 |
| 怪物記錄的 `SpecialIndex` 為 0 表示沒有特殊攻擊（143 隻） | 碼 0 是 `sprays poison`。143 隻碼 0 的怪裡有 **140 隻機率也是 0**（那 140 隻才是真的沒有），另外三隻 Swamp Thing／Dino Spider／Death Spider 機率 90 —— 蜘蛛噴毒。**判準是 `SpecialChance`，不是碼** | 同一次統計。碼與機率兩個欄位的零值數量差三，差的就是那三隻 |
| 群體攻擊一次打 3–6 個位置 | **走 4–7 格**（`rand(1,4) + 3`），而且走到空位或倒下的人那一格白費、照樣算一次。六人隊碰上第 1 組順序表（`7 6 5 4 3 2 1 0`）時，前兩格永遠落空 | 為了實作逐條讀 `sub_1B116`，`add al, 3` 接在 `rand(1,4)` 之後 |
| (7,5) 的旅店招牌是「非阻塞標示，所以截圖看不到對話」 | **根本沒有觸發**。那筆 `Kind = 0x20` 只認面南，而重測是面北走上去 | 同一份 playtest 表裡第 1、3、4 步的 `Kind` 都是 `0x80`（北）且三步全部有反應 —— 只有方向不同的那一步沒有。「非阻塞」的解釋能說明「不攔輸入」，說明不了「一個字都沒有」 |
| `ds:5AD6`（屬性層）是執行時算出來的，不是地圖檔的第二個 256 bytes | **就是檔案的第二層。** `2PLAY sub_1BE24` 換圖時呼叫 `sub_160D0(0x5AD6, 地圖號)`，與地形層的 `sub_160B4(0x59D6, …)` 是同一支 `sub_160ED` 的兩個入口，差別只在記錄內偏移 `0x100` 對 `0` | 從門的問題反追：先掃出屬性層的全部寫入端只有六處，再看誰持有那六個緩衝區的位址，`sub_1BE24` 一次列出全部六個。原本的判斷來自「有一段程式碼會寫 `[si+5AD6h]`」—— **有人寫它不代表它沒有來源**，而 `06-map.md` §1 早就寫著檔案 `+0x100` 是屬性層，同一份文件自相矛盾了很久 |
| 一般寶箱的內容來源還沒找到 | **原版沒有那條路徑。** `ds:0434` 與 `ds:6950`–`695F` 掃過十五個資料庫，寫入端共八處，內容只來自事件腳本（`2PLAY sub_1A1A0`／`sub_19B44`）與戰鬥戰利品（`2COMBAT sub_19A3C`／`sub_188FC`）；`ds:0434 == 0` 是清空後的預設狀態，不是「等人來填」 | 把「找不到來源」當成缺口掛了一輪。改成掃全部寫入端之後，清單本身就是答案 —— **要證明某條路徑不存在，得掃完全部寫入端，不是找不到就繼續找** |
| Mega Drive 的 tilemap 不在壓縮區塊裡 | **在**。`0x06F520` 解出 4,100 bytes，扣掉 4 bytes 檔頭正好 4,096 ＝ 64×32 cell × 2 bytes，由 `sub_42FC` 解壓後送進 `0xFF3400` | 追 `sub_42FC` 載入的兩個來源時發現。舊掃描要求「16 bytes 區塊頭 ＋ `F0FF` magic」，而這個區塊只有裸的 `compSize`／`rawSize`／位元流。改用解壓常式的呼叫慣例當結構條件重掃，全 ROM 從 72 個區塊變成 334 個 |
| MD 版原版進旅店不換曲，音樂包的 `inn` 沿用 `town` | **旅店有自己的曲子** `0x0BA608`（case 11）。走進 Middlegate (7,3) 的旅店入口，`sub_B620` 命中 `d0=0x0B`、呼叫端 `0x18BCA` | 原本的理由是「旅店程式在 `0x28xxx`，那一區零個選曲呼叫端」—— 但那一區是**旅店的名冊畫面**，走進城裡旅店入口跑的是設施處理區（`0x18BCA`）的另一段。**用「某個字串附近沒有呼叫端」推「這個場景不換曲」是無效的**：字串與呼叫端本來就可以在不同的函式。改用中斷點在遊戲裡走一次，一次就分開了 |
| `ds:584E` 的 0–25 排列「只寫不讀，是被砍掉的功能留下的死碼」；remake 的貨色輪替因此算是「讓死碼有用途」 | **它是結局控制室密碼題的替代字母表。** 唯一的寫入端 `sub_1D19C` 只被 `sub_1D2A4` 呼叫，讀取端是同一支 overlay 的 `sub_1D1FC`。貨色輪替仍然是 remake 自己加的功能，但理由不是「死碼」，而是原版的貨架本來就是死的 | 讀 `2SMITH` 的控制室那一段時撞到的。原本的掃描比對 `[si+584Eh]` 與 `[bx-A7B2h]` 兩種印法，而**真正的讀取端把基底摺進了常數**：`[si+580Dh]`（索引 `A`–`Z`）與 `[si+57EDh]`（索引 `a`–`z`），兩者加上索引都等於 `0x584E`。**掃一塊記憶體的位址掃不到「基底被摺過」的存取**，零命中與「真的沒人讀」長得一模一樣 |
| MD 版的設施音樂只能是強推論，要升級得先解出文字索引表 | 五個設施（旅店 11、鐵匠 13、酒館 14、神殿／公會 15、訓練所 16）全部**已證實**，而且不必解文字索引表 | 靜態確實到頂了（沒有字串指標表），但那只堵死了靜態這條路。把遊戲跑起來、在選曲函式下中斷點、走到那家店，`d0` 與回傳位址就是遊戲自己的答案，截圖說明玩家看到的是哪一家 |
| MD 的 18 首曲子只有 7 個 case 被用到，其餘是死分支 | **21 個 case 幾乎全部有人用**。`sub_B620` 的呼叫端有 39 個，其中 28 個走 a5 thunk 表（`jsr $22E(a5)`）| 只掃了直接 `bsr`／`jsr`／`jsr abs.l` 三種形式。這個編譯器的跨模組呼叫預設走 thunk，**直接呼叫只佔四分之一**。教訓寫成規則：掃呼叫端時，先查目標函式在不在 thunk 表裡，在的話連 `jsr d(a5)` 一起掃 |
| Mega Drive 的第一人稱視窗是 14×14 個 cell | **還沒證明**。那段 14×14 的疊加在 `sub_706` 裡，而 `sub_706` 只從 reset 路徑呼叫一次 —— 它畫的是開機那一幕，第一人稱用不用同一套機制沒有驗過 | 追 a5 的時候順手確認 `sub_706` 的呼叫者，只有 `initial_pc_entry`。原本是從「14×14 ＝ 112×112 像素，尺寸像 3D 視窗」推的 —— **尺寸像不是證據** |
| MSX 的磁區表只有一張、涵蓋 407,711／737,280 bytes，圖形在表外 | **表有兩張**：磁區 1–3 與磁區 4–6，各 192 筆。第二張才有圖形檔（`0x20xx`／`0x21xx`／`0x30xx`）。兩片共 553 筆 | 追「畫圖時 `ld bc,2020h` 的 id 2020 在哪」時，表裡查無此筆。查表那一段（`sub_C5F5`）讀的是 `ld b,3` **三個**磁區，沒中再讀磁區 4 起的三個；`sub_C698` 的迴圈計數 `0C0h` ＝ 192 也對得上。只讀第一張表**完全解得出一批合法檔案**，所以沒有任何症狀 |
| MSX 的圖形要整片掃磁片才找得到 | 圖形檔各自有 id，照表取即可（56 張，比掃描多 12 張）。掃描版留在 `tools/msxdsk.py` 的 `images()` 當考古 | 同上。掃描當時的三個驗收條件本身是對的，錯的是前提「表外的那一大塊沒有索引」|
| `SPELLS.DAT` 是 LZW 段，解出 256 bytes | **不是壓縮檔**，是 192 bytes 明文。判準是段頭第二個 word 必須為 0，它是 `0x0180` | Go 版的位元流耗盡檢查比 Python 版嚴格，報「EOF 碼之前就用完」。Python 版超出範圍時補零繼續讀，湊出「長度剛好對」的假陽性 |
| `STR.DAT` 解密是每 byte −4，得到大寫單字表 | **是 +0x1C**，直接得到大小寫與標點都正確的可讀文字。−4 讓字母落在大寫 ASCII 範圍，看起來像對的，但句首字母會變成 `!`/`#`/`)` 這類符號 —— 那正是被誤讀的小數點：實際上是**小寫字母**沒被 +0x20 |
| 長文字是單字索引組出來的 | 不是。`STR.DAT` 就是完整的行文字，解密解對之後直接可讀。之所以會誤判成「單字索引」，是因為 −4 的錯誤解密把空格（`0x00`）留成分隔符，看起來像單字表 |
| 角色記錄的年齡在 `+0x27` | **在 `+33`**。`+0x27` 的名冊值域是 6–45（含 14 這種不合理的年齡），`+33` 是 18–42、預設角色全是 18，與手冊 p.34 的人物資料截圖 `Age=18` 一致 | 只用六個預設角色看不出來，兩個欄位都像年齡；拿 `ROSTER.DAT` 的四十個練過的角色一比就分開了。`+0x27` 現在標為未解 |
| 牆是 2-bit 欄位（值 0 無牆 / 1 牆 / 2 另一種牆 / 3 門） | **是單一位元**。`and 55h` 只取每個欄位的低位元，高位元屬於另一個平面。當成 2-bit 欄位時自洽率 81%，改成單位元後 93.8% | 「值 3 = 城門」是看了牆線圖底列的四個就推廣的。寫成測試（門應該全在外圈）當場打掉：21 面裡只有 5 面在外圈，而且 56 面值 2/3 全部集中在南向 —— 因為 bit 7 同時是南向的高位元 |
| 用原版按鍵量到的 11 個通行樣本 | **不可信，已棄用**。任何位元／欄位／層的組合都與它零相關（值 0 五可三不可） | 判讀方式是比對 3D 視圖區的畫面差異，但踩到事件格會彈訊息吃掉後續按鍵 —— 畫面不動被記成「走不動」，而且一旦錯一次，後面整串都是錯的 |
| 「進城」之後的起點是 `ATTRIB` `+14` 那一格 (7,5) | **是 (7,3)**。記憶體 dump 讀 `ds:0393`／`ds:0394` 直接量到。`+14` 是地圖的預設進入座標（傳送帶 `0FFh` 時用），「進城」選單走另一條路 | 引擎從 (7,5) 面西算得出七步暢通，DOSBox 實機第四步就 `Solid!`。原本懷疑牆模型，改用實測起點 (7,3) 重算只剩一步，與實機一致 —— 錯的是起點不是牆 |
| Middlegate 起點是 (7,10) | **是 (7,5)，掛著「中門旅店」招牌的那一格**。`ATTRIB.DAT` 的 `+14` 是每張地圖的預設進入座標（低 nibble = X、高 nibble = Y），地圖 0 的值是 `0x57`。旅店的**入口**（opcode `0e 01`）另在 (7,3) —— 招牌格與入口格是分開的兩格 | 原本的 (7,10) 是從「面北走四步進神殿」回推的，而牆規則解出後那條路走不通。`+14` 是程式碼直讀（`sub_1B5EA` 與 `sub_5E68` 兩處都把它拆成兩個 nibble），不靠回推 |
| 「(7,5) 是從角色選擇進城的起點」 | **已推翻：新鮮 DOSBox 從 `G`／`Z` 進城的記憶體 dump 是地圖 0、(7,3)、面北。**(7,5) 仍是 `ATTRIB.DAT +14` 的傳送預設入口，兩條進入路徑不同 | 2026-08-12 以一次性原版副本重測：從 (7,3) 向北，第 3 步顯示 `Gateway Temple`，第 4 步才出現神殿 Y/N 對話；見 [`docs/playtest/01`](docs/playtest/01-oracle-timeline.md) §6、§7。保留上一列，因為它正確記下 `ATTRIB` 欄位，錯的是把它外推成主選單進城位置 |
| DGROUP 整段是 BSS，查表只能靠執行時的記憶體 dump | **EXE 尾部那 43,472 bytes 就是 DGROUP 的初值段**，對應 `EXE 檔內偏移 = DGROUP 偏移 + 0x8630`。十張表位移一致、各自只命中一處，與記憶體 dump 的值逐項相同 | 找特殊攻擊的播報字串時，先在 `MM2.EXE` 裡直接搜到了它們，而那些字串的指標表就在「BSS」裡。IDA 顯示 `db N dup(?)` 是因為它只載入 MZ header 宣告的 34,320 bytes |
| 物品區是「12 個編號接 12 個屬性」 | **是六組平行陣列**：已裝備的編號／第二欄／屬性在 `+40`／`+46`／`+52`，背包的在 `+58`／`+64`／`+70` | 只看了 `sub_CE12` 的 `+0x28`／`+0x34` 就推廣成連續兩排。給予物品的 `0x19` 直接寫 `+0x3A`／`+0x40`／`+0x46`，與那個推廣衝突；照新排法算出來的武器骰面數與存檔裡的 `+76` 逐筆相同 |
| 盜行在角色記錄的 `+116` | **在 `+30`**。`+116` 是人物畫面讀取的**有效生命上限**，`+96` 是基礎上限；原始名冊兩者恰好相同，不能據此當成冗餘副本。`+30` 才是盜行 —— 四十筆裡六個非零，職業全是賊或忍者 | 「賊類明顯偏高」是看了值域就下的結論，沒有真的按職業分組。分組一算：武士平均 6.2、遊俠 43.7、野蠻人 50.0，與賊完全無關 |
| 狂暴術扣的是運氣 | **扣的是 `+115`，而 `+115` 是耐力** —— 與手冊說明一致 | `sub_1CC64` 直接 `dec [bx+73h]`。「`+115` 是運氣」這個中間結論來自「屬性區六格含耐力」，那一條後來被下面那列推翻；欄位位址一直沒變，變的是欄位叫什麼 |
| 商店進貨的洗牌（`sub_1D19C`）決定貨架順序 | **那個排列沒有人讀**。位元組序列 `4E 58` 在十四個映像裡只出現一次，就是寫入指令本身；DGROUP 初值段裡也沒有指向它的指標。商店賣什麼完全由 `ds:43C8` 起的貨架表決定 | 先前只掃過 `[si+584Eh]`／`[bx-A7B2h]` 兩種反組譯印法就停在「讀取端未找到」。改掃**位元組序列**繞過印法問題，再加驗初值段沒有指標，才排除掉間接定址 |
| 角色記錄的屬性區可能是七格（角色創建的擲骰程序處理七格）| **記錄是六格**（`+16`–`+21`）。那七格是創建時的暫存，沒進區塊的第七項（耐力）另外存到 `+39`／`+115` | 名冊四十筆的分佈：`+16`–`+21` 每格三成以上落在擲骰值域 3–21、平均 15–17，而 `+22` 只有 1/40、平均 6.0 且多數為 0。擲骰的下界是 3，`+22` 有一大半是 0 —— 它從來沒被擲骰填過 |
| 角色記錄 `+38` 的 `0x10` 是「衰弱」（急救術會清掉的那一項）| **是沈睡**。喚醒術（`sub_1CBEC`）整隊掃過去只做 `and [記錄+38], 6Fh`，而 `0x6F` 在那條路徑上唯一清掉的就是 `0x10` | 原本是從「急救術會清掉它」反推的，但急救術清的是 `0x2F` 一整組（`0x10`／`0x40`／`0x80`），推不出單一語意。喚醒術只清一個位元，而且名字直接說了那是什麼 |
| 物品記錄 `+0x0D` 是**可用**職業的位元遮罩 | **是「禁用」不是「可用」**。裝備指令（`2CMDS.img` `0x1CA01`）`test 物品[+0Dh], ds:3408[職業]` 之後**非零才是錯誤**；`ds:3408 = [80,40,20,10,08,04,02,01]`，職業 0 是最高位 | 資料只有這個讀法說得通：**41 件近戰刀劍類全部設了牧師那個位元、零例外**，棍棒類一個位元都沒設，Ring Mail 禁巫師與野蠻人。反過來讀的話牧師就只能用刀劍。欄位名（先前寫「可用」）不是證據，要追到讀取端看它怎麼用 |
| 怪物記錄 `+20` 的高 nibble 是「每輪最多行動幾次」，`sub_1847E` 的擲骰照面值實作會讓 140 隻怪整場不動，所以先不實作 | **那一擲決定的是攻擊種類，不是行不行動**。`sub_1847E` 回傳 0 時呼叫端改走普通攻擊（前排近戰、後排要 `ds:9E32` 且擲 80%）。所以 `+20` 高 nibble 是**特殊攻擊的可用次數**，`ds:9E25` 是**使用機率**，140 隻機率 0 正是「這些怪沒有特殊攻擊」 | 追 `sub_1847E` 的**呼叫端**而不是只讀函式本身。定案的判準是 b17 的兩半互相對照：低 5 位是特殊攻擊型態、高 3 位查出機率，而「有型態卻機率 0」**一隻都沒有**（140/3/0/113）。原版那個 `>> 4` 確實寫錯位移（應為 `>> 5`），索引走到表後面的字串上，照抄即可 |
| 心智渙散（狀態 bit6）讓怪物完全不行動 | **是用不了特殊攻擊**，普通攻擊照舊 | 同上，`0x184A1` 在 `sub_1847E` 內，而那支函式管的是攻擊種類 |
| 怪物的屬性抗性來自 `b22` 的高兩位（冷／酸）與 `b17` 的低三位（睡／法術狀態），抗魔法索引在 `b17 >> 5` | **五格讀錯了位元組**。七格抗性攤在 `ds:9E36` 起的**連續七個位址**，`sub_18674` 直接 `ds:9E36[屬性-1] != 0`，所以位址就是索引：冷／酸來自 **b24** 的 bit6／bit7，睡／法術狀態／第七格來自 **b25** 的 bit1／bit0／bit2，抗魔法索引是 **`b25 >> 5`** | 先從 `sub_18674` 拿到基底 `ds:9E36`，再用「位址即索引」回推每一格的寫入處。名字是獨立判準，四格全部落點：四隻 Fire 全有火抗、Frost Dragon 與 Snowbeast 有冷抗、Acidic Blob 有酸抗、Lightning Bugs 有電抗。抗魔法換成 b25 之後八個階層才用滿，而且平均難度層一路從 5.9 遞增到 13.7 —— 舊的「只用到 0–6，第八格沒人用」正是讀錯位元組的症狀 |
| `ATTRIB.DAT` `+13` 是遭遇難度的候選（在 `2COMBAT` 被拿去跟 `rand(1,100)` 比，語意未定） | **是戰鬥中「溜跑」的成功率**。讀取端 `0x19162` 所屬的 `sub_1914A` 就是戰鬥指令跳表 `0x19578` 裡 `R` 那一格；擲 `rand(1,100)` 小於它就成功，而且**跑掉的是下指令的那一個人**（`ds:9FD2`），不是整隊 | 反過來從跳表找呼叫者，而不是從「它像什麼」猜。定案之後值的分佈才對得上：**五座城鎮一律 100**（在城裡打不贏一定跑得掉），野外 20–90 —— 當成難度或遭遇率解釋不出「城鎮全滿」這件事 |
| 怪物記錄 `+17 >> 5` 是「每次輪到時真的行動的機率」，而且索引會走到 14、一半落在 `ds:4DC0` 表外 | **是抗魔法百分比**，索引在原版是 `(b17 >> 5) & 7`，**`& 7` 保證落在八項之內**。用法在 `sub_1714A`：非 0 時擲 `rand(施法者等級, 90)`，抗性大於擲值就擋下整個法術 | 追 `sub_1714A` 的抗性判定時追到 `ds:9E31`，再回頭找它的寫入處（`2COMBAT.img` `0x13E1E`）。表的值 `[0,10,20,35,50,75,90,100]` 是一道乾淨的百分比階梯，而擲值上限 90 讓最高階正好是「免疫」—— 當成行動機率解釋不出這個上限。實測 256 隻的索引只用到 0–6，第八格沒人用到 |
| 攻擊法術的傷害範圍照手冊（火箭術 2—8、火焰枷鎖 100—400……） | **以程式碼為準**。`2CAST2` 十支 handler 的擲骰式逐條讀出，上界與手冊逐條相符，**下界只有月光術對得上**（火箭術實際 4—8、火焰枷鎖 255—400、隕石雨 25—50）。裁決順序裡手冊是第 4 順位 | 手冊寫的是骰子式範圍（2d4、10d10……），實作改成了 `rand(1,N) + M` 這種單擲加常數；同一次改寫沒有把下界跟著調回來。`sub_11C88` 的餘數法確認回傳值兩端都含，不是換算誤差 |
| `MONSTERS.16` 的像素編碼「高 nibble + 1 = 長度、低 nibble = 顏色」逐張吻合 0/433，已排除 | **那就是正確答案**。59 張基準圖 render 出來是可辨識的蜘蛛、甲蟲群、龍、骷髏、騎士、城堡 | 驗收條件寫錯了：影格與下一個偏移之間有填充位元組，把整段吃完再比對像素數，每張都會多出一點，「逐張吻合」因此永遠是 0。總量比值 1.020 早就在喊「差 2%」，卻被讀成巧合 |
| `MONSTERS.16` 的解碼器在顯示驅動裡，`EGA.DRV` 跳表第 22 項就是它 | 第 22 項是**字型展開**（`si` 由 0x20 跑到 0x7F，每個字元把 6 個 word 的 1bpp 圖樣攤成 EGA 四平面）。像素編碼在資料本身，不必追驅動 | 把那 771 bytes 整段讀完，看到的是 ASCII 可列印範圍的迴圈 |
| 圖號指到 `MONSTERS.16` 的空槽是壞資料 | **是往後借下一張圖**，掃到第 75 項回繞。16 個空槽裡有 12 個真的有怪物指過去 | `sub_6818` 的 `dx | cx == 0 → si += 4、索引 += 1` 寫得清清楚楚。只看檔案永遠猜不到 |
| 單獨把 `.OVL` 丟進 IDA 就能反組譯 | overlay 與 root 共用段基準，near call 會跨界，單獨載入時每個位移都算錯。要重建執行時佈局 | 1MENU2 的進入點第一件事就是 `call near ptr 0F052h`，目標在 overlay 自身長度之外 |
| 12 段事件檔的段不符合事件表佈局，結構留未解 | **一段未解都沒有**。11 段是**沒有事件表的腳本庫**（編號 60 以上，`uint16 skip` + `0xFF` 分隔），第 12 段（`EVENTSO` 段 37）是被過濾器誤殺的正常事件表 | 判準改成位置不是內容（`raw[2] == 0xFF`），並停止拿「Kind 低 nibble 恆為 0」當條件 —— 整份資料只有一筆反例，卻讓一整段（47 筆事件、14 條腳本）消失。`TestNoIrregularSegments` 現在逐段歸類，把「解不開」變成會失敗的測試而不是一句備註 |
| 怪物名與物品名的譯文用**原文**當鍵查 | **鍵是編號**（`monster.%03d`／`item.%03d`）。用原文查一條都查不到，而查不到的行為是顯示原文 —— 512 個名字整批沒有中文，畫面上只看得出「還沒翻」，不會有任何錯誤 | 對照 `cmd/mm2play` 的同一段（它用的是編號）發現。`TestItemAndMonsterNamesAreTranslated` 現在守著 |
| 排版的字寬各處自己算一套 | **一律問 `render.TextStyle.Advance`**。目前是漢字 24 px（全形）、英數字 12 px（半形）；換字型時這個比例會變，而估算若不跟著改就會漂移 | 兩次都在建角畫面的兩欄上現形：先是中文被算成兩倍寬（那時中英同寬），改掉之後又因為英文換成半形而反過來。**教訓不是某個數字，是「估算要跟著渲染器」** |
| 角色記錄 `+0x10` 那六格是力量／智慧／人格／**耐力**／速度／準確度，運氣在 `+39`／`+115` | **六格是力量／智慧／人格／速度／準確度／運氣，`+39`／`+115` 是耐力** | 四條獨立證據：建角色的寫入處 `sub_18624` 把畫面第 4 格（耐力）寫進 `+0x27`／`+0x73`；大腦淨化 `sub_1C5CA` 扣的欄位與手冊的技能效果七比七全中；原版自己的六項加值標籤表（`ds:4318`）就是 Might/Intellect/Personality/**Speed/Accuracy/Luck**；六個預設角色的峰值落在自己職業門檻要的那一項（弓箭手在準確度、賊在運氣）。**錯的代價是建角色寫錯欄位** —— 七格照序號抄進六格，耐力之後整批錯開一位 |
| 寶箱陷阱的傷害公式還沒解，先用箱子難度推一個值 | **與地圖陷阱是同一條公式、同一張表**，而那張表早就在 `data/traps.json` 裡 | `sub_1C4A6` 只做畫面閃爍與播報，結算在它呼叫的 `sub_1C390` → `sub_1C338`。**停在第一層函式就下「還沒解」的結論**，而答案在下一層 |
| 屬性層 bit 5/3/1「不是牆的性質」之後就停在那裡 | **是整格的旗標**：bit 3 禁休息、bit 1 禁施法、bit 5 沒人讀 | 統計只能削弱假設、不能定案。決定性的問題是「程式有沒有讀它」，而**讀取點在快取 `ds:59C8` 不在平面** —— 掃錯地方就會一直回空，而回空與「真的沒有」長得一模一樣 |
| 第一級的生命與法力原版怎麼給還沒解，先沿用升級那一套 | **是查表**：`ds:6E6[職業] + ds:6F2[耐力]`，法力查 `ds:71E` | 答案在 `sub_18624` —— 上一輪為了屬性順序讀過同一支函式，因為只在找屬性偏移而讀過去了。**讀一支函式時要把它寫的每一個欄位都記下來** |
| 出手順序與防護等級加成讀耐力，與手冊寫的「速度」不符，手冊是二手資料所以以程式為準 | **兩者都讀 `+110`，而 `+110` 是速度 —— 手冊本來就是對的** | 屬性順序改對之後這個「不一致」自己消失了。**假的矛盾會養出假的規則**：當時的結論是「手冊在這一項不可信」，而真正的問題在我這邊 |
| 事件腳本的 opcode「已認得 37 個／49 個」 | **49 種全部有解**，而且直譯器全部有分支 | `TestEveryOpcodeInDataIsHandled` 拿 switch 的 `default` 分支當量測 —— 不另外維護一份「有解名單」，名單會與程式漂移，而這正是當初漂移的原因 |
| 盜行在 `+116`、第七項屬性的位置未定 | **盜行在 `+30`，沒進六格區塊的第七項（耐力）在 `+39`／`+115`；`+116` 是有效生命上限，不是盜行** | 同一列裡兩句話互相矛盾（前半說 `+116`、後半說 `+30`）。矛盾的列比錯的列難發現，因為兩個讀者各看到一半都覺得沒問題 |
| Amiga 版的 `.32`／`mm2` 全部抽壞了，而**建立在它們之上的每一條結論都作廢**（`mm2` 不是 hunk 檔、A4 = 0x10000 或 0x2F324、目錄在 `0x875C`、「三個場景檔的目錄同位置代表固定緩衝區」、走 `udos.library`）| **`mm2` 就是 Amiga hunk 檔**（開頭 `00 00 03 F3`），`.32` 的目錄在**偏移 0**，調色盤緊接其後。舊抽取程式沒有剝掉 OFS 資料區塊那 24 bytes 的標頭 —— **長度正確、內容從第 1 個位元組就錯** | 抽 `libs/` 時發現舊的抽取只做了根目錄，重寫成會遞迴的 `tools/adf.py`；新抽出來的 `mm2` 開頭是 hunk magic，與舊檔第 1 個位元組就不同。**壞資料看起來完全像「壓縮過的資料」** —— 熵合理、有疑似結構、自相關量得到週期，於是花了很多輪在對垃圾做解壓實驗。教訓寫成規則：**拿到抽出來的檔案，先驗一個與內容無關的外部判準**（可執行檔的 magic、已知檔頭），不要直接開始解格式 |
| Amiga 版 `mm2` 的 A4 = 0x10000，四個檔名逐字精確命中所以值是對的 | **A4 = 0x2F324**（thunk 表起點 `0x2732C` + 0x7FF8）。定址窗 `[0x27324, 0x37324)` 在資料段與 BSS 上，**蓋不到程式段裡的檔名表**（0x85C2–0x8716），所以那些字串本來就不是靠 A4 取的 | 「命中檔名」不是驗證：A4 的窗有 64 KB，任何一個 A4 都會讓一堆位移落進某個字串裡。改看結構就一次定案 —— 3,237 條 `jsr (d16,a4)` 的位移有 236/249 是 6 的倍數、最小值恰為 `−0x7FF8`，6 bytes 一格就是 `jmp abs.l`，檔案裡唯一那張表在 `0x2732C` |
| 角色記錄 `+38` 的 `0x01`／`0x02`／`0x20` 與整個位元組 `0xFF` 語意未知，只知道誰造成它們 | **`0x01` 詛咒、`0x02` 沈默、`0x20` 麻痺、`0xFF` 抹消**（`is cursed`／`is silenced`／`is paralyzed`／`is eradicated!!`）| `2COMBAT` 的 `ds:1456`（位元值）與 `ds:145C`（訊息索引）把位元直接配到 `ds:106E` 的十六則訊息；另一條獨立證據是特殊攻擊三十路跳表的 case 1–16 與那張訊息表逐項對齊。**先前只從「哪支法術清哪幾位」單向推，治不到的位元就永遠推不出來** —— 反方向（誰造成它）才有答案 |
| Mega Drive 第一人稱視野用 ROM `0x6FBEA` 的**第 0 條**調色盤（拿它畫出來灰石、白雲、赭紅地磚，整幅自洽）| **由區域類型決定**：類型 0 → 第 1 條、類型 1 → 第 2 條、類型 2／5 → 第 0 條、戶外 → 第 3 條（`0x8166` 起那段依 `-$4C6(a5)` 抄不同的 32 bytes）。remake 用的方石砌那一套是類型 2／5，所以是第 0 條 | 視野那塊 nametable 的項值 bit14–13 ＝ 10 ＝ 調色盤線 2；再拿執行時的 CRAM 對回去，執行時取樣的那一幀是區域類型 1，CRAM 第 2 條與 `0x6FC2A` 的每個 3-bit 分量差剛好右移 2 位（光照＝2，公式是 `分量 × 亮度 ÷ 8`），十六格全中。**「畫出來像合理的畫面」不是證據** —— 四條線畫出來都合理，差別只在雲是白的還是土黃的；要分辨只能看誰參考它、以及執行時實際載進 CRAM 的是哪一條 |
| Mega Drive 版城鎮用的是「方石砌」那一套場景素材（`0x06FFAE` 起，牆上有壁燈，看起來最像 DOS 的 `TOWN.16`）| **城鎮是區域類型 0，用的是亂石砌那一套**（`0x0795EC` 起），配第 1 條調色盤 —— 藍紫色的石頭 ＋ 紅褐地磚。方石砌那一套是類型 2、5 | 實機截圖：Middlegate (8,0) 面東，`0xFFFE4C`（區域類型）＝ 0。**先前是拿「哪一套最像 DOS 的城鎮」推的** —— 那是二手推論；區域類型的值與跳表才是一手資料。同一批亂石素材在類型 0 與 1 底下是不同顏色，所以「看起來不像同一個地方」也不能當反證 |
| MSX 的第一人稱是「側牆一支函式、正牆一支函式」（`sub_1A40`／`sub_1C2B` 側牆、`sub_1103` 正牆）| **是三種牆面材質各一支**：`sub_2A57` 依地圖格的牆值分派（1／2 → `sub_1103`、3 → `sub_1A40`、4 → `sub_1C2B`），每一支自己畫左右側牆與正牆、四個深度 | 追「深度 2 的正牆為什麼切不出圖」時把三支的來源矩形全列出來，發現 `sub_1A40` 與 `sub_1C2B` 的每一筆正好差 98 px —— **同一個形狀平移，不是兩種用途**。連帶量出 remake 的 MSX 牆混了兩種材質 |
| MSX 的素材不在 `F6` 循環裡，是因為「這台機器沒有 MSX 的 `.dsk`」（寫進過 commit `57f9c46` 的訊息與 `cmd/mm2shots` 的註解）| **磁片在，壞的是素材表**：`sub_1103` 深度 2 那筆貼圖矩形右緣落在 490、素材表只有 462 寬，`Scene` 那一格回 `nil`，`requireTownSet` 因此打掉整組 MSX ——而 loader 的失敗屬於「玩家不一定有那份原版」那一類，不印任何訊息。連帶讓版控裡那張 `01f-first-person-msx.png` 其實是高解析素材包的畫面。| 盤點「還剩什麼工作」時順手 `ls workplace/msx/` —— 磁片四份都在。回頭讓 `selectTheme` 把 loader 的理由接進錯誤訊息，一問就答：「MSX 素材組火炬第 6 張無法解碼」。**「查詢回空」與「查詢的東西壞了」要分得出來**，否則前者會被當成環境問題結案 |

DGROUP 那條的教訓獨立成一條規則：**段落屬性描述的是載入器被告知了什麼，
不是檔案裡有什麼。** 要判斷某塊資料有沒有初值，量實體檔案。

前兩條同源：**長度對得上不等於解對了**。任何「剛好整除／長度吻合」的證據，
都要再找一個獨立的驗證面（第二個實作、可見字串、render 出來的畫面）。

牆那三條同源：**資料自己能檢驗的事，不要用 oracle 去猜**。牆有兩面、
城鎮要封閉、素材寬度要對得上 —— 這些約束不花成本、不會被操作失誤污染，
而且一次就把 192 種排列篩到只剩一種。用按鍵樣本回歸則耗了十幾輪，
最高只到 12/14，還推出三個錯結論。**先問「這批資料自己有沒有內部約束」，
再考慮去外面量。**

## 6. 工作紀律

- 動手前查 `~/.claude/rules/00-rules-index.md` 的觸發表，命中就先讀對應 rulebook。
- 每一輪：反組譯或實作 → 更新 markdown → 清掉被推翻的斷言 → commit + push → 更新本檔 §1.5。
- 宣告完成前重跑完整 Go 測試（容器跑法見 [`AGENTS.md`](AGENTS.md) §2），並留下畫面證據。
  編譯成功不是視覺測試。
- 推論等級標籤：`已證實` / `強推論` / `假設` / `未知`。`已證實` 要附得出證據。
- 追函式前先查 [`docs/re/00-function-index.md`](docs/re/00-function-index.md)，解完新函式後重跑索引。
